# SETUP — Claude가 따라 하는 설치 매니저

> 이 문서는 사람을 위한 설명이자, Claude에게 주는 설치 지시서다.
> 사용자가 이 저장소 링크와 함께 "설치해줘"라고 하면 Claude는 아래를 순서대로 수행한다.
> 각 단계는 실패하면 멈추고 원인과 해결 방법을 보고한다 — 조용히 건너뛰지 않는다.

## 0. 헬스체크와 사전 합의 — 사용자 접점은 카드 한 번뿐이다

이 설치의 사용자 접점은 **0-0의 합의 카드 단 한 번**이다. 카드 이후는 7절 완료 보고까지
전 구간 무인으로 진행한다 — 중간에 질문을 만들지 않는다. 질문이 생기면 그것은 절차의
결함이므로, 디폴트로 진행하고 완료 보고의 '발견 사항'에 남긴다.

### 0-0. 사전 합의 카드 — 설치 시작 시 단 한 번

필수 충족 조건은 티어로 가른다 — 우선순위가 곧 카드의 순서다:
- **T0 차단 조건** (카드 이전, 기계가 판정): 세션 유형·python·git·디스크·스케줄 도구
  (0-1·0-2). 미충족이면 카드 없이 해결 방법과 함께 멈춘다.
- **T1 필수 사용자 접점** (카드의 두괄): ① 브라우저 로그인 — Claude가 대신 못 하는
  유일한 행동 · ② 무인 권한 동의 — 되돌리기 어려운 유일한 결정.
- **T2 디폴트 대체 가능** (카드의 꼬리, 고지만): ③ 위치·엔진·시각·reviewer.
T1 미완 항목이 남으면 완료 보고는 "설치 완료"가 아니라 "T1 대기"다.

0-1·0-2 판별과 0-3의 **판정 절차 1~2단계**(패널 준비·로그인 판정)를 조용히 먼저 끝낸 뒤,
헬스체크 표와 함께 아래 세 부분으로 구성된 카드 하나를 낸다. 카드는 ①②③ 순서를 지킨다.

**① 지금 해주실 것 — 유일한 사용자 행동 (해당할 때만)**
- 0-3 판정이 **로그인됨**이면: ①은 "브라우저 로그인 확인됨 — 하실 일이 없습니다" 한 줄로
  고지만 한다.
- **로그아웃**이면: 카드를 내밀기 **전에** 0-3의 표시 절차(tabs_create → navigate →
  screenshot 판정)까지 마쳐 브라우저 패널을 실제로 화면에 띄워 놓고, 카드에서 이렇게
  요청한다: "옆에 브라우저 패널을 띄웠습니다 — **claude.ai 로그인 화면이 보이시나요?**
  보이면 로그인해 주세요(설치가 도는 동안 하시면 됩니다). **안 보이면 '안 보여'라고만
  답해 주세요** — 앱 우측 상단 브라우저 아이콘으로 펼 수 있습니다. 공유 설정 자동화에
  필요하며, 로그인은 본인이 직접 해야 하고 Claude는 비밀번호·인증코드를 대신 입력하지
  않습니다." '안 보여' 응답이 오면 tabs_create 부터 재시도하고, 재실패 시 아이콘 안내로
  폴백한다. **스크린샷 성공을 '사용자에게 보인다'로 단정하지 않는다** — 앱 최소화·다른
  모니터·가림은 기계가 못 본다. '보인다'의 최종 확정은 사용자 행동(로그인 진행 또는
  응답)이다.
- 로그인을 기다리지 않는다 — 카드 직후 1절로 진행하며, 로그인은 설치와 **병렬**이다.

**② 동의가 필요한 것 — 되돌리기 어려운 유일한 결정**
- **포괄 승인이 아니다 — 열거 승인이다.** 카드의 이 부분은 아래 세 가지를 **각각
  구체적으로 열거**해 보여주고, 사용자의 승인은 **열거된 것에만** 미친다:
  1) **무인 실행 권한 목록** — 5절의 허용 목록을 카드 안에 통째로 제시.
     `Bash(python:*)` 가 "설치 위치의 파이썬을 매일 아침 묻지 않고 실행"을 뜻한다는 것,
     `rm`·`git push` 는 제외했다는 것을 명시
  2) **정기 삭제의 범위** — 보존 정리는 "설치 폴더 안 `output/audio/` 의 30일 초과
     오디오 파일"뿐이며 그 외 어떤 파일도 삭제하지 않는다는 것
  3) **공개 발행의 범위** — 매일 아침 그날의 브리핑 아티팩트를 사용자 본인 계정으로
     발행하고 "링크가 있는 누구나" 공유를 켠다는 것 (내용은 공개자료 기반 브리핑뿐)
- 카드에 이 문장을 그대로 넣는다: **"이 동의는 위에 열거된 항목에만 적용됩니다 —
  여기 없는 권한 추가·파일 삭제·다른 내용의 공개 발행이 필요해지면 그때 따로
  여쭙습니다."**
- "목록을 만들기 전에 `tools/` 의 파이썬 파일 전부를 훑어보실 수 있습니다 — 원하시면
  지금 말씀해 주세요"를 한 줄로 안내한다 (개수는 `ls tools/*.py` 로 그때그때 센다).

**③ 고지 — 질문이 아니다**
- 0-5의 디폴트 값 네 가지(설치 위치 · TTS 엔진 · 루틴 시각 · reviewer)를 표로 나열하고
  이렇게 닫는다: "**디폴트로 진행합니다 — 바꾸려면 지금 한마디만 해주세요.**"
- 사용자가 링크에 조건을 달아 왔으면(예: "D 드라이브에 깔아줘") 묻지 않고 그 값을 쓰고,
  표에는 조건이 반영된 값을 적는다.
- 위치·엔진·시각·reviewer 는 전부 설치 후에도 한마디로 재설정된다 — 그래서 묻지 않는다.
  되돌리기 어려운 것은 ②의 권한 목록 하나뿐이다.

**카드 이후**: ②의 승인이 오면(또는 사용자가 "진행해" 류로 답하면) 1~5절과 6절 1·2항을
**끝까지 무인**으로 수행한다 — 사용자의 브라우저 로그인과 병렬이다. 로그인 모니터링은
**단계 경계 폴링**으로 한다: 2절·4절이 끝날 때 `get_page_text` 로 ⓐ를 조용히 재확인
(1초, 화면 점유 없음)하고, 확인되는 순간 카드 ①을 완료로 갱신하고 더 보지 않는다.
채근 메시지를 보내지 않는다. 최종 차단 판정은 6절 3항 직전 한 번뿐이다.

체크 결과를 통과/실패 표로 카드와 함께 보여준 뒤 진행한다.

### 0-1. 세션 유형 판별
- **Bash·파일 도구(Read/Write)가 없는 세션**(claude.ai 웹 채팅 등)이면 설치가 불가능하다.
  이렇게 안내하고 멈춘다: "이 킷은 Claude Code에서 설치됩니다. Claude 데스크톱 앱의
  Claude Code 탭이나 터미널 `claude`에서 이 링크를 다시 붙여넣어 주세요.
  Claude Code가 없다면: https://claude.com/claude-code"
- Bash·파일 도구가 있으면 통과.

### 0-2. 실행 환경
- `python --version` — 3.10 이상. 없으면 https://www.python.org/downloads/ 안내 후 중단
- `git --version` — 없으면 zip 다운로드로 대체 가능하다고 표시
- 디스크 여유 1GB 이상 (오디오 산출물 누적 대비)
- 스케줄 도구 확인: scheduled-tasks MCP 또는 세션 크론(CronCreate) 중 무엇이 가능한지
  판별해 표에 적는다. 둘 다 없으면 "루틴 자동 실행 불가 — 수동 실행 모드로 설치"를 고지

### 0-3. 브라우저와 claude.ai 로그인 — **시험 실행(6절)의 차단 선행조건**
이 항이 답하는 질문은 둘이고, 분리해서 판정한다:
ⓐ **로그인돼 있나** — DOM(`get_page_text`)으로 즉답한다. 사용자 개입 불필요.
ⓑ **로그인 화면이 사용자에게 보이나** — 스크린샷으로만 답한다. **ⓐ가 '로그아웃'일 때만
   의미가 있다** — 로그인돼 있으면 ⓑ는 판정하지 않는다.
1~5절은 로그인 없이도 진행할 수 있지만, **6절 3항(아티팩트 발행·공유)은 로그인 확인을
통과해야만 수행한다.**

**판정 절차 — 반드시 이 순서대로 한다 (카드 전에 1~2단계, 로그아웃일 때만 3단계):**
1. `preview_start {url: "https://claude.ai"}` 를 **선행조건으로 먼저 호출한다** — 패널이
   한 번도 열린 적 없는 세션에서는 `tabs_create` 가 실행되지 않는다(2026-08-12 실측,
   이슈 #8). `reused: true` 로 돌아와도 무방하다 — 이 단계의 목적은 패널 세션 생성이다.
2. `get_page_text` 로 **로그인 여부(ⓐ)를 판정한다.** Title 이나 본문에 "Sign in" 이
   없고 채팅 홈이 보이면 **로그인됨 = 통과.** 스크린샷 없음, 사용자 개입 없음,
   여기서 끝 — 카드 ①은 "확인됨" 고지가 된다.
3. **로그아웃일 때만** 화면 표시(ⓑ)가 문제가 된다 — 카드를 내밀기 전에 이 순서로
   패널을 실제로 띄운다: `tabs_create` → `navigate`(https://claude.ai) → `computer` 의
   **screenshot 1회.** ⓑ의 근거는 스크린샷뿐이다 — `read_page`·`get_page_text` 는
   패널이 접혀 있어도 성공하므로 **ⓑ의 판정 도구로는 쓰지 않는다** (ⓐ에는 그것이
   유일하게 맞는 도구다). 스크린샷이 "the Browser pane is not displayed" 류 오류로
   실패하면 그것이 곧 패널 접힘 판정이다 — 카드 ①에 패널 펼치는 방법(앱 우측 상단
   브라우저 아이콘)을 함께 안내한다. 로그인 요청 문안은 0-0 카드 ①에 있다.
4. **재판정(6절 3항 직전, 또는 사용자가 로그인을 마쳤다고 할 때)**: `get_page_text` 로
   ⓐ를 다시 판정한다 — Sign in 이 사라져야 통과. 재판정 없이 통과 처리하지 않는다.
   패널이 표시된 상태라면 스크린샷도 1회 남긴다.

- 재판정까지 미로그인이면: 6절 3항만 **'대기'** 로 표시하고 나머지 설치는 완결한다
  (6절·7절 참조). 이 경우 7절 보고는 성공이 아니라 **설치 미완결**로 쓴다.
- 내장 브라우저가 없으면 claude-in-chrome(크롬 확장) 여부를 확인하고, 둘 다 없으면
  "발행은 되지만 공유 설정은 매일 수동"이라고 카드 ③에 고지한다.
- 로그인이 확인되면 이렇게 고지한다: "로그인은 이번 한 번이면 됩니다 — 내장 브라우저에
  세션이 유지되므로, 앞으로 매일의 공유 켜기·버전 고정·검증은 Claude가 이 안에서
  자동으로 합니다. 사용자의 크롬 브라우저는 건드리지 않습니다."
- 로그아웃 분기의 스모크 재현이 필요하면 `https://claude.ai/logout` 으로 navigate 하면
  세션이 끊긴다(`Title: Sign in - Claude`, 2026-08-12 실측).

### 0-4. TTS 경로 판별 — 사다리: supertonic → gemini → edge
- 기본값 **supertonic**(로컬 ONNX, 키·네트워크 불필요, Windows 네이티브 동작 확인):
  `python -m pip install supertonic soundfile` 가능한지 확인한다. 최초 1회 실행 때
  모델(~99MB)을 HuggingFace에서 자동 다운로드한다고 미리 안내한다.
  기본 보이스는 킷의 `voices/anchor-f1f2-30.json` — 합성 배속은 걸지 않는다(음절 씹힘,
  tts-guard pace 절). 청취 배속은 웹판 플레이어가 기본 1.3×.
- **gemini**(2순위, 감정·톤 지시가 필요할 때)를 원하면: "Google AI Studio
  (https://aistudio.google.com/apikey)에서 본인 키를 발급받아, 설치 후 `.env` 파일의
  `GEMINI_API_KEY=` 뒤에 직접 붙여넣어 주세요. 키는 채팅에 붙여넣지 마세요 —
  Claude는 키를 보거나 출력하지 않습니다." 라고 안내한다.
- **edge**(3순위 예비, 네트워크 필요·키 불필요): `pip show edge-tts`. 위 둘이 모두
  불가한 환경에서만 쓴다.

### 0-5. 디폴트 값 — 묻지 않고 고지한다 (카드 ③의 원천)
아래 네 값은 **질문이 아니라 고지 대상**이다. 전부 설치 후 한마디로 재설정되므로,
카드 ③에 표로 나열하고 "디폴트로 진행합니다"로 닫는다. 사용자가 링크에 조건을 달아
왔으면(예: 설치 위치 지정) 그 값을 디폴트 대신 쓴다.

| 항목 | 디폴트 | 기록 위치 |
|---|---|---|
| 설치 위치 | 사용자 문서 폴더 아래 `briefing-kit` | — |
| TTS 엔진 | `supertonic`(키 불필요; 2순위 gemini, 예비 edge — 0-4) | config.yaml `tts.engine` |
| 루틴 시각 | 생성 07:00 / 배포 07:45 (배포는 생성 +30분 이상 권장) | config.yaml `schedule` |
| reviewer | 기본 비움(검토자 없음) | config.yaml `reviewer` |

시각은 config.yaml `schedule`(daily 크론·publish_at)에 기록하고 하드코딩하지 않는다 —
이후에도 사용자가 요청하면 config 를 고쳐 재설정한다.

## 1. 저장소 받기
```
git clone https://github.com/INTEGRITY2077/cheil-briefing-kit <설치 위치>
```
git이 없으면 zip 다운로드로 대체한다.

## 2. 의존성
```
python -m pip install supertonic soundfile edge-tts requests pyyaml
```
gemini 선택 시 추가 설치는 없다 — `tools/make_audio_gemini.py` 는 표준 라이브러리
(urllib)로 REST 를 직접 호출하므로 google-genai 패키지가 필요 없다. `.env` 의
GEMINI_API_KEY 만 있으면 된다.
(supertonic 은 최초 실행 때 모델 ~99MB 를 자동 다운로드한다 — 0-4에서 이미 고지)

**선택 의존성 — 웹판 오디오 MP4(AAC 96k) 변환용**: `python -m pip install imageio-ffmpeg`
(설치 약 87MB, ffmpeg 바이너리 동봉이라 별도 ffmpeg 설치가 필요 없다.
`tools/embed_radio.py` 가 WAV/MP3 입력을 자동 변환하는 데 쓴다(D2) — 없으면
경고를 내고 WAV 원본 임베드로 강등되어 호가 10MB 를 넘고 첫 렌더가 느려진다.
2026-08-12 실측: 12.26MB WAV → 1.79MB MP4, 길이·표본율 동일)

## 3. 설정 생성
1. `config.example.yaml` → `config.yaml` 복사 후, 선택한 TTS 엔진과 시각을 반영한다.
   reviewer 는 기본 비움(검토자 없음) — 사전 합의 카드의 고지 항목이다.
2. gemini 선택 시: `.env.example` → `.env` 복사를 안내하고 **사용자가 직접** 키를 넣게 한다.
3. 원장(`okf/`)과 수집 커서(`profiles/*.yaml` 의 last_checked)는 **원작자 시드**다 —
   지우지 않는다. 첫 실행이 last_checked 이후의 새 사실을 자동으로 따라잡고,
   낡은 개념은 stale_after·SUPERSEDE 가 정리한다. 사용자에게 "원장은 시드에서
   출발하며 첫 호부터 최신으로 갱신된다"고 한 줄 안내한다.

## 4. 루틴 등록 — 현존 세션 우선
1. **1차: 세션 크론** — CronCreate로 합의된 생성 시각 3분 전(디폴트 06:57)에 `routine-SKILL.md` 실행을
   등록한다. 세션 크론은 7일 자동 만료라는 점을 7절 완료 보고에 고지하고, 만료 전
   재등록을 루틴 보고에 포함시킨다.
2. **예비: 스케줄드 태스크** — scheduled-tasks 도구가 있으면 같은 내용을 생성 시각 +7분(디폴트 07:07)에
   새 세션 태스크로도 등록한다. 이중 생산은 routine-SKILL의 0-a 단일 생산자 규칙이 막는다.
2b. **운영 구조 세 문장** — 중간 메시지로 보내지 않고 7절 완료 보고에 그대로 싣는다
   (카드 이후 무인 원칙):
   ① "지금 이 세션이 루틴의 1차 실행자입니다 — 이 대화를 닫지 않고 두면 매일 같은
   세션에서 브리핑이 만들어집니다 (규칙·브라우저 로그인을 쥔 채로)."
   ② "이 세션이 닫혀 있는 날은 예비 태스크가 새 세션으로 대신 만듭니다 — 그때의
   기억은 대화가 아니라 파일(config·원장·routine-SKILL·CLAUDE.md)로 이어집니다."
   ③ "어느 쪽이든 그 시각에 이 컴퓨터와 Claude 앱이 켜져 있어야 합니다 — 꺼져 있던
   날의 호는 건너뛰고, 다음 실행이 원장 커서로 그 기간을 따라잡습니다." 
3. `routine-SKILL.md`의 「킷 위치」 절에 있는 `{{KIT_ROOT}}` 플레이스홀더를 실제 설치
   경로(절대경로)로 치환한다. **잔존 판정은 「# 킷 위치」 절 범위로만 한다** — 다른 절
   (저장소 동기화 절 등)에는 동기화 규칙을 설명하는 정당한 `{{KIT_ROOT}}` 언급이 있어,
   파일 전체 문자열 검사로 판정하면 정상 설치도 실패로 뜬다(이슈 #10). 판정은 이 명령
   하나로 한다:
   ```
   python tools/sync_skill.py --verify-installed routine-SKILL.md
   ```
   종료코드 0(통과)이어야 이 단계가 끝난다. 레포 원본(치환 전)은 이 명령이 1로 끝나는
   것이 정상이다 — 이 명령은 "설치본인가"를 판정하므로 치환을 마친 뒤에만 돌린다.
   (발행 이력은 `output/artifact-url-*.txt` 파일이 유일한 소스다 — 문서 안에 비우거나
   채울 URL 블록은 없다.)

## 5. 무인 실행 권한
**동의는 0-0 카드 ②에서 이미 받았다** — 이 절에서는 다시 묻지 않고, 승인된 목록을
파일로 만들기만 한다. 카드 ②에 제시할 목록·고지 문구의 원천이 이 절이다.
설치 위치의 `.claude/settings.json`에 아래 허용 목록을 만든다 (이 프로젝트에만 적용):
```json
{
  "permissions": {
    "allow": [
      "Read", "Write", "Edit", "Glob", "Grep", "Artifact",
      "WebFetch", "WebSearch",
      "Bash(python:*)", "Bash(py:*)", "Bash(ls:*)", "Bash(cp:*)",
      "Bash(mv:*)", "Bash(mkdir:*)", "Bash(echo:*)", "Bash(cat:*)", "Bash(cd:*)",
      "Bash(git status:*)", "Bash(git add:*)", "Bash(git commit:*)",
      "Bash(git diff:*)", "Bash(git log:*)",
      "mcp__claude-in-chrome", "mcp__Claude_Browser", "mcp__scheduled-tasks",
      "CronCreate", "CronList",
      "PowerShell(Invoke-WebRequest:*)"
    ]
  }
}
```
`CronCreate`·`CronList` 는 세션 크론 7일 만료 전 재등록(4-1)이 무인으로 돌게 하기 위한
것이고, `PowerShell(Invoke-WebRequest:*)` 는 4b 배포 검증(공유 링크 실측 다운로드)용이다 —
빠지면 무인 아침 실행이 권한 프롬프트에서 정지한다. PowerShell 도구가 없는 환경
(macOS/Linux)이면 이 항목은 무시되고, 검증은 `Bash(python:*)` 로 커버되는
python urllib 로 대신한다.
`rm` 등 셸 파괴 명령은 의도적으로 제외한다. 단, 루틴에 삭제가 아예 없는 것은 아니다 —
루틴 8절의 보존 정리(`retention.audio_days`, 기본 30일 초과 오디오 삭제)가 매달
`Bash(python:*)` 로 수행된다. 이 삭제 범위(오디오 산출물, 30일 초과분)는 카드 ②의
고지 항목에 포함돼 있다 — 여기서 별도 동의를 다시 받지 않는다. 그 외의 파일 삭제는
루틴에 없다.
`git push` 도 제외한다 — 매일 루틴은 로컬 `output/` 에만 쓰고, 저장소 push 는
원작자·포크 전용이다. 포크에 밀어 올릴 일이 생기면 그때 사용자가 직접 승인한다.
카드 ②에 반드시 들어가는 고지: 이 목록의 `Bash(python:*)` 는 **설치 위치의 파이썬
실행을 매일 아침 묻지 않고 승인**한다는 뜻이다. 목록을 만들기 전에 `tools/` 의 파이썬
파일 전부를 (개수는 `ls tools/*.py` 로 그때그때 센다 — 하드코딩하지 않는다) 훑어볼
기회가 있다는 안내도 카드 ②에 포함한다.

## 6. 시험 실행
1. TTS 1회(기본 사다리): `python tools/make_audio_supertonic.py examples/sample-script.md output/audio/smoke-test.wav`
   — 소리 파일이 0바이트가 아니면 통과. supertonic 실패 시
   `python tools/make_audio.py examples/sample-script.md output/audio/smoke-test.mp3`(edge)로
   강등 확인하고 어느 단으로 통과했는지 보고에 남긴다
2. 루틴을 수동 1회 실행해 `output/web/YYYY-MM-DD.html` 생성 확인
3. Artifact 발행 → **발행 직후 Share "Anyone with the link" 켜기** (비공개 기본값 = 404 함정).
   **이 항 직전에 0-3의 재판정(4단계)을 거친다** — 카드 ① 이후 사용자가 병렬로
   로그인을 마쳤을 시간이므로, 확인되면 그대로 발행·공유까지 수행한다.
   재판정까지 미로그인이면 이 항만 **'대기'** 로 표시하고 나머지 시험 실행은 완결한다 —
   로그인 후 첫 발행에서 검증되며, 7절 보고는 설치 미완결로 쓴다.
4. 발행 전 `templates/publish-checklist.md` A~E 전 항목 실행·기록

## 7. 설치 완료 보고
헬스체크 표 · 확정 값 표(설치 위치 · TTS 엔진 · 루틴 시각 · reviewer — 카드 ③의 고지값,
각 항목에 "바꾸려면 한마디" 명시) · 루틴 등록 내역(크론 ID, 태스크 ID) · 시험 실행 결과 ·
운영 구조 요약(4절 2b의 세 문장: 이 세션이 1차 실행자 / 닫히면 예비가 파일 승계로 대행 /
그 시각에 컴퓨터·앱이 켜져 있어야 함) ·
**사용자가 직접 해야 할 남은 일** 칸을 한 화면으로 보고한다.

**'남은 일' 칸은 비어 있어야 정상이다.** 카드에서 모은 접점(로그인·권한 동의·gemini
선택 시 .env 키) 외의 항목이 여기 남아 있다면 그것은 절차가 사용자에게 결함을 떠넘긴
것이다 — 이 경우 보고 제목을 성공이 아니라 **"설치 미완결"** 로 쓰고, 남은 항목마다
어느 절에서 왜 처리되지 못했는지를 명시한다. 숙제 목록을 정상 출력처럼 쓰지 않는다.

**미로그인 설치(6절 3항 '대기')의 보고에는 다음 문장을 반드시 포함한다:**
"배포 경로(아티팩트 발행·공유)는 검증되지 않았습니다 — 로그인 후 첫 발행에서 확인됩니다."

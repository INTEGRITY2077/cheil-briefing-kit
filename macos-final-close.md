# 최종 종결 — macOS 지원 (08-15 추가분 포함)

08-14 「종결 — macOS 지원 확정」 이후 08-15 에 코멘트 5건이 더 붙었다. 그 발견분까지
포함해 이 이슈를 최종 종결한다. 아래는 종결 시점의 확정 상태이며, 남은 항목은
이 이슈에 두지 않고 별도 이슈로 분리한다.

---

## 1. macOS 지원 — 확정

**macOS 고유 결함 0건.** CLI 범위(08-14)와 앱 범위(08-15) 두 차례 실측에서
런타임·게이트·앱 어포던스가 전부 동작했다.

| 층 | 실측 결과 |
|---|---|
| 런타임 | selftest 24/24 · Apple Silicon ONNX(supertonic) 합성 · arm64 휠 전종 · imageio-ffmpeg 동봉 바이너리 MP3 변환 |
| 게이트 | 실전 산출물에 로스터 9종 종료코드 0 (`check_ledger` 3중 장부 정합 포함) |
| 앱 어포던스 | 사이드패널 브라우저 · scheduled-tasks MCP · 세션 크론 · Workflow 병렬 수집 |
| 배포 | 아티팩트 발행 · 브라우저 대행 공유 토글 · 핀 자동 고정 |

실제로 macOS 때문에 고쳐야 했던 것은 **두 개뿐**이었고 둘 다 v2026.08.14.6 에 반영됐다 —
`python` 단독 판별(macOS 에 그 명령이 없다)과 3.9 하한 조정. 코드가 아니라 SETUP 판별
문안이었다.

**README 지원 OS 절 갱신 완료.**

## 2. 귀속 정정 — 「맥 때문」이 아니었던 것들

실측 중 막힌 항목들을 초기에 macOS 탓으로 귀속했으나, 이는 **단일 변수 실험이 아니었던
데서 온 오류**다. 이번에 바뀐 것은 OS 만이 아니라 기계·회선·IP·세션·시점·하네스가
동시에 바뀌었다. 킷이 여태 한 대의 기계에서만 검증됐기 때문에, 처음 다른 기계에
올라가면서 그동안 조용히 전제로 깔려 있던 것들이 한꺼번에 드러난 것이다.

| 증상 | 실제 원인 | OS 종속 |
|---|---|---|
| 실물 호(2.13MB) 발행 실패 | 상행 약 85KB/s, 1MB 초과 업로드 리셋 (curl 로 무관한 호스트에 독립 재현) | 아니오 |
| E6 게이트 실패 | Cloudflare 챌린지 — 아래 3 참조 | 아니오 |
| allowlist 미실측 | 하네스가 `.claude/settings.json` 쓰기를 차단 (팝업이 아니라 도구 호출 거부) | 아니오 |
| 세션 중단 반복 | 워커가 대용량 진단 업로드를 반복해 좁은 업링크를 점유 | 아니오 |

**이 실측이 남긴 가장 큰 값은 macOS 검증 자체가 아니라, 킷이 회선 품질·IP 평판을
암묵적으로 가정하고 있었다는 발견이다.** 그것은 OS 와 무관하게 모든 설치본에 걸린다.

## 3. 분리할 항목 — 별도 이슈로 옮긴다

아래는 macOS 와 무관한 **전역 결함**이므로 이 이슈에 두지 않는다.

**⑴ E6 게이트(`check_publish`)의 거짓 음성** — 공유가 실제로 켜져 있는데도 게이트가
실패로 판정한다. 익명 판정 경로가 Cloudflare 챌린지를 받으면 그것을 「공유 OFF」와
구분하지 못한다. 게이트가 「판정 불가」와 「판정 결과 실패」를 같은 출구로 내보내는
구조적 문제이며, 이슈 #21 이 세운 「간접 휴리스틱 폐기」 원칙의 반대편 구멍이다.

**⑵ `check_run` 의 자정 넘김** — 날짜 판정이 `ended_at or started_at` 이라, 자정을
넘긴 실행은 시작 줄과 종료 줄이 두 날짜로 갈린다. routine-SKILL 0-원장 절의
「같은 started_at 으로 짝을 맞춘다」 규정과 어긋난다. 이슈 #9 가 막으려던 지점이
지연·재시도 상황에서 다시 열린다.

**⑶ `check_size` 의 침묵** — 상한 통과가 발행 성공을 뜻하지 않는데 출력에 그 사실이
없다. 2.13MB 호가 「상한 대비 71% 통과」 초록을 받고 발행에 실패한 것이 실측 사례다.

## 4. 공식 문서 대조 — 킷 규정의 근거 상태

종결에 앞서 Anthropic 공식 문서를 조사해 킷 규정의 근거를 대조했다.

**공식 근거가 있는 것:**

| 항목 | 공식 내용 | 출처 |
|---|---|---|
| 크기 상한 | **16 MiB** (16,777,216 바이트) | code.claude.com/docs/en/artifacts |
| 버전 고정 | 게시마다 버전 생성, 공유 컨트롤에서 뷰어가 볼 버전 선택 | 위 동일 |
| 커넥터 제한 | 커넥터를 호출하는 아티팩트는 어떤 플랜에서도 공개 링크 공유 불가 | 위 동일 |
| 게시 취소 비가역 | 한 번 게시 취소하면 같은 아티팩트를 다시 게시할 수 없다 | support.claude.com/9547008 |

**공식 근거가 존재하지 않는 것 — 킷 규정이 여기 서 있다:**

- **콘텐츠 스캔 / `unscannable`** — 공식 문서에 스캔 절차도 분류 기준도 없다.
  킷의 E5(409 unscannable 처방)와 E6 전체가 미문서화 동작 위에 있다.
- **오디오 코덱** — 공식 문서의 data URI 언급은 **이미지와 폰트뿐**이다.
  MP3 강제·AAC 금지 규정은 전적으로 킷 자체 실측에 근거한다.
- **iframe sandbox 토큰·다운로드 차단** — 「샌드박스된 오리진에서 로드된다」 한 줄이
  전부다. D2b 의 3중 차단 서술은 실측 관찰이다.

킷이 이것들을 실측으로 규명한 것은 정확했다. 다만 **규정 문안에 「공식 미문서화 동작」
표시가 없어서**, 플랫폼이 조용히 바꾸면 규정의 근거가 사라진 줄 모른 채 계속 돌게 된다.
표시를 넣을지는 원작자 판단 사항으로 남긴다.

**부수 확인 — 3MB 상한의 지위.** 공식 상한은 16 MiB 이고, 킷의 `max_html_mb: 3.0` 은
config 주석이 이미 밝힌 대로 「검증된 임계가 아니라 편집 규율」이다. 그 규율의 원래
근거(크기가 409 를 유발한다)는 이슈 #14 에서 오진으로 판명됐고, 지금 살아 있는 근거는
첫 렌더 속도 하나다. MP3 64k(호 4.4MB)는 공식 상한의 26% 에 불과하다 — 넘으면 안 되는
선이 아니라 편집자가 정할 값이라는 것이 문서로 확인됐다.

## 5. 종결 상태

- **macOS 지원**: 확정. 런타임·게이트·앱 어포던스 실측 완료
- **미실측으로 남는 것**: 정상 회선에서의 실물 호 배포 종단(발행→공유→E6), allowlist 실적용
  (하네스 차단으로 이 환경에서는 경로 자체가 없음 — Windows 앱 교차 실측 대상)
- **분리 이슈로 이관**: 위 3 의 전역 결함 3건
- **이 이슈는 여기서 닫는다.**

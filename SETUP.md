# SETUP — Claude가 따라 하는 설치 매니저

> 이 문서는 사람을 위한 설명이자, Claude에게 주는 설치 지시서다.
> 사용자가 이 저장소 링크와 함께 "설치해줘"라고 하면 Claude는 아래를 순서대로 수행한다.
> 각 단계는 실패하면 멈추고 원인과 해결 방법을 보고한다 — 조용히 건너뛰지 않는다.

## 0. 헬스체크와 사전 합의 — 사용자 접점은 카드 한 번뿐이다

이 설치의 사용자 접점은 **0-0의 합의 카드 단 한 번**이다. 카드 이후는 7절 완료 보고까지
전 구간 무인으로 진행한다 — 중간에 질문을 만들지 않는다. 질문이 생기면 그것은 절차의
결함이므로, 디폴트로 진행하고 완료 보고의 '발견 사항'에 남긴다.

### 0-0. 사전 합의 카드 — 설치 시작 시 단 한 번

필수 충족 조건은 티어로 가른다 — 우선순위가 곧 카드의 순서다:
- **T0 차단 조건** (카드 이전, 기계가 판정): 세션 유형·디스크·스케줄 도구 (0-1·0-2).
  미충족이면 카드 없이 해결 방법과 함께 멈춘다. **python·git 은 T0 이 아니다** —
  부재·앨리어스·구버전(3.9 미만)이어도 멈추지 않고 자동 설치 대상으로 표시한다(0-2).
  카드 이전 단계에서는 부재·구버전 감지와 "카드 ②에 자동 설치 항목을 넣는다"까지만
  하고, **실제 설치는 카드 ② 승인 직후(1절 진입 전)에 수행한다** — 승인 전에는
  아무것도 설치하지 않는다.
  자동 설치가 불가능한 환경만 수동 설치 안내와 함께 멈춘다 — 이 **불가 판정
  (플랫폼이 Windows 인가 · winget 이 있는가)은 카드 이전, 0-2 판별에서 끝낸다**
  (0-2 자동 설치 절 참조). 카드 ② 4)항이 실현 불가한 약속을 열거하는 일이 없도록,
  카드에 자동 설치가 오르는 것은 이 판정을 통과한 환경뿐이다.
- **T1 필수 사용자 접점** (카드의 두괄): ① 브라우저 로그인 — Claude가 대신 못 하는
  유일한 행동 · ② 무인 권한 동의 — 되돌리기 어려운 유일한 결정.
- **T2 디폴트 대체 가능**: 설치 위치·루틴 시각은 **카드 안의 선택지 질문**(②와
  같은 AskUserQuestion 한 번에 세 질문 — 접점은 여전히 1회), 엔진·reviewer 는
  ③ 고지만. 위치와 시각은 사람마다 다른 개인값이라 디폴트로 밀지 않는다.
T1 미완 항목이 남으면 완료 보고는 "설치 완료"가 아니라 "T1 대기"다.

0-1·0-2 판별과 0-3의 **조용한 판정**(1단계 — 화면에 UI 를 남기지 않는 경로만)을 먼저
끝낸 뒤, 헬스체크 표와 함께 아래 세 부분으로 구성된 카드 하나를 낸다. 카드는 ①②③
순서를 지킨다. UI 를 남기는 호출(preview_start)은 카드 ① 문안 직후가 유일한 자리다.

**① 지금 해주실 것 — 유일한 사용자 행동 (해당할 때만)**
- 0-3 판정이 **로그인됨**이면(조용한 경로 — 크롬 연결 목록 또는 이미 뜬 패널):
  ①은 "브라우저 로그인 확인됨 — 하실 일이 없습니다" 한 줄로 끝난다. **UI 호출 없음.**
- **로그아웃(또는 판정 불가)이면 — 순서가 생명이다 (이슈 #12):**
  1. 카드 ①에 **먼저 문안을 쓴다**: "claude.ai 로그인이 필요합니다 — **바로 아래에
     브라우저 카드를 띄울 테니, 카드의 「열기」 버튼을 눌러 로그인해 주세요.**
     설치가 도는 동안 하시면 됩니다. 로그인은 본인이 직접 해야 하고 Claude는
     비밀번호·인증코드를 대신 입력하지 않습니다."
  2. **그 문장 직후에** `preview_start {url:"https://claude.ai"}` 를 호출한다 —
     브라우저 카드가 방금 쓴 문안 **바로 아래**에 붙는다. **UI를 남기는 호출은 그것을
     설명하는 문장 직전에 두지 않는다 — 직후에 둔다.** 문안보다 먼저 호출하면 카드가
     설명 없이 떠서 사용자가 그것이 무엇인지 모른다(2026-08-12 실측: 카드와 안내가
     화면 두세 장 떨어져 "카드가 어딨는지 모르겠어").
  3. **카드는 한 장만** — preview_start 한 번으로 끝낸다. tabs_create·navigate 를
     겹쳐 부르면 같은 것이 두 장 떠서 혼란을 더한다. 패널 표시는 사용자의 「열기」
     클릭이 해결한다(스크린샷으로 표시를 선판정하지 않는다 — 스크린샷 성공도
     '보인다'의 보장이 아니고, 실패해도 「열기」가 1차 해결책이다).
- 로그인을 기다리지 않는다 — 카드 직후 1절로 진행하며, 로그인은 설치와 **병렬**이다.

**② 동의가 필요한 것 — 되돌리기 어려운 유일한 결정**
- **포괄 승인이 아니다 — 열거 승인이다.** 카드의 이 부분은 아래 네 가지를 **각각
  구체적으로 열거**해 보여주고, 사용자의 승인은 **열거된 것에만** 미친다:
  1) **무인 실행 권한 목록** — 5절의 허용 목록을 카드 안에 통째로 제시.
     `Bash(python:*)`·`Bash(python3:*)` 가 "설치 위치의 파이썬을 매일 아침 묻지 않고 실행"을 뜻한다는 것(macOS 는 python3 — #33 실측),
     `rm`·`git push` 는 제외했다는 것을 명시
  2) **정기 삭제의 범위** — 보존 정리는 "설치 폴더 안 `output/audio/` 의 30일 초과
     오디오 파일"뿐이며 그 외 어떤 파일도 삭제하지 않는다는 것
  3) **공개 발행의 범위** — 매일 아침 그날의 브리핑 아티팩트를 사용자 본인 계정으로
     발행하고 "링크가 있는 누구나" 공유를 켠다는 것 (내용은 공개자료 기반 브리핑뿐)
  4) **자동 설치의 범위** — Python 이 컴퓨터에 없거나 **3.9 미만 구버전**일 때,
     또는 Git 이 없을 때만 Windows 기본 패키지 도구(winget)로 Python 3.12·Git 을
     자동 설치하며, 그 외 어떤 소프트웨어도 설치하지 않는다는 것. 구버전 Python 이
     있는 경우 **제거하지 않고 3.12 를 병행 설치**하며, 기존 파이썬과 그 위에서
     도는 프로그램은 건드리지 않는다는 것. 이 설치는 winget 의 **패키지·소스 약관
     (제3자 라이선스)을 사용자 대신 자동 수락**(`--accept-source-agreements
     --accept-package-agreements`)한다는 것.
     (자동 설치가 안 되는 환경이면 이 항은 카드에 오르지 않는다 — 그 판정은 카드
     이전 0-2 에서 끝나 있고, 불가면 카드 없이 수동 설치 안내와 함께 멈춘다.)
     둘 다 이미 있고 버전도 충족하면 이 항목은 카드에서 "이미 설치되어 있어 해당
     없음"으로 표시
- 카드에 이 문장을 그대로 넣는다: **"이 동의는 위에 열거된 항목에만 적용됩니다 —
  여기 없는 권한 추가·파일 삭제·프로그램 설치·다른 내용의 공개 발행이 필요해지면
  그때 따로 여쭙습니다."**
- **동의는 강요하지 않는다 — 선택지가 있는 진짜 질문으로 낸다.** AskUserQuestion
  도구(선택지 UI) **한 번에 세 질문**을 담는다:
  **[질문 1 — 설치 위치]** 하드코딩 금지, 사용자가 고른다:
  「문서 폴더 아래 briefing-kit (권장)」 / 「바탕화면」 / 「D:\briefing-kit」 —
  그 외 위치는 Other(직접 입력)로 받는다. 링크에 위치 조건이 있었으면 이 질문은
  생략하고 그 값을 쓴다.
  **[질문 2 — 루틴 시각]** 생성/배포 짝으로 고른다:
  「07:00 생성 / 07:45 배포 (기본)」 / 「06:30 / 07:15 — 이른 출근」 /
  「08:00 / 08:45 — 늦은 아침」 — 그 외 시각은 Other(직접 입력, "생성 HH:MM
  배포 HH:MM")로 받는다. 배포는 생성 +30분 이상을 권장한다고 설명란에 적는다.
  링크에 시각 조건이 있었으면 생략. (설치 후에도 한마디로 재설정된다는 문구 유지)
  **[질문 3 — 무인 권한 동의]** 선택지는 네 개:
  「승인하고 진행」 / 「tools/ 파이썬을 먼저 보여줘 (열람 후 다시 묻기)」 /
  「일부 항목을 수정하고 싶다」 / 「설치하지 않겠다」.
  도구가 없으면 같은 네 갈래를 번호 목록의 명시적 질문 한 문장으로 낸다.
  「일부 항목을 수정하고 싶다」는 5절 허용 목록만이 아니라 **② 열거 항목 전체**에
  적용된다 — 열거 승인의 취지는 항목별 통제이므로, 수락도 항목별로 가능해야 한다.
  특히 "나머지는 승인하되 **Python/Git 은 내가 직접 설치하겠다**"는 이 선택지
  (또는 Other 직접 입력)로 받는다: 그 경우 4)항 자동 설치만 열거에서 빼고,
  0-2 강등 목록의 수동 설치 링크를 안내한 뒤 그 자리에서 멈춘다 — 단
  「설치하지 않겠다」와 달리 **"사용자 설치 후 재개"** 로 기록하고, 사용자가
  설치를 마쳤다고 알리면 0-2 판별부터 재개한다(나머지 항목의 승인은 유효하게
  유지되어 다시 묻지 않는다).
  "답해 주세요"류 재촉 문구·기본값 승인 처리("응답 없으면 진행") **금지** —
  ②의 승인 없이는 5절(권한 파일 생성)과 6절 3항(발행)을 수행하지 않는다.
  「설치하지 않겠다」를 고르면 그 자리에서 멈추고 아무것도 만들지 않는다.
  (열람·수정 선택지는 처리 후 같은 질문을 다시 낸다 — 이것은 접점 1회 원칙의
  예외가 아니라 같은 접점의 연장이다.)

**③ 시작값 안내**
- 질문(①·②)에 없는 나머지 시작값 두 가지(TTS 엔진 · reviewer)는 카드 끝에
  **자연스러운 한 문장**으로만 알린다. 사용자 화면 문안 예:
  "그 밖의 설정은 이렇게 시작합니다 — 음성은 로컬 엔진 Supertonic(키 불필요),
  검토자는 따로 두지 않습니다. 언제든 한마디로 바꿀 수 있습니다."
- **규정 언어를 사용자 화면에 내보내지 않는다** — "고지", "질문이 아닙니다",
  "T0/T1/T2", "티어", 절 번호(0-5 등)는 이 문서의 내부 용어다. 카드에는 사람이
  쓰는 말만 쓴다. (브리핑의 독자 언어 게이트 B1과 같은 원칙 — 내부 작업명 노출 금지)
- 사용자가 링크에 조건을 달아 왔으면(예: "D 드라이브에 깔아줘") 해당 질문을 생략하고
  그 값을 쓴다.

**카드 이후**: ②에서 「승인하고 진행」이 선택되면 1~5절과 6절 1·2항을
**끝까지 무인**으로 수행한다 — 사용자의 브라우저 로그인과 병렬이다. 로그인 모니터링은
**단계 경계 폴링**으로 한다: 2절·4절이 끝날 때 `get_page_text` 로 ⓐ를 조용히 재확인
(1초, 화면 점유 없음)하고, 확인되는 순간 카드 ①을 완료로 갱신하고 더 보지 않는다.
채근 메시지를 보내지 않는다. 최종 차단 판정은 6절 3항 직전 한 번뿐이다.

체크 결과를 통과/실패 표로 카드와 함께 보여준 뒤 진행한다.

### 0-1. 세션 유형 판별
- **Bash·파일 도구(Read/Write)가 없는 세션**(claude.ai 웹 채팅 등)이면 설치가 불가능하다.
  이렇게 안내하고 멈춘다: "이 킷은 Claude Code에서 설치됩니다. Claude 데스크톱 앱의
  Claude Code 탭이나 터미널 `claude`에서 이 링크를 다시 붙여넣어 주세요.
  Claude Code가 없다면: https://claude.com/claude-code"
- Bash·파일 도구가 있으면 통과.

### 0-2. 실행 환경
- **python 판별** — **비 Windows(macOS·Linux)는 `python3` 를 먼저 시도한다**:
  `python3 -c "import sys;print(sys.version)"` → 실패 시에만 `python` 재시도.
  macOS 에는 `python` 명령이 아예 없어(#33 실측) python 단독 판별이 멀쩡한 3.9.6 을
  "부재"로 오판하고 winget 경로로 강등시켰다. Windows 는 종전대로
  `python -c "import sys;print(sys.version)"` 종료코드 0 이고 3.9
  이상이면 통과. **`python --version` 만으로 판정하지 않는다** — Windows 는 파이썬이
  없어도 MS스토어 앨리어스(`…\WindowsApps\python.exe`, 0바이트)가 PATH 에 잡혀
  "Python was not found; run without arguments to install from the Microsoft
  Store..." 를 내며 종료코드 9009 로 끝나거나 스토어 창을 띄운다. `-c` 실행은 실제
  인터프리터만 통과한다. 보조 판별: `where.exe python` 출력에 WindowsApps 경로만
  있으면 앨리어스뿐 = 진짜 파이썬 없음.
  부재·앨리어스·**구버전(종료코드 0 이지만 3.9 미만)** 모두 **중단하지
  않는다** — 아래 「자동 설치」 경로로 간다. 구버전은 부재와 동일하게 자동 설치
  대상이되, 기존 파이썬을 제거하지 않는 **병행 설치**다(카드 ② 4)항에 그렇게
  열거돼 있다). 이 킷의 모든 python 호출은 병행 설치된 3.12 를 쓴다.
- **git 판별** — `git --version` 종료코드 0 이면 통과. 없으면 **중단하지 않는다** —
  아래 「자동 설치」 경로로 간다 (설치 후 clone 우선, zip 은 최후 강등 — 1절).

**자동 설치 (python·git 부재·앨리어스·구버전 시)** — 카드 이전에 두 가지를 끝낸다:
⑴ 부재·구버전 사실과 설치 예정을 표에 기록하고 카드 ② 4)항에 열거할 준비,
⑵ **자동 설치 가능 판정** — Windows 는 `where.exe winget` 성공 여부,
**macOS 는 `brew --version` 성공 여부**를 **카드 이전에** 판별한다 (#33 실측 —
brew 는 winget 과 대등한 무인 설치 경로다. winget 이
`%LOCALAPPDATA%\Microsoft\WindowsApps\winget.exe` 에 있는 것은 정상 — python
앨리어스와 혼동 금지). 불가(Windows 인데 winget 부재 · macOS 인데 brew 부재 ·
그 외 플랫폼)면 **카드를 내지 않고**
아래 3번 강등 안내와 함께 멈춘다 — T0 문안의 "자동 설치가 불가능한 환경만 멈춘다"
의 판정 시점이 바로 여기이며, 카드 ② 4)항은 이 판정을 통과한 환경에서만 열거된다.
(이 판정과 멈춤은 **자동 설치 대상이 있을 때만** 발동한다 — python·git 이 둘 다
충족이면 플랫폼·winget 과 무관하게 이 절 전체를 건너뛴다.)
**실제 설치는 카드 ② 승인 직후, 1절 진입 전에 수행한다.**
1a. **macOS — brew 무인 설치.** (brew 존재는 카드 이전 ⑵에서 확인했다.)
   부재하거나 구버전인 쪽만: `brew install python git` — 이 둘 외 설치 금지.
   설치 후 판별은 `python3 -c …` 로 재실행한다.
1. **Windows — winget 무인 설치.** (winget 존재는 카드 이전 ⑵에서 이미 확인했다.)
   **부재하거나 구버전인 쪽만** 설치한다 — 이 두 프로그램 외에는 어떤 것도 설치하지
   않는다:
   ```
   winget install --id Python.Python.3.12 -e --accept-source-agreements --accept-package-agreements
   winget install --id Git.Git -e --accept-source-agreements --accept-package-agreements
   ```
2. **PATH 반영 주의 — 설치 직후 재판정은 같은 셸에서 그냥 되지 않는다.** winget 설치
   직후의 현재 셸 PATH 에는 새 실행 파일이 반영되지 않는다. PowerShell 에서
   `$env:Path = [Environment]::GetEnvironmentVariable('Path','Machine') + ';' + [Environment]::GetEnvironmentVariable('Path','User')`
   로 PATH 를 갱신한 뒤 위 판별(`python -c …` / `git --version`)을 재실행해 통과를
   확인한다. 파이썬 설치 후에도 WindowsApps 앨리어스가 PATH 에서 앞서면 여전히
   앨리어스가 잡힐 수 있다. **이때 사용자에게 조치를 요구하며 재판정을 반복하지
   않는다** — 별칭 끄기는 사용자만 할 수 있는 GUI 조작이고 카드 이후는 무인
   구간이므로, 사용자 조치 전의 재판정은 반드시 다시 실패한다. 대신 **경로 우회로
   그 자리에서 해소한다**: ⑴ `where.exe python` 출력에서 WindowsApps 가 아닌
   실제 설치 경로(예: `…\Programs\Python\Python312\python.exe`)를 찾아 그
   **절대경로**를 이후 모든 단계(2절 pip · 5절 권한 목록 · 루틴 호출)의 python
   으로 쓰거나, ⑵ `py -3.12` 런처로 호출한다. 둘 중 하나로 `-c` 판별이 통과하면
   이 단계는 **통과**다 — 더 재판정하지 않는다. 「설정 > 앱 > 고급 앱 설정 >
   앱 실행 별칭」에서 python.exe/python3.exe 별칭을 끄면 `python` 명령 자체도
   살아난다는 안내는 7절 완료 보고의 발견 사항에 남긴다(무인 구간의 질문이
   아니라 보고의 참고 항목이다). 실경로도 py 런처도 통과하지 못하면 3번 강등으로
   간다.
3. **강등 — 자동 설치가 불가한 이 경우에만 안내 후 멈춘다.** (winget 부재·비
   Windows 는 **카드 이전 ⑵ 판정**에서, 설치 실패·우회 실패는 카드 승인 후 실행
   중에 이 강등에 도달한다.)
   - winget 부재(`where.exe winget` 실패): Microsoft Store 의 「앱 설치 관리자
     (App Installer)」 설치 또는 https://aka.ms/getwinget 안내.
   - winget 설치 실패: python 은 https://www.python.org/downloads/ ·
     git 은 https://git-scm.com/downloads 링크 안내.
   - **winget 설치는 성공했으나 앨리어스가 계속 가리고, 실경로·py 런처 우회
     (위 2번)로도 인터프리터를 잡지 못함**: python.org 재설치 링크와 함께
     「앱 실행 별칭」 끄기 안내를 남기고 멈춘다 — 재시도를 반복하지 않는다.
   - macOS: `brew install python@3.12 git` (Homebrew 없으면 https://brew.sh 참조).
   - Linux(Debian/Ubuntu): `sudo apt-get update && sudo apt-get install -y python3 python3-venv python3-pip git`.
   수행한 자동 설치(무엇을 설치했는지)와 강등 사실은 7절 완료 보고의 발견 사항에
   남긴다.
- 디스크 여유 1GB 이상 (오디오 산출물 누적 대비)
- 스케줄 도구 확인: scheduled-tasks MCP 또는 세션 크론(CronCreate) 중 무엇이 가능한지
  판별해 표에 적는다. 둘 다 없으면 "루틴 자동 실행 불가 — 수동 실행 모드로 설치"를 고지
- **Claude Code 버전 확인** — `claude --version`(CLI) 또는 데스크톱 앱 버전을 판별해
  표에 기재한다. 문서 요건(2.1.183 이상) 미만이면 **설치는 진행하되** 업데이트를 권하는
  자연문 한 줄을 카드에 넣는다. (2026-08-13 정정: 공개 공유 거절(409 unscannable)의
  원인은 클라이언트 버전이 아니라 **AAC(audio/mp4) 오디오 임베드**로 규명됐다 — 이슈
  #14 종결. 현행 킷은 MP3 로 이식하므로 이 문제와 무관하다. 공유가 거절되면 E5·E6
  절차를 따르고 임베드 MIME 이 audio/mp4 인지부터 본다)
- **킷 버전 확인** — `python tools/check_version.py` 종료코드 0 (로컬 `VERSION` =
  원격). clone 직후라 통과가 정상이며, 이후에는 루틴이 매 실행 같은 판정을 한다
  (routine 0-버전 절 — 구판 정책 위 생산 금지, CHANGELOG.md 참조)
- **다이나믹 워크플로우(Workflow 도구) 유무 확인** — 도구 목록에 있는지 판별해 표에
  **수집 모드**로 적는다: 「병렬(워크플로우)」 또는 「순차(도구 부재)」.
  이 차이는 아침 생성 소요 시간의 편차가 크다(병렬은 채널 7종을 동시에 훑고,
  순차는 하나씩 — 2026-08-12 실측 기준 병렬이 수 배 빠르다). 순차 모드면 카드에
  "이 환경은 순차 수집이라 아침 생성이 더 오래 걸립니다"를 자연문으로 알린다.
  판별만 하고 설치를 막지는 않는다 — 순차로도 전 기능이 동작한다

### 0-3. 브라우저와 claude.ai 로그인 — **시험 실행(6절)의 차단 선행조건**
이 항이 답하는 질문은 둘이고, 분리해서 판정한다:
ⓐ **로그인돼 있나** — DOM(`get_page_text`)으로 즉답한다. 사용자 개입 불필요.
ⓑ **로그인 화면이 사용자에게 보이나** — 스크린샷으로만 답한다. **ⓐ가 '로그아웃'일 때만
   의미가 있다** — 로그인돼 있으면 ⓑ는 판정하지 않는다.
1~5절은 로그인 없이도 진행할 수 있지만, **6절 3항(아티팩트 발행·공유)은 로그인 확인을
통과해야만 수행한다.**

**판정 절차 — 조용한 경로부터, UI 를 남기는 호출은 카드 ① 문안 직후에만 (이슈 #12):**
1. **조용한 판정 먼저 — 화면에 아무것도 남기지 않는 경로 순서대로:**
   ⑴ 이 세션에 이미 열린 내장 브라우저 탭이 있으면 그 탭에서 `get_page_text` (UI 없음)
   ⑵ `mcp__claude-in-chrome__list_connected_browsers` — 사용자 크롬이 연결돼 있고
      탭 제목에 로그인된 claude.ai("새 채팅 - Claude" 류)가 보이면 그것으로 판정
      (2026-08-12 실측: 내장 패널이 끝내 표시되지 않은 설치에서 크롬은 이미 로그인
      상태였다 — 이 경로였으면 접점 자체가 불필요했다).
   어느 쪽이든 **로그인됨이면 ⓐ 통과** — 카드 ①은 "확인됨" 한 줄, UI 호출 0회.
   배포 조작은 로그인된 쪽을 쓴다(내장 우선, 크롬 폴백 — routine 4b 와 동일).
2. 조용한 경로로 판정이 안 되면(둘 다 없음/로그아웃): **카드 ① 문안을 먼저 쓰고,
   그 직후** `preview_start {url:"https://claude.ai"}` **한 번만** 호출한다 — 카드가
   문안 바로 아래 붙는다(0-0 ① 참조). 이어서 `get_page_text` 로 ⓐ를 확정한다.
   tabs_create·navigate·screenshot 을 겹쳐 부르지 않는다 — 카드가 두 장 뜬다.
   (`tabs_create` 는 preview_start 없이 단독 호출하면 실패한다 — 이슈 #8. 필요해지는
   경우는 사용자가 「열기」 후에도 패널 문제가 이어질 때뿐이다.)
3. **재판정(6절 3항 직전, 또는 사용자가 로그인을 마쳤다고 할 때)**: `get_page_text` 로
   ⓐ를 다시 판정한다 — Sign in 이 사라져야 통과. 재판정 없이 통과 처리하지 않는다.

- 재판정까지 미로그인이면: 6절 3항만 **'대기'** 로 표시하고 나머지 설치는 완결한다
  (6절·7절 참조). 이 경우 7절 보고는 성공이 아니라 **설치 미완결**로 쓴다.
- 내장 브라우저가 없으면 claude-in-chrome(크롬 확장) 여부를 확인하고, 둘 다 없으면
  "발행은 되지만 공유 설정은 매일 수동"이라고 카드 ③에 고지한다.
- 로그인이 확인되면 이렇게 고지한다: "로그인은 지금 한 번이면 설치가 끝납니다 —
  앞으로 매일의 공유 켜기·버전 고정·검증은 Claude가 내장 브라우저 안에서 합니다.
  다만 앱을 재시작하면 다시 로그인이 필요할 수 있고, 그때는 아침 보고에 알려드립니다.
  사용자의 크롬 브라우저는 건드리지 않습니다."
  **세션 지속을 단정하지 않는다** (2026-08-15 맥 앱 실측, 이슈 #33: 앱 재시작 후
  내장 브라우저 로그인 세션이 풀려 공유 토글에 도달하지 못했다). 종전 문안은
  "세션이 유지되므로"로 단정해 사용자에게 다시 안 물을 것처럼 약속했다 —
  실제 규정은 4b 첫 동작의 발행 직전 재판정이고, 그 판정이 미로그인을 잡으면
  그날 공유는 수동 강등 + 보고 명기다. 고지도 그 규정과 같은 말을 해야 한다.
- 로그아웃 분기의 스모크 재현이 필요하면 `https://claude.ai/logout` 으로 navigate 하면
  세션이 끊긴다(`Title: Sign in - Claude`, 2026-08-12 실측).

### 0-4. TTS 경로 판별 — 사다리: supertonic → gemini → edge
- 기본값 **supertonic**(로컬 ONNX, 키·네트워크 불필요, Windows 네이티브 동작 확인):
  `python -m pip install supertonic soundfile` 가능한지 확인한다. 최초 1회 실행 때
  모델(실측 385MB — 캐시 ~/.cache/supertonic3)을 HuggingFace에서 자동 다운로드한다고 미리 안내한다.
  기본 보이스는 킷의 `voices/anchor-f1f2-30.json` — 합성 배속은 걸지 않는다(음절 씹힘,
  tts-guard pace 절). 청취 배속은 웹판 플레이어가 기본 1.3×.
- **gemini**(2순위, 감정·톤 지시가 필요할 때)를 원하면: "Google AI Studio
  (https://aistudio.google.com/apikey)에서 본인 키를 발급받아, 설치 후 `.env` 파일의
  `GEMINI_API_KEY=` 뒤에 직접 붙여넣어 주세요. 키는 채팅에 붙여넣지 마세요 —
  Claude는 키를 보거나 출력하지 않습니다." 라고 안내한다.
- **edge**(3순위 예비, 네트워크 필요·키 불필요): `pip show edge-tts`. 위 둘이 모두
  불가한 환경에서만 쓴다.

### 0-5. 디폴트 값 — 묻지 않고 고지한다 (카드 ③의 원천)
설치 위치·루틴 시각은 카드의 **선택지 질문**이다(0-0 질문 1·2 — 개인값이라 디폴트로
밀지 않는다). 아래 표에서 엔진·reviewer 만 고지 대상이며, 전부 설치 후 한마디로
재설정된다. 사용자가 링크에 조건을 달아 왔으면 그 값을 쓴다.

| 항목 | 디폴트 | 기록 위치 |
|---|---|---|
| 설치 위치 | 사용자 문서 폴더 아래 `briefing-kit` | — |
| TTS 엔진 | `supertonic`(키 불필요; 2순위 gemini, 예비 edge — 0-4) | config.yaml `tts.engine` |
| 루틴 시각 | 생성 07:00 / 배포 07:45 (배포는 생성 +30분 이상 권장) | config.yaml `schedule` |
| reviewer | 기본 비움(검토자 없음) | config.yaml `reviewer` |

시각은 config.yaml `schedule`(daily 크론·publish_at)에 기록하고 하드코딩하지 않는다 —
이후에도 사용자가 요청하면 config 를 고쳐 재설정한다.

## 1. 저장소 받기
```
git clone https://github.com/INTEGRITY2077/cheil-briefing-kit <설치 위치>
```
git 이 없으면 사다리 순서로 처리한다:
1. **0-2 의 자동 설치로 git 을 설치한 뒤 clone 한다** — 이것이 기본 경로다.
2. **zip 다운로드는 최후 강등이다** — 자동 설치가 불가한 경우(0-2 강등 조건)에만
   쓴다. **zip 강등은 공짜가 아니다**: zip 설치본에는 `.git` 이 없어
   `tools/check_version.py` 가 구판을 감지해도 `git pull` 자가 치유가 불가능하다 —
   routine-SKILL 0-버전 절의 "구판 정책 위 생산 금지"가 차단에서 경고로 약해지고,
   저장소 동기화 절 전체가 비활성화된다. 강등했으면 그 사실과 이 비용을 7절 보고의
   발견 사항에 남긴다.

## 2. 의존성
```
python -m pip install supertonic soundfile edge-tts requests pyyaml
```
gemini 선택 시 추가 설치는 없다 — `tools/make_audio_gemini.py` 는 표준 라이브러리
(urllib)로 REST 를 직접 호출하므로 google-genai 패키지가 필요 없다. `.env` 의
GEMINI_API_KEY 만 있으면 된다.
(supertonic 은 최초 실행 때 모델 실측 385MB 를 자동 다운로드한다 — 0-4에서 이미 고지.
macOS CLT python3.9 는 LibreSSL 경고가 stderr 에 매 실행 남는다 — 차단 아님, #33 실측.
pip 스크립트 PATH 경고도 무해하다 — 킷은 python -m 경유 호출이다)

**선택 의존성 — 웹판 오디오 MP3(audio/mpeg) 변환용**: `python -m pip install imageio-ffmpeg`
(설치 약 87MB, ffmpeg 바이너리 동봉이라 별도 ffmpeg 설치가 필요 없다.
`tools/embed_radio.py` 가 WAV/MP3 입력을 자동 변환하는 데 쓴다(D2) — 없으면
경고를 내고 WAV 원본 임베드로 강등되어 호가 10MB 를 넘고 첫 렌더가 느려진다.
2026-08-12 실측(당시 표준이던 AAC 기준): 12.26MB WAV → 1.79MB, 길이·표본율 동일 —
현행 변환 대상은 MP3 다, 2026-08-13 개정)

## 3. 설정 생성
1. `config.example.yaml` → `config.yaml` 복사 후, 선택한 TTS 엔진과 시각을 반영한다.
   0-2에서 판별한 워크플로우 유무를 `execution.workflow_available` 에 기록한다 —
   단계별 모드(`execution.stages`)는 기본값이 이미 업무 성격에 맞게 배분돼 있다
   (수집·검증=병렬, 집필·게이트·발행=직렬). 손대지 않아도 된다.
   reviewer 는 기본 비움(검토자 없음) — 사전 합의 카드의 고지 항목이다.
2. gemini 선택 시: `.env.example` → `.env` 복사를 안내하고 **사용자가 직접** 키를 넣게 한다.
3. 원장(`okf/`)과 수집 커서(`profiles/*.yaml` 의 last_checked)는 **원작자 시드**다 —
   지우지 않는다. 첫 실행이 last_checked 이후의 새 사실을 자동으로 따라잡고,
   낡은 개념은 stale_after·SUPERSEDE 가 정리한다. 사용자에게 "원장은 시드에서
   출발하며 첫 호부터 최신으로 갱신된다"고 한 줄 안내한다.

## 4. 루틴 등록 — 현존 세션 우선
1. **1차: 세션 크론** — CronCreate로 합의된 생성 시각 3분 전(디폴트 06:57)에 `routine-SKILL.md` 실행을
   등록한다. 세션 크론은 7일 자동 만료라는 점을 7절 완료 보고에 고지하고, 만료 전
   재등록을 루틴 보고에 포함시킨다.
2. **예비: 스케줄드 태스크** — scheduled-tasks 도구가 있으면 같은 내용을 생성 시각 +7분(디폴트 07:07)에
   새 세션 태스크로도 등록한다. 이중 생산은 routine-SKILL의 0-a 단일 생산자 규칙이 막는다.
2b. **운영 구조 세 문장** — 중간 메시지로 보내지 않고 7절 완료 보고에 그대로 싣는다
   (카드 이후 무인 원칙):
   ① "지금 이 세션이 루틴의 1차 실행자입니다 — 이 대화를 닫지 않고 두면 매일 같은
   세션에서 브리핑이 만들어집니다 (규칙·브라우저 로그인을 쥔 채로)."
   ② "이 세션이 닫혀 있는 날은 예비 태스크가 새 세션으로 대신 만듭니다 — 그때의
   기억은 대화가 아니라 파일(config·원장·routine-SKILL·CLAUDE.md)로 이어집니다."
   ③ "어느 쪽이든 그 시각에 이 컴퓨터와 Claude 앱이 켜져 있어야 합니다 — 꺼져 있던
   날의 호는 건너뛰고, 다음 실행이 원장 커서로 그 기간을 따라잡습니다." 
3. `routine-SKILL.md`의 「킷 위치」 절에 있는 `{{KIT_ROOT}}` 플레이스홀더를 실제 설치
   경로(절대경로)로 치환한다. **잔존 판정은 「# 킷 위치」 절 범위로만 한다** — 다른 절
   (저장소 동기화 절 등)에는 동기화 규칙을 설명하는 정당한 `{{KIT_ROOT}}` 언급이 있어,
   파일 전체 문자열 검사로 판정하면 정상 설치도 실패로 뜬다(이슈 #10). 판정은 이 명령
   하나로 한다:
   ```
   python tools/sync_skill.py --verify-installed routine-SKILL.md
   ```
   종료코드 0(통과)이어야 이 단계가 끝난다. 레포 원본(치환 전)은 이 명령이 1로 끝나는
   것이 정상이다 — 이 명령은 "설치본인가"를 판정하므로 치환을 마친 뒤에만 돌린다.
   (발행 이력은 `output/artifact-url-*.txt` 파일이 유일한 소스다 — 문서 안에 비우거나
   채울 URL 블록은 없다.)

## 5. 무인 실행 권한
**동의는 0-0 카드 ②에서 이미 받았다** — 이 절에서는 다시 묻지 않고, 승인된 목록을
파일로 만들기만 한다. 카드 ②에 제시할 목록·고지 문구의 원천이 이 절이다.
설치 위치의 `.claude/settings.json`에 아래 허용 목록을 만든다 (이 프로젝트에만 적용).
**파일 생성이 도구 층에서 거부되면**(권한 팝업이 아니라 호출 자체 거부 — 2026-08-15
맥 앱 실측, 이슈 #33: 무인 구간에서 이 절이 완주 불가였다) 설치를 실패로 끝내지 말고
**사용자 직접 생성으로 강등한다**: 아래 JSON 전문과 저장 경로를 그대로 보여주고,
"이 파일만 직접 만들어 주시면 나머지는 자동으로 진행합니다"라고 안내한 뒤 7절 보고에
「허용 목록 수동 생성 대기」로 남긴다. 목록 없이도 설치·생산은 되고, 매일 아침
권한 프롬프트가 뜨는 것만 차이다 — 그 사실도 같이 알린다.
```json
{
  "permissions": {
    "allow": [
      "Read", "Write", "Edit", "Glob", "Grep", "Artifact",
      "WebFetch", "WebSearch",
      "Bash(python:*)", "Bash(python3:*)", "Bash(py:*)", "Bash(ls:*)", "Bash(cp:*)",
      "Bash(mv:*)", "Bash(mkdir:*)", "Bash(echo:*)", "Bash(cat:*)", "Bash(cd:*)",
      "Bash(git status:*)", "Bash(git add:*)", "Bash(git commit:*)",
      "Bash(git diff:*)", "Bash(git log:*)",
      "mcp__claude-in-chrome", "mcp__Claude_Browser", "mcp__scheduled-tasks",
      "CronCreate", "CronList",
      "PowerShell(Invoke-WebRequest:*)"
    ]
  }
}
```
`CronCreate`·`CronList` 는 세션 크론 7일 만료 전 재등록(4-1)이 무인으로 돌게 하기 위한
것이고, `PowerShell(Invoke-WebRequest:*)` 는 4b 배포 검증(공유 링크 실측 다운로드)용이다 —
빠지면 무인 아침 실행이 권한 프롬프트에서 정지한다. PowerShell 도구가 없는 환경
(macOS/Linux)이면 이 항목은 무시되고, 검증은 `Bash(python:*)` 로 커버되는
python urllib 로 대신한다.
winget 은 이 목록에 넣지 않는다 — 자동 설치(0-2)는 카드 ② 승인 직후의 **1회 실행**이고
매일 루틴에는 프로그램 설치가 없기 때문이다. 그 1회 실행의 동의 원천은 카드 ② 4)항이며,
실행 시 도구 권한 확인이 뜨더라도 그것은 이미 받은 동의의 기술적 확인일 뿐 새 질문을
만들지 않는다 — 상시 허용 목록에 설치 명령을 남겨 두는 쪽이 오히려 열거 승인 원칙에
어긋난다.
`rm` 등 셸 파괴 명령은 의도적으로 제외한다. 단, 루틴에 삭제가 아예 없는 것은 아니다 —
루틴 8절의 보존 정리(`retention.audio_days`, 기본 30일 초과 오디오 삭제)가 매달
`Bash(python:*)` 로 수행된다. 이 삭제 범위(오디오 산출물, 30일 초과분)는 카드 ②의
고지 항목에 포함돼 있다 — 여기서 별도 동의를 다시 받지 않는다. 그 외의 파일 삭제는
루틴에 없다.
`git push` 도 제외한다 — 매일 루틴은 로컬 `output/` 에만 쓰고, 저장소 push 는
원작자·포크 전용이다. 포크에 밀어 올릴 일이 생기면 그때 사용자가 직접 승인한다.
카드 ②에 반드시 들어가는 고지: 이 목록의 `Bash(python:*)` 는 **설치 위치의 파이썬
실행을 매일 아침 묻지 않고 승인**한다는 뜻이다. 목록을 만들기 전에 `tools/` 의 파이썬
파일 전부를 (개수는 `ls tools/*.py` 로 그때그때 센다 — 하드코딩하지 않는다) 훑어볼
기회가 있다는 안내도 카드 ②에 포함한다.

## 6. 시험 실행
1. TTS 1회(기본 사다리): `python tools/make_audio_supertonic.py examples/sample-script.md output/audio/smoke-test.wav`
   — 소리 파일이 0바이트가 아니면 통과. supertonic 실패 시
   `python tools/make_audio.py examples/sample-script.md output/audio/smoke-test.mp3`(edge)로
   강등 확인하고 어느 단으로 통과했는지 보고에 남긴다
2. 루틴을 수동 1회 실행해 `output/web/YYYY-MM-DD.html` 생성 확인
3. Artifact 발행 → **발행 직후 Share "Anyone with the link" 켜기** (비공개 기본값 = 404 함정).
   **이 항 직전에 0-3의 재판정(4단계)을 거친다** — 카드 ① 이후 사용자가 병렬로
   로그인을 마쳤을 시간이므로, 확인되면 그대로 발행·공유까지 수행한다.
   재판정까지 미로그인이면 이 항만 **'대기'** 로 표시하고 나머지 시험 실행은 완결한다 —
   로그인 후 첫 발행에서 검증되며, 7절 보고는 설치 미완결로 쓴다.
4. 발행 전 `templates/publish-checklist.md` A~E 전 항목 실행·기록

## 7. 설치 완료 보고
헬스체크 표 · 확정 값 표(설치 위치 · TTS 엔진 · 루틴 시각 · reviewer — 카드 ③의 고지값,
각 항목에 "바꾸려면 한마디" 명시) · 루틴 등록 내역(크론 ID, 태스크 ID) · 시험 실행 결과 ·
운영 구조 요약(4절 2b의 세 문장: 이 세션이 1차 실행자 / 닫히면 예비가 파일 승계로 대행 /
그 시각에 컴퓨터·앱이 켜져 있어야 함) ·
**사용자가 직접 해야 할 남은 일** 칸을 한 화면으로 보고한다.

**'남은 일' 칸은 비어 있어야 정상이다.** 카드에서 모은 접점(로그인·권한 동의·gemini
선택 시 .env 키) 외의 항목이 여기 남아 있다면 그것은 절차가 사용자에게 결함을 떠넘긴
것이다 — 이 경우 보고 제목을 성공이 아니라 **"설치 미완결"** 로 쓰고, 남은 항목마다
어느 절에서 왜 처리되지 못했는지를 명시한다. 숙제 목록을 정상 출력처럼 쓰지 않는다.

**미로그인 설치(6절 3항 '대기')의 보고에는 다음 문장을 반드시 포함한다:**
"배포 경로(아티팩트 발행·공유)는 검증되지 않았습니다 — 로그인 후 첫 발행에서 확인됩니다."

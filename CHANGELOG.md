# 킷 버전 이력 — 정책이 바뀐 날만 올린다

버전 체계는 **CalVer `vYYYY.MM.DD[.N]`** 이다 (2026-08-13 창설):

- 정본은 저장소 루트의 `VERSION` 파일 한 줄. git tag 는 같은 문자열로 병기한다.
- **정책·게이트·도구·템플릿이 바뀐 커밋만** 버전을 올린다 — 원장(okf) 추가나
  문서 오타는 버전을 올리지 않는다.
- 같은 날 두 번째 정책 변경은 `.2` 를 붙인다 (예: v2026.08.13.2).
- 워커(설치본)는 루틴 시작마다 `python tools/check_version.py` 로 원격과 대조한다 —
  구판이면 **생산을 시작하기 전에** `git pull` 한다. 산출물은 워커마다 독립이지만
  정책은 전 워커가 같은 버전 위에서 생산해야 한다 (2026-08-13 지시:
  "저쪽의 독립 생산물임. 유착되면 안됨. 다만 레포의 정책은 정렬되어야함").

## v2026.08.16.3

**E9 상행 사전판정 게이트 신설** (`tools/check_uplink.py`) — 맥 워커의 「API 연결 끊김」
규명에서 나왔다. 원인은 회선도 NIC 도 TCP 도 PMTU 도 아니라 **버스트 허용량을 가진
폴리서**였다: 1MB 까지 8.3MB/s 로 올라가다 그 위는 15.0초에 ECONNRESET, 같은 파일을
45KB/s 로 흘리면 22.8초에 완주. 대역 부족이 아니라 셰이퍼보다 빨리 밀어서 끊긴 것이다.

이 실패는 종전에 **발행 순간에만** 드러났고, 그때는 호를 다 만든 뒤라 원인 추적 대신
호 크기를 깎는 오진으로 흘렀다(D8 이 막으려는 바로 그 행동). 판정을 생산 전으로 당긴다.

- `check_uplink.py` — 크기 사다리로 셰이퍼/좁은 상행을 가르고 버스트·지속 상한을 보고.
  3분법(0 완주 / 1 못 올림 / 2 판정 불가 / 64 사용법). 판정은 순수 함수로 떼어
  selftest 가 합성 사다리로 계약을 시험한다 (60 케이스, +7).
- SETUP 0-2 헬스체크에 등재 — 설치 직후, 호를 만들기 전에 그 기기의 발행 가능 여부 확정.
- publish-checklist **E9** 신설(기존 번호 불변), E8 의 3분법 게이트 목록을 E6·E7·E9 로 정정.
- 일일 required-gates 로스터에는 **의도적으로 넣지 않았다** — 로스터는 「그 호」를,
  이 게이트는 「그 기기」를 판정한다. 매 발행마다 외부 계측 종단에 수 MB 를 올리는
  의존을 전 설치본에 지우면 그 종단이 바뀌는 날 모두가 발행 불가가 된다.

## v2026.08.16.2

**#37 게이트에 「모르겠다」의 출구를 낸다.** 실측(맥 회선, #33 파생): CF 봇 차단
챌린지가 /api/frame 에 403 을 주자 E6 가 「공유 OFF」로 오진하고 E5 처방(MP3
재이식·재발행)까지 안내했다 — 공유는 켜져 있었다. 더 위험한 것은 **대조군(무작위
uuid)도 같은 403 을 받아 #21 의 자기검증이 조용히 통과**한 점이다: 판정 경로가
죽었는데 살아 있다고 보고했다.
- **종료코드 3분법** — 0 통과 · 1 판정 결과 실패 · **2 판정 불가**(신설).
  판정 불가를 통과로 접지 않는다(#21 회귀 금지). 종료코드를 가르는 것은 **왜 못
  냈는지를 원장에 보존**하기 위해서다 — run_log 에 `check_publish:2` 로 남고
  check_run 로스터 대조가 「판정 불가로 미완결」 별도 사유로 차단한다.
  check_publish 의 사용법 오류는 2 와 뜻이 충돌해 64(EX_USAGE)로 이관.
- **CF 감지** — 본문 표식·`cf-mitigated` 헤더. **200 응답에서는 본문 표식을 보지
  않는다**(검수 지적): fbody 는 그 호의 발행본 HTML 자체라, 지면이 「just a moment」를
  한 번만 인용해도 정상 호가 판정 불가로 접혀 기계로 영영 닫히지 않는다. 헤더는
  지면이 흉내 낼 수 없으므로 200 에서도 CF 로 본다.
- **대조군 강화** — 대조군이 CF 면 **대상 조회 전에** 판정 불가로 끝낸다.
- **형제 게이트 E7 동반 수리**(검수 지적) — `check_formats --check-links` 도 같은
  /api/frame 을 익명 GET 하면서 CF 403 을 「공유 OFF 의심」 exit 1 로 찍고 있었다.
  E6 가 정직하게 2 를 내는 같은 순간 로스터 게이트인 E7 이 1 을 내면 실행자는
  결국 재발행하러 간다 — #37 이 지목한 그 행동이다. 3분법을 E7 에도 폈다.
- **check_size 출력** — 통과 줄에 「통과가 발행·공유를 보장하지 않는다 · D8 은 편집
  규율이다」. config 주석에만 있던 문장을 출력으로 올렸다(실행자는 「통과」만 본다).
- **호 마감 정의** — 「URL 기록」 → **「URL 기록 + check_publish 0」**. 2 면 마감이
  아니다. CF 회선을 위해 「수동 확인 마감」 경로를 두되(소유자 직접 확인 + 사유 기록),
  그 날의 check_run 은 계속 1 이며 그것이 정상이라고 명시했다 — 기계 판정과 문안이
  어긋난 채 남지 않게(검수 지적).
- **한계 명시** — (a) 3분법은 E6·E7 두 게이트에만 (b) CF 판정은 문자열 휴리스틱이라
  문구가 바뀌면 다시 오진 (c) 이 수리는 오진을 막을 뿐 CF 회선에서 공유 상태를
  판정해 주지 않는다.

검증: selftest **53케이스**(40→53) 불일치 0 — CF 스텁은 urlopen 층에 HTTPError 를
세워 실제 수리 지점(챌린지 본문·헤더 회수 갈래)이 돌게 했다(검수 지적). 기존 산출물
게이트 10종 회귀 0, 정상 회선 오탐 0(08-13 호 exit 0 재확인).
부수 — 이슈 5번(check_run 자정 넘김)은 v2026.08.15 에서 이미 수리됨을 확인만 했다.

## v2026.08.16

**#35 증류 루프 — 원장이 확증만 쌓는 구조를 끊는다.** 실측 배경: 원장 67건의 방향
태그가 지지 9·조건형성 4·중립 2·**반증 0**, 세 축 점수판도 전부 반증 0 이었다.
반증 재료(챗GPT CPM $60→$25 하락·OpenAI 목표 대비 10% 페이스·빅6 점유율 하락)가
이미 지면에 실려 있는데도 반증으로 계상되지 않았다 — 축이 옳아서가 아니라 아무도
반증을 찾지 않았기 때문이다. #28 의 「축이 수집을 끈다」 배선이 확증 쪽으로만 돌았다.
- **반증 전담 렌즈** — 수집 스윕에 refuter 채널 상설. 지시가 다른 채널과 반대다:
  「축의 전제를 무너뜨릴 관측만 찾는다 — 지지 사실은 이 채널의 산출이 아니다」.
  탐색 지침은 profiles macro_axes[].refute_probes 가 정본(축별 5종). **찾지 못한
  날은 그 사실 자체를 기록**한다(axes.md 「반증 탐색 이력」) — 「안 찾은 날」과
  「찾았는데 없는 날」을 가르지 않으면 반증 0 이 관측인지 미탐색인지 아무도 모른다.
- **증류 명제 슬롯** — axes.md 축마다 「현재 판정(증류)」 한 문장 + 근거 좌표.
  지면의 추적 줄이 이것을 읽는다. 내용은 사람/패널 몫(황금률).
- **reinduct_when 재귀납 트리거** — 「반증 >= 1 / 무소속 >= 3 / 창마감」. 충족 시
  다렌즈 재독 → 축 유지·분할·합병·신설 판정 → 편집장 승인 → 기록(SKILL 2b 절 신설).
  종전 「소급 재조사는 끝났고 이후는 증분」 하드코딩을 이 절이 대체한다 — 그 문장은
  축이 틀렸을 가능성을 절차에서 지웠다.
- **기계 봉인** — check_ledger ⑥: 증류 좌표 실존 · **반증 조건 충족인데 재귀납
  기록이 없으면 차단** · 반증 탐색 이력 부재는 경고. 형식만 본다.

**#36 소스 로스터 봉인 — 좁게 돈 날을 기계가 잡는다.** #24 의 required-gates 대조와
동형: 산출물이 있는 날 상시 소스 전수의 last_checked 가 당일인가.
- profiles sources 에 **cadence 신설**(매일 9 · 주간 1 · 월간 1 · 연간 1) — 실측
  last_checked 분포로 부여했다. 연간 통계를 매일로 넣으면 상시 실패가 된다.
- 판정은 check_run(날짜·산출물·run_log 개념이 이미 그 자리에 있다), **발행 시점
  강제는 check_publish ④** — check_run 을 required-gates 에 넣으면 순환이라
  (로스터가 정의하는 gates 필드를 check_run 이 판정한다) 이미 로스터 안에 있고
  run_log 를 읽지 않는 E6 에서 같은 판정 함수를 호출한다.
- 강등: quiet_day·검증만 면제 · 사유를 적은 건너뜀은 경고 · **전수 건너뜀은 차단**
  (전부에 사유를 적는 것은 정직한 부분 실패가 아니라 스윕 자체의 부재다).
- 한계 명시(코드·체크리스트·SKILL 동일 문안): last_checked 는 자기 보고라 이 봉인이
  잡는 것은 「갱신조차 안 한 채 좁게 돈 날」이지 「형식적으로 갱신만 한 날」이 아니다.

검증: selftest **40케이스** 불일치 0(26→40), 기존 산출물(08-14) 게이트 9종 회귀 0,
check_ledger 67건 3중 장부 정합, 마스터↔레포 SKILL 왕복 안정.

## v2026.08.15.2

- **맥 앱 실측의 규정 반영 2건** (#33 — 환경 무관 결함만 반영):
  ① SETUP 0-3 **로그인 세션 지속 단정 철회** — 앱 재시작 후 내장 브라우저 세션이
  풀린 실측. 종전 고지 「세션이 유지되므로」는 사용자에게 다시 안 물을 것처럼
  약속했으나 실제 규정은 4b 첫 동작의 발행 직전 재판정이다 — 고지를 규정에 맞춘다.
  ② SETUP §5 **allowlist 생성 차단 시 강등 경로** — 권한 팝업이 아니라 도구 호출
  자체가 거부되면(맥 앱 실측) 설치를 실패로 끝내지 말고 JSON 전문·경로를 보여주는
  사용자 직접 생성으로 강등하고 7절에 「수동 생성 대기」로 남긴다.
- **D8 문안 — 환경 상한과 킷 상한을 층으로 가른다**: 맥 앱은 1.00MB 통과/1.25MB
  이상 ECONNRESET, 같은 시기 개발 로컬은 2.61MB 재발행 3회·7.84MB 1회 발행.
  그 임계는 API 상한이 아니라 회선의 연결 지속시간 한계다 — 발행이 끊기면 D8
  상한을 내리지 말고 **그 환경의 상한을 재서 보고**한다. (맥 워커의 「게이트가
  발행 불가능한 호를 통과시킨다」 결론은 이 반증으로 정정 — #33 코멘트)
- **적대 검증 앵커 3종** (게이트 9종 전초록·자체 심사 통과 지면에서 잡힌 것):
  R5 floor 에 **범주 바꿔치기**(좌표의 숫자는 맞는데 그 숫자의 **이름**을 바꿔 부름
  — 「특수관계자 거래 합계」→「계열 매출」)와 **미열람 문서 내용 단정**(접수 사실만
  확인하고 내용을 단정 — 캡션의 유보가 화두의 단정을 사면하지 않는다),
  R1 에 **출처 이해관계 계량**(파는 쪽 조사·자기 주장·계열 주최·트레이드 매체를
  중립 관측과 같은 무게로 두면 4 이하). review-prompts refuter 필수 점검 3종으로 배선.

## v2026.08.15

- **도구층 결함 2건 수리 — 맥 앱 실측 근거** (#33 코멘트 `5300218207` 이 근거물.
  둘 다 macOS 무관, 모든 플랫폼의 모든 설치본에 걸린다):
  ① **selftest 픽스처가 시드 수치를 하드코딩해 실사용 시작과 동시에 죽었다** —
  `okf/axes.md` 의 「`**점수판** 지지 12`」를 문자열로 박아 두어, 설치본이 루틴을
  한 번이라도 돌려 점수판이 갱신되면(실측: 관측 10건 등재로 지지 12→14)
  `RuntimeError` 로 **15케이스에서 중단**됐다. 원장은 되돌아가지 않으므로 자가
  치유도 없다 — 도구층 검증이 가장 필요한 시점(운영 중 회귀 검출)에 정확히
  무력했고, 종전 「24/24 통과」는 그때가 루틴 실행 **전**이었던 부작용이다.
  `bump_score()` 신설로 **현재값 기준 증분**으로 바꾼다.
  ② **`check_run` 이 자정 넘긴 실행의 짝을 두 날짜로 갈랐다** — `load_entries` 가
  `ended_at` 을 우선해, 21:36 시작→다음날 09:35 종료한 실행에서 시작 줄은 08-14,
  종료 줄은 08-15 로 갈렸다. 그 결과 시작일 판정이 **게이트 10종을 전부 돌리고
  기록까지 한 실행을 「게이트 기록 없는 발행」(#18 문면)으로 오진**했다 — #18 이
  막으려던 것과 정반대 방향이다. routine-SKILL 0-원장 절의 "같은 `started_at` 으로
  짝을 맞추고" 규약에 맞춰 **`started_at` 우선**으로 정정 (`ended_at` 은 시작 줄이
  없는 기록에서만). selftest 26케이스 (자정 넘김 시작일·종료일 2건 추가).

## v2026.08.14.3

- **타이틀 게이트 신설 — 생성 규칙 + 간명 상한** (2026-08-14 편집장 반려 실측:
  PoC 08-14호 헤드 「전환의 값은 세계가 먼저 매겼다 — …반기보고서부터다」가 심사
  패널 R6 8.0 을 받고도 반려됐다 — 실존·담보 판정만으로는 요약문과 카피를 못 가른다):
  - 계약금 생성 규칙 명문화(pipeline ①·SKILL 3b①) — 후보는 주요 사실 문장(원장
    문면)을 줄인 것에서만 출발, 분석 요약·개념 배열·은유 제조 금지, 실물 명사 우선
  - headline-guard `brevity` 신설 — h1 공백 제외 30자·대시 2절 상한 (낙점 계보
    26자·20자 통과 / 반려 헤드 34자 실패로 캘리브레이션), check_insight ⑤b 가 판정
  - R6 앵커 보강 — 「실존해도 전부 개념어면 요약문」(anchor_4 이하), 계보 5단
    등재(개념어 배열 기각 — 패널 8.0 vs 편집장 반려의 편차 실측)
  - selftest 21케이스 (간명 상한 위반 추가)

## v2026.08.14.2

- **tools/selftest.py 신설 — 게이트의 게이트**: 메커니즘 감사의 회귀 케이스
  20건(정상 통과 7 · 우회/위반 차단 13)을 정본화한 자가 시험. 임시 킷 사본에
  합성 입력(심사 기록·지면·뼈대·run_log·마스터 SKILL)을 만들어 각 게이트의
  통과/차단 계약을 exit code 로 실측한다 — 게이트·정본 템플릿을 고친 커밋은
  이 시험을 먼저 돌린다. 발행 게이트 아님(required-gates 로스터 밖, 개발용).
  다른 OS 설치 실측(macOS 등)에서 도구층 검증을 명령 하나로 만드는 것이 부수
  목적 — 리눅스 실측 확정, 표준 라이브러리+pyyaml 만 사용.

## v2026.08.14

메커니즘 감사(2026-08-14, 이슈 #22~#31)의 일괄 수리 — 「질(심사)과 양(축적)을 위한
개발이 어디까지 기계인가」를 실행 실측으로 판정한 감사에서 나온 결함들이다.
게이트 강화는 전부 형식·봉인이지 내용 판정이 아니다 (황금률 유지).

- **#22 check_review 임계 폴백 수리**: rubric 의 중첩 threshold 매핑을 파서가 못
  읽어 config 부재 환경이 전 케이스 exit 1 이던 고장 — config.example 의 폴백
  약속이 실동작하게.
- **#23 IG4 배선 진술 통일**: 「배선은 임계 확정과 함께」(checklist) vs 이미 배선
  (5절)의 모순을 코드 실동작대로 이원 배선으로 명문화 — 봉인 위반은 차단, 임계
  미달은 비차단(재심사 기록 후 발행+명기). 임계 확정·차단 승격은 여전히 편집장 결정.
- **#24 check_run 로스터 대조**: gates 목록 「비어 있지 않음」 판정이 한 줄 자기
  보고로 뚫리던 것 — publish-checklist 의 required-gates 블록(신설)을 정본으로
  전 게이트 `이름:0` 전수 대조.
- **#25 check_insight ⑧ 발행본 좌표 실존**: 지어낸 (F-999) 병기가 Q2 를 통과하던
  구멍 — 발행본 F-### 전수를 okf/facts/ 실존과 대조 (deprecated 참조는 경고).
- **#26 뼈대 판형 정본 배포**: templates/proto-skeleton.md 신설 — 규정이 gitignore
  된 eval/proto-2026-08-13.md 를 정본으로 가리키며 공전하던 것을 배포본 안으로.
  review-form 심사 대상을 웹판→뼈대 MD 로 정정(정본 선언과의 상충 해소),
  check_review 가 심사 대상 행을 날짜와 대조 (--retro 만 발행본).
- **#27 큰축 축적 봉인**: check_ledger ④ 유효 사실의 계보/무소속 등재 완결 ·
  ⑤ 점수판 3중 장부(axes·profiles·index) 재계산 대조, check_theme ⑤ data-macro
  실존 lint. 축 소속의 정본은 계보 표로 확정 (axes.md 머리).
- **#28 세계 층 상시화**: profiles 에 global-demand(광고주 수요측)·ai-ad-supply
  (플랫폼·조사) 상시 수집원 신설, 스윕 채널 편입, 축→수집 역방향 배선(스윕 전
  axes.md 구멍·검증 지점을 과제로 읽는다), eval-rubric 세계 층 T1/T2 매핑 명문화,
  전시물 화이트리스트에 세계 층 공식 도메인 편입(업계지·조사기관은 여전히 금지).
- **#29 3렌즈 독립 채점 이식**: templates/review-prompts.md(심사자 프롬프트·독립
  실행 규정 — 병렬, 순차라도 앞 표 비공개) 신설, config execution.stages 에
  review_panel 등재, check_review 형식 봉인 확장(점수 0~10·사유 빈 칸·렌즈
  정체성·지적 표 실존 — 단일 심사자 3표·99점 세탁 우회 실측 차단).
- **#30 편집 3단 이행**: 4절에 인계 문장(입력은 3b 통과 뼈대), fail_path 언어
  통일(중도금 뼈대 수정은 무제한, 1회 한정은 잔금 이후 소급만), check_insight ⑨
  뼈대 배신 대조(화두·함의 문장 보존을 정규화 문자열로 판정).
- **#31 경미 표류**: sync_skill 유닉스 절대경로 누출 패턴 + --check(무기록 검사),
  check_insight 자기 소개 「4개」→아홉 갱신, Q1 「절반 이상」→「절반 초과」 문구 통일.

## v2026.08.14.6

- **macOS 지원 실측 반영** (#33 — 맥미니 CLI 실측: selftest 24/24·애플실리콘
  TTS·MP3 이식·게이트 전부 python 3.9.6 통과. 막힌 건 킷 문안 자체):
  ① SETUP python 판별 — 비 Windows 는 `python3` 우선 (macOS 에 python 명령 부재)
  ② 하한 3.10→3.9 (실측 근거 — 3.9.6 전 항목 통과)
  ③ brew 를 winget 과 대등한 무인 설치 경로로 승격 (macOS 자동 설치)
  ④ allowlist `Bash(python3:*)` 병기 — 없으면 macOS 무인 아침 실행 정지
  ⑤ SKILL 인터프리터 자리표시 규정 (macOS·Linux = python3)
  ⑥ 모델 크기 고지 정정 ~99MB→실측 385MB(~/.cache/supertonic3) + LibreSSL·
  pip PATH 경고 무해 고지. 실측 보고 전문은 #33 코멘트.

## v2026.08.14.5

- **콜드 리더(배경 0 독자) 렌즈 상설** (편집장 반려 실측 — "독자가 척척박사라
  가정하는 문장 생산"): 필자도 3렌즈도 원장을 읽고 들어와 생략이 생략으로 안
  보이는 전원 배경 오염을, 지면 텍스트만 받는 4번째 렌즈가 깬다. 게이트 전종
  초록 지면에서 막힘 25곳 실측(AAM 미정의·내부 체계 노출·지시어 미복원) →
  수리 후 재독 19곳 해소·신규 0·"추측 없이 골격 그대로 읽힘". review-prompts
  cold 절·rubric panel(scored_dims: R6·R5 부분 채점)·pipeline 콜드 리딩 단계·
  SKILL 3b② 배선. check_review 가 렌즈별 채점 스코프를 rubric 에서 읽도록 확장
  (심사자 수 정본 = panel 크기). 생략의 규칙 명문화: 생략·지시어는 그 지점까지
  읽은 텍스트 안에서 복원될 때만 허용 — 제목은 보편 상식으로만.
- **B2 확장**: 제작 체계 언어(런·스윕·수집·등재·큰축·MA#·점수판 기호)는 취재
  과정 언어와 같은 항 — 추적 줄도 사람 문장으로. 약어는 첫 등장에 풀어쓴다.
- selftest 24케이스 (cold 표 누락 차단 추가).

## v2026.08.14.4

- **편집장 반려 3건의 규정화** (08.14호 V3 소급 반영 완료): ① R6 계보 6단 —
  압축이 목적어·연어 경계를 넘으면 실존 어휘라도 기각, 음독 오독 즉시 기각
  (「광고를 잰다」→「광고 성과를 측정한다」) ② 화두 세 칸 자립성(I8b) —
  각 칸은 배경지식 없이 단독 완결, 근거 칸은 사람 문장+좌표 괄호 병기
  (check_insight ⑩ 정적 판정 신설, selftest 2케이스 추가 — 23케이스) ③ 중도금
  음독 테스트 — 헤드·칸·절 제목은 귀로 먼저 검수 (「곧 갈리는 것들」→「곧
  드러나는 것들」). selftest subprocess 디코딩 utf-8 고정(cp949 크래시 수리).

## v2026.08.13.5

- **큰축(macro axes) 창설 + 소급 재조사 완료**: 원장 51건 전수 재독(자본·구조
  2렌즈 독립 → 통합)으로 추적 질문 3축을 귀납 확정 — MA1 계열 물량·권한의
  재배분 / MA2 광고회사 자본의 사람→AI·데이터 이동 / MA3 광고주 예산의 AI
  지면 이동과 대행사의 자리. profiles macro_axes 정본 + okf/axes.md 관측
  점수판(방향 태그: 지지/반증/조건형성/중립). 작은축은 소속 큰축을 선언한다
  (스켈레톤 data-macro·IG5) — 무소속 = 필요성 재평가. 이후 태깅은 수집 시 증분.
- **신규 원장 7건** (전부 원문 확인): F-043·044 삼성전자 광고선전비(연 6조 34억,
  1Q −19.1% — DART 원문), F-045 퍼블리시스=삼성 글로벌 미디어 AOR(시장별 목록은
  비공개라는 사실 포함), F-046 호주 $120m은 2020년부터 클레멘저 경유 — 제일기획
  경유 미디어 물량 공개보도 기준 0, F-047 제일 인도 ₹700crore 통합 미디어 수임
  (호주와 정반대 사례), F-048 본사 인원 완만 증가, F-049 AI 명목 투자 공시 부재 확인.
- **편집 3단 확정** (templates/pipeline-three-stage.md·SKILL 3b): 계약금(화두
  가설 심사 — 큰축 대조 우선)·중도금(뼈대 MD 조판+정성 심사 3렌즈)·잔금(지면
  조판 1회, 정량만). 근거: 뼈대 루프 분 단위 vs HTML 재조판 시간 단위 실측.
- **루브릭 v2**: 독자 재정의("의사결정을 이루는 팀" — R1·R2 앵커를 팀 동사로),
  R3 큰축 장부 대조, R6 카피 인테그리티 신설(헤드 명사는 본문·원장 실존 명사만,
  발명 은유·내부어 기각 — 2026-08-13 카피 계보 4단 앵커). insight-guard
  audience_filter 동기화, IG4 문안 갱신, IG5 신설.

## v2026.08.13.4

- **정량·정성 이원 게이트 창설 (시범 운용·비차단)**: 황금률 — 정량(check_*)은
  형식을, 정성(심사 패널)은 「말할 가치」를 판정하고, 정량은 사람이 다시 보지
  않고 정성은 기계가 대신 답하지 않는다. `templates/insight-rubric.yaml`
  (5차원 × 10점: 이해관계 계량·함의 실행가능성·축 교차·새로움·근거-주장 정합),
  `templates/review-form.md`, `tools/check_review.py`(IG4 봉인 — 기록 실존·임계·
  지적 처리 완결만 판정, --retro 소급 예외), checklist IG4 신설.
- `check_insight` 정적 확장: Q1 유보 종결 상한(함의 절반 초과 금지) ·
  Q2 무담보 긴박감(시한·의무 주장 문단에 원장 좌표 의무).
- 시범 심사 실측: 08.13호 3렌즈 독립 채점 평균 6.40/10 (임계 시작값 7.0 미달) —
  정량 12종 전초록과의 대비가 두 축의 존재 증명. 임계 확정·루틴 배선은
  편집장 결정 대기 (그때까지 비차단).

## v2026.08.13.3

- **게이트가 발행본을 직접 본다** (#21, 노트북 워커 발제): `check_publish` 를
  404 휴리스틱에서 `api/frame` 200+public 직접 판정으로 교체(무작위 uuid 대조군
  자기검증 포함 — 구판은 없는 uuid·오타 URL 도 통과시켰다), 발행본 본문에 오늘 호
  h1 대조 추가. `check_formats --check-links` 에 ③ 발행본 대조 신설 — 로컬 링크가
  핀 버전 본문에 실려 있지 않으면(「V4 발행, 핀은 V3」) 실패. 표준라이브러리만 사용.

## v2026.08.13.2

- **SETUP 자동 의존성 설치 승격**: python·git 부재는 더 이상 "안내 후 중단"(T0
  차단)이 아니다 — Windows 는 winget 무인 설치(`Python.Python.3.12`·`Git.Git`,
  확정형 플래그) 후 재판정한다. 설치 시점은 카드 ② 승인 직후(승인 전 설치 금지),
  카드 ② 열거 승인에 4)항 "자동 설치의 범위" 추가. winget 부재·설치 실패·
  macOS/Linux 만 링크·brew/apt 안내로 강등하고 그때만 멈춘다.
- **MS스토어 파이썬 앨리어스 함정 판별**: `python --version` 은 앨리어스에서도
  잡힌다(종료코드 9009·스토어 창) — 판별을 `python -c "import sys;..."` 종료코드
  기준으로 교체, `where.exe python` 의 WindowsApps 경로 필터를 보조 판별로 명시.
  winget 설치 직후 현재 셸 PATH 미반영 주의점 병기.
- **zip 강등 사유 명문화** (SETUP 1절): git 부재 시 "winget 설치 후 clone > zip
  최후 강등" 사다리로 교체. zip 설치본은 `.git` 부재로 check_version 구판 감지 시
  `git pull` 자가 치유가 불가 — "구판 정책 위 생산 금지"가 차단에서 경고로 약해지고
  저장소 동기화 절이 비활성화된다는 비용을 명시.
- **AAC 잔존 문구 정정** (v2026.08.13 MP3 개정의 후속): README 산출물 절·SETUP §2
  선택 의존성·publish-checklist D8·check_size 공식 주석을 MP3 기준으로 정렬.
  `embed_radio.py` 의 .mp4/.m4a 입력은 직접 임베드(audio/mp4) 경로를 제거하고
  MP3 변환 강제(변환 불가 시 중단)로 개정 — D2 금지와의 자기모순 해소.

## v2026.08.13

- **공개 공유 409 unscannable 종결** (#14): 원인은 AAC(`data:audio/mp4`) data URI —
  스캐너가 못 다뤄 버전에 박제. `embed_radio.py` 변환 코덱을 MP3(`audio/mpeg`)로
  개정, `.mp4/.m4a` 직접 이식 금지. 처방은 재공유가 아니라 MP3 재이식 후 재발행.
- **E6 공유 확정 게이트** `tools/check_publish.py` 신설 — 발행·공유 후 익명 조회로
  비공개(403)를 잡는다. "항상 전체공유"가 정책이고, 발행의 완결 조건은 익명 개통이다.
- **IG3 연간 주제 정렬 게이트** `tools/check_theme.py` 신설 — 마지막 전체검수.
  화두·단신·축절·전시물 단위별 정렬/보조/무관 판정(`eval/theme-날짜.md` 정본),
  화두=정렬 필수·무관 0건. 검수 메타 언어(규명·표본·주장 강도·보류 슬롯)는 지면 금지.
- **판형 바 개정** (`check_formats`): 전환할 판형이 하나뿐인 호는 fmtbar 를 넣지
  않는다 — 세그먼트 1개짜리 바는 죽은 버튼이라 실패.
- `tts.embed.bitrate` 기본 48k (64k 는 긴 대본 날 호 상한 3MB 초과 실측).

## v2026.08.12 이전 (소급 기재)

- TTS 사다리 확정: supertonic(1순위, 로컬)–gemini–edge. 1인 대본 표준, 합성 1.05 고정.
- 전시물 계약(exhibit-contract)·축 절·화두 세 칸·실행 원장(run_log, gates 필드 의무)·
  원장 정책 A안(JSONL+okf 미러 한 동작)·기계 게이트 7종 체계 수립.
- 발행 규정: 호마다 새 아티팩트(새 URL), 발행 즉시 공유 ON, 공유 버전은 번호 핀.

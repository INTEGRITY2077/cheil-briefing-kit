# 발행 전 체크리스트 — 전 항목 통과 없이는 배포하지 않는다

매 발행 직전에 산출물별로 돌리고, 결과를 `eval/YYYY-MM-DD.md`에 항목 번호별
통과/실패로 기록한다. 실패 항목은 고친 뒤 재검사한다. 기록이 없으면 그 발행은 실패다.
전부 2026-08-11~12 이틀간 실제 반려에서 나온 항목이다.

## A. 주어와 선별
- A1. 헤드라인의 주어가 제일기획(또는 계열 물량)인가? 경쟁사가 헤드에 있으면 실패
- A2. 경쟁사 항목이 2건 이하이고, 층(글로벌/침입자/국내)과 축(내수/해외)이 명시됐는가
- A3. 어제 실린 사실의 재편찬이 없는가? 오늘 뉴스가 없으면 quiet_day로 썼는가
- A4. 슬라이드 표지 헤드라인 = 당일 웹판 헤드라인인가? 덱에 전일 승계 슬라이드가 없는가
- A5. 경쟁사 단독 절 0건 — 경쟁사는 경쟁 지형 절(층×축 한 표) 안의 행으로만 다룬다.
  한 경쟁사의 해부·특집 절이 있으면 실패. 제일기획 심층 절이 경쟁사 절보다 앞·크게 배치됐는가
- A6. **화두·헤드라인이 회사에 일어난 일인가** — 수집·계량 과정이 헤드에 올라오면 실패
  (insight-guard I12). 자료의 속성(등장 횟수·게시 여부·목록 건수·「신규 0건」)은 근거이지
  화두가 아니다. 2026-08-12 반려 실측: 「제일기획이 조용한 날」(수집 결과의 부재)과
  「AI가 실적자료에 처음 올랐다」(PDF 단어 세기 결과) 두 호가 이 항목에서 걸렸어야 했다
- A7. **축마다 함의가 있는가** — 「이것이 독자에게 무엇을 뜻하는가」가 한 문장으로 적혀 있고,
  그 독자가 `profiles/<profile>.yaml` 의 `audience` 인가 (insight-guard 3-2절·I11).
  규명(R1/R2/R3)에서 멈춘 축은 지면에 올리지 않는다. 함의의 어미는 주장 강도 사다리를
  따른다 — 근거가 얇으면 버리지 말고 「~로 볼 여지가 있다」로 낮춘다. 빈 함의
  (「시사하는 바가 크다」류)는 style-guard D-2 와 같이 실패
- A8. **투자자용 좌표가 화두에 올라와 있지 않은가** — 목표주가·이익 추정·배당·자사주는
  시장이 회사 계획에 매긴 값으로 인용할 수는 있으나 헤드·화두가 되지 않는다
  (`audience.out_of_scope`). 이 브리핑의 독자는 투자자가 아니라 회사의 의사결정자다
- F1. 절(`section.sec`)마다 전시물(`figure.exhibit`) ≥1 인가. `data-edition="digest"`면
  면제하되 그때 `section.sec`가 존재하면 즉시 실패. 제일기획 절은 강등 전 재전시 시도 필수.
  전시물 정의·강등 사다리는 `templates/exhibit-first.md` 참조. 우선순위는 A5 > F1 —
  전시를 위해 경쟁사 절을 만들지 않는다
- F2. 줄글 상한 — `li.news`의 `.s` ≤2(`details` 밖 기준), 절 도입 `<p>`의 `.s` 또는
  종결부호 ≤3. 초과는 경고, 요소 수 초과는 실패. 주석(`.ex-note`)에도 B7 전문 적용

## B. 언어
- B1. 내부 작업명 0건 — 화두판·발표판·특별판·확정판·보강/반증/전환/무관·"축으로 올린다"
- B2. 취재 과정 언어 0건 — "확인됐다"·"정정"·"미확인"·"새벽 기준"·"보도로만 알던".
  **제작 체계 언어도 같은 항이다** (2026-08-14 콜드 리더 실측): "런"·"스윕"·"수집"·
  "등재"·"큰축"·"MA#"·축 점수판 기호(지지 n/반증 n)는 편집실 어휘지 독자의 말이
  아니다 — 추적 줄도 사람 문장으로 쓴다. 약어(AAM·SOT 등)는 첫 등장에 풀어쓴다
- B3. 자기 참조 0건 — "어제의 화두"·"지난 호에서 말한"·"다음 호에서 다룬다"
- B4. 명사 조각 0건 — 본문·카드·슬라이드는 완전한 문장
- B5. 날짜는 날짜로 — "5.27"식 소수점 표기 0건
- B6. 숫자·인용은 원문 그대로, 파생값 0건, 등급(1차자료/보도/관찰) 표기
- B7. 선답 금지 — 헤드라인·리드는 사실형. 해석·인과("선행지표다", "신호가 켜졌다")는
  1차자료 근거가 있을 때만 단정하고, 없으면 한계를 같은 문장에 명시한다
  ("~와 맞닿은 숫자다 — 다만 그 관계가 공시로 확인된 바 없다"). 구조는 그날 재료가
  만든다 — 재료를 미리 정한 서사 틀에 끼워맞추지 않는다

## C. 판형
- C1. 이 호는 자기 URL인가? 전날 URL을 덮어쓰지 않았는가
- C2. 구조가 그날 재료를 따르는가? 전날 지면의 절 배치를 복사하지 않았는가
- C3. 다른 호 수록 자료는 본문 재수록이 아니라 링크 참조인가 — 단, 당일 뉴스의 직접 근거 표는 그날 호에 수록한다
- C4. 모든 링크·버튼은 실제로 동작하는가? 링크 없는 죽은 라벨이 없는가
- C5. 판형 바 게이트 통과 — `python tools/check_formats.py <웹판.html>` 종료코드 0.
  그날 만든 판형이 전부 `.fmtbar` 세그먼트로 있는가. 덱을 발행해 놓고 웹판에
  프레젠테이션 세그먼트를 안 넣으면 독자에겐 없는 것과 같다 (2026-08-11 실측:
  발표판을 발행했는데 웹판 fmtbar 에는 「라디오 버전」 하나뿐이었다).
  아카이브 호의 링크 없는 PPTX 라벨은 `--archive` 로 통과시킨다 (라벨의 「PPTX」도
  덱 세그먼트로 인정된다). 덱 요구의 정본은 **의도**다 —
  루틴이 EVAL 판정대로 `--expect-deck`(심층) / `--no-deck`(표준·간략)을 명시한다.
  역방향도 게이트다: 전환할 판형이 하나뿐인 날(덱 없는 호)은 fmtbar 블록 자체를
  없앤다 — 세그먼트 1개짜리 바는 죽은 버튼이라 실패다 (2026-08-13 편집장 반려).
  플래그 없이 돌리면 파일 실존 추론으로 강등되며 그 사실이 출력에 남는다
  (이슈 #5: 파일 추론은 덱을 만들고 기록을 빠뜨린 날에 눈을 감는다)

## D. 기술
- D1. 표: thead·1열에 rowspan 0건, **4열 이상 표만** min-width 지정(스크롤 판형),
  3열 이하 표는 D6의 fit 판형(min-width 해제 — `class="fit"`)을 따른다, 모바일 폭에서 1열 고정 확인
- D2. 오디오: 컨테이너는 **MP3(audio/mpeg)** 로 넣는다 (2026-08-13 개정 — AAC/audio/mp4
  는 공개 공유 스캔이 못 다뤄 409 unscannable, E5 종결 참조) — **embed_radio.py 가 WAV
  입력을 자동 변환한다**(imageio-ffmpeg, SETUP §2 선택 의존성). 미설치·변환 실패면 도구가
  경고를 내고 원본 임베드로 강등되므로, 실행 로그에 「경고: imageio-ffmpeg 없음/변환
  실패」가 없는지로 판정한다. .mp4/.m4a 직접 이식은 금지. data:→blob 전환 코드 유지,
  배속 −/+ 버튼 동작. WAV 를 그대로 실으면 호가 10MB 를 넘어 첫 렌더가 눈에 띄게 느리다.
- D2b. **공개 배포 호에서는 아티팩트 안 다운로드를 포기한다.** 세 겹으로 막혀 있다
  (2026-08-11~12 실측): ① 뷰어 iframe 이 `sandbox="allow-scripts allow-same-origin
  allow-forms"` 라 `allow-downloads` 가 없다 ② 런타임의 `downloads` capability 를 쓰면
  플랫폼이 **공개 공유 자체를 거부**한다(deploy 422: "artifacts that use the downloads
  capability can't be shared publicly") ③ 그 capability 의 허용 확장자에 wav·mp3 가
  없다(gif png jpg jpeg webp mp4 webm txt json md). 그래서 저장 버튼은 넣지 않고,
  오디오가 필요한 독자에게는 `output/audio/YYYY-MM-DD.wav` 를 배포 묶음으로 보낸다 (E4)
- D3. 셀 줄바꿈 3줄 이하(모바일 스크린샷으로 확인)
- D4. 미정의 CSS 변수 게이트 통과 — `python tools/check_css_vars.py <웹판.html>`
  종료코드 0. var()가 참조하는 변수가 전부 정의돼 있어야 한다. 미정의 변수는 선언
  전체를 무효로 만들고 에러도 안 낸다 — 눈으로는 "좀 밋밋하네"로 넘어간다.
  다른 스킴에서 가져온 절을 붙일 때 특히 확인한다 (2026-08-12 바차트 투명 렌더,
  2026-08-11 라디오 블록·판형 바가 `--card`·`--line` 등 7개를 참조해 카드가 통째로
  배경·테두리 없이 떴다)
- D5. 바차트·게이지류는 라벨을 막대 위에 쌓는다 — 좌측 고정폭 라벨 열은 모바일 확대에서 글자가 세로로 쪼개진다
- D6. 표 판형 게이트 통과 — `python tools/check_tables.py <웹판.html>` 종료코드 0.
  3열 이하 표에 일괄 min-width 를 물리면 남는 폭이 전부 1열로 몰려 다음 열이 화면 밖으로
  밀린다(2026-08-12 모바일). 좁은 표는 fit(화면 폭 맞춤), 4열 이상은 스크롤 판형 — 눈이 아니라
  스크립트가 판정한다
- D7. 원장 정합 게이트 통과 — `python tools/check_ledger.py` 종료코드 0.
  ① okf/**·profiles/** 가 참조하는 F-### 개념이 전부 `okf/facts/` 에 실존(매달린 참조 0건)
  ② `status: deprecated` 인 개념이 index 「사실 — 유효」에 남아 있지 않음
  ③ index `generated.at` 이 facts 최신 파일·log.md 최신 항목보다 새것.
  JSONL 원장(`output/ledger/` — gitignore 대상)에만 쓰고 `okf/` 미러를 빠뜨리면
  배포본에 실체 없는 참조만 남고, 대체된 수치가 유효한 최신값으로 방송된다
  (2026-08-12 이슈 #7 실측: 매달린 참조 6건 + F-011 유효/대체 충돌 + 인덱스 08-10 정지).
  미러 + index 재생성은 JSONL 기입과 같은 실행에서 한 동작이다 (배포 정책 A안)
- D8. 호 크기 게이트 통과 — `python tools/check_size.py <웹판.html>` 종료코드 0.
  상한의 정본은 config 의 `tts.embed.max_html_mb`(기본 3.0MB — README 의 「호가 3MB
  안쪽」). 웹판은 오디오를 data URI 로 싣고 base64 는 원본의 4/3 로 부푸니
  「MP3 크기 × 1.34 + 지면」이 곧 호 크기다.
  **이 항은 편집 규율이지 공개 공유의 보증이 아니다.** 통과해도 E1 이 막힐 수 있다 —
  아래 E5 참조. 호를 작게 유지하면 스캔 가능성이 올라간다는 가정 위에 서 있다.
  **환경 상한은 킷 상한과 다른 층이다** (2026-08-15 양측 실측): 맥 앱 환경에서
  합성 프로브가 1.00MB 통과 / 1.25MB 이상 ECONNRESET 로 갈렸고, 같은 시기 개발
  로컬(Windows)은 2.61MB 를 재발행 3회·7.84MB 를 1회 발행했다. 즉 그 임계는 API
  상한이 아니다. **원인은 회선이 아니라 그 기기의 상행 대역이었다**
  (2026-08-15 재실측으로 정정): 같은 공유기·같은 순간·같은 종단에서 맥은 65~112KB/s,
  다른 PC 는 9.9MB/s 였다. 그 맥의 LAN 은 양방향 11.7MB/s · 인터넷 하행 6.2MB/s ·
  링크는 기가비트 full-duplex 에 오류 0 이었고 — **인터넷 상행만** 조여져 있었다
  (2026-08-16 확정: **버스트 허용량을 가진 토큰 버킷**이었다 — 512KB 까지 8.6MB/s 로
  올리다가 1MB 부터 60KB/s 로 무너지고, 30초 쉬면 회복했다. 같은 순간 다른 PC 는
  20MB 를 11.3MB/s 로 완주했다). 그러므로 발행이 ECONNRESET 로 끊기면 D8 상한을
  내리기 전에 **셰이퍼부터 드러낸다**. ⑴~⑶ 은 관리자 자격이 필요 없다:
  ⑴ **크기 사다리** — 64KB·256KB·512KB·1MB·2MB 를 차례로 올린다. 작은 것은 빠른데
     특정 크기부터 무너지면 셰이퍼다. 무너지기 직전이 **버스트 허용량**, 무너진 뒤가
     **지속 상한**이다. 전 구간이 고르게 느리면 그때는 진짜로 상행이 좁은 것이다.
  ⑵ **총량인지 연결당인지** — 업로드 3개 동시. 합이 단일과 같으면(117 vs 112KB/s)
     그 기기에 걸린 총량 제한이다.
  ⑶ **유선·무선 동시** — MAC 도 IP 도 다른 두 인터페이스인데 합이 단일과 같으면
     (실측 111KB/s) 버킷이 MAC/IP 가 아니라 **기기 단위**다. 랜선·IP 교체는 헛수고다.
  ⑷ **소재 판별** (자격 필요) — 기기 쪽은 `sudo dnctl list`·`sudo pfctl -sa` 로 dummynet
     파이프 확인(Network Link Conditioner 를 켠 채 잊는 사고가 흔하다), 망 쪽은 공유기
     QoS·트래픽 관리 확인. **핫스팟에 붙여 ⑴ 을 재현**하면 자격 없이도 기기/망을 가른다.
  ⑸ 랜포트·케이블 교체. 전부 정상일 때만 호 크기를 줄인다 — 킷 상한(편집 규율)을
  한 기기의 조여진 상행에 맞춰 내리면 전 설치본이 판형을 잃는다
- E5. **공개 전환 거절의 실체는 UI 문구가 아니라 API 응답이다** (2026-08-12 추적).
  Share → Anyone with the link 이 막히면 화면에는 이렇게 뜬다:
  "This version can't be shared publicly. Publish a new version or change the shared
  version, then try again." — **이 문구를 믿고 버전을 만지지 마라.** 헛수고다.
  같은 조작의 네트워크 응답은 `PATCH /api/frame/perm/<id>` → **409** 이고 본문이 원인을
  말한다: `{"reason":"unscannable", "error":"…public serving requires the served
  version's content scan to be dispatched (unscannable)"}`.
  공개 서빙은 **콘텐츠 스캔 통과가 전제**이고, 스캔이 안 걸리면 버전을 몇 개를 쌓아도
  안 풀린다. UI 가 제안하는 두 우회로가 소용없는 이유이기도 하다 — 새 버전도 같은
  내용이고, 공유 버전 콤보박스는 공개 전환 전까지 `disabled` 다.
  **거절되면 문구를 옮겨 적지 말고 개발자도구 네트워크 탭에서 이 PATCH 의 응답 본문을
  확인해 보고에 적는다.**

  실측 (2026-08-12, 같은 계정·같은 세션·같은 호를 오디오 비트레이트만 바꿔가며):

  | 호 크기 | 결과 |
  |---|---|
  | 1KB (순수 HTML) · 23KB (오디오 페이로드만 뺀 사본) | 공유됨 |
  | 0.61MB · 1.10MB · 2.14MB · 3.28MB | 전부 거절 |

  같은 페이지에서 **오디오 페이로드만 빼면 공유되고, 넣으면 거절된다** — `<script>`·
  `<audio>` 요소·CSS·구조·작은 data URI 는 통과본에도 그대로 있으므로 전부 원인이 아니다.

  **종결 (2026-08-13 규명): 원인은 코덱이다.** 위 거절 표본은 전부 AAC(`data:audio/mp4`)
  였다. 같은 호·같은 크기(2.6MB)를 MP3(`data:audio/mpeg`)로 바꾸자 즉시 공개 전환됐다.
  스캐너가 audio/mp4 data URI 를 못 다뤄 unscannable 로 박제하는 것. 처방은 재공유가
  아니라 **MP3 재이식 후 재발행** — embed_radio 가 2026-08-13 부터 MP3 로 변환한다.
  (한때 "발행 클라이언트 버전" 가설(#14)이 있었으나 오진 — 같은 데스크톱 앱에서
  AAC 는 거절되고 MP3 는 통과했다.)
  1.10MB 호는 시간을 두고 재시도해도 거절돼 「스캔 착수 대기」 가설도 반증됐다.
  (구판의 「크기 축 미확정」 결론과 anthropics/claude-code#79824 대조는 위 종결로
  닫는다 — 코덱 축이 거절/통과를 전 표본에서 일관되게 가른다.)
  판정 도구는 E6 — 공유를 켜고도 익명 개통을 확인하지 않으면 발행이 아니다
- F3. PPTX 전시 슬라이드 노트에 `[EXHIBIT: 문서명|항목명]` 토큰 ≥1, 문서명이 웹판
  `data-doc`과 일치하는가. 라디오 대본에 `[EX: 문서명]` 참조 토큰 3~4개
- F4. 전시물 계약 게이트 통과 — `python tools/check_exhibits.py <웹판.html>` 종료코드 0.
  계약의 구체 값(필수 속성·자식 요소·`.ex-hl` 상한·도메인 화이트리스트·재전시 규칙)의
  **정본은 `templates/exhibit-contract.yaml`** — check 가 그 값을 읽어 판정한다.
  요지: 출처 data 속성+주석 3요소 전부 존재, 원문 파일 실존, 공식 소스만 전시,
  과거 전시물은 재전시 표기 강제
- F5. 게이트의 정직성 — 정적 게이트(check)는 **존재·계약만** 판정한다. 판형 일치·
  하이라이트의 질은 `eval/YYYY-MM-DD.md`의 수동 스팟체크(호당 전시물 1개 무작위 →
  원문 대조)로 분리해 기록한다. 출처 각주(`figcaption.ex-src`)에 원문 URL 의무

## I. 인사이트·헤드라인
- IG1. 인사이트 게이트 통과 — `python tools/check_insight.py <웹판.html>` 종료코드 0.
  판정 규칙의 정본은 `templates/insight-guard.yaml`·`templates/headline-guard.yaml`,
  원장은 `okf/facts/`(status: deprecated 제외). issue #15 의 정적 4개에서 출발해
  지금은 아홉을 본다 (2026-08-14 문면 갱신, 이슈 #31 — 종전 「4개만」은 구현 7개와
  어긋난 낡은 자기 소개였다): ① 축(`[data-axis]`) ≥1 ② I2 — 축마다 양쪽
  (`data-side-a/b`) 사실 ≥2·원장 실존 ③ H6 — 헤드라인 자리(h1·h2)와 pull 박스의
  단위 붙은 숫자가 원장에서 문자열로 찾아짐 ④ I8 — 화두 세 칸
  (`[data-spine="결론|발견|근거"]`)이 지면에 실림 ⑤ H9 화두 구체성(IG2)
  ⑥ Q1 함의 유보 종결 상한 ⑦ Q2 시한 주장 좌표 ⑧ 발행본 F-좌표 실존
  (매달린 좌표 0건, 이슈 #25) ⑨ 뼈대 배신 대조(eval/proto 실존 시, 이슈 #30).
  판정 대상 마크업이 웹판에 없는 항목은 「판정 불가 — 마크업 계약 없음」 경고로
  통과한다(F5 — 못 재는 것을 재는 척하지 않는다). 그 항목과 나머지 I1~I10·H1~H8 은
  `eval/YYYY-MM-DD.md` 의 사람 판정으로 기록한다. 축이 0개인 호가 기계 게이트 전부를
  초록으로 통과한 설치 시험(2026-08-12, issue #15)에서 나온 항목이다
- IG2. **헤드 + 다음 한 줄 구체성** — 배경 지식 0 독자가 화두(h1)와 바로 다음 한 줄
  (answer2/standfirst)만 읽고 무슨 일이 있었는지 한 문장으로 말할 수 있는가
  (headline-guard `masthead.h1.concreteness` · 게이트 H9). 압축 ≠ 추상화 — 구체어
  (사물·수·행동)를 남기고 설명을 덜어내며, 추상 헤드는 바로 다음 줄이 즉시 구체로
  받을 때만 허용한다. 서지 사실·부재·과정은 헤드가 아니라 근거 한 줄의 재료다.
  정적 부분(h1 과 받는 줄에 단위 붙은 수·원장 고유명사가 둘 다 0이면 실패)은 IG1 의
  `check_insight.py` 가 ⑤(H9) 로 같이 판정하고, background_test 는 사람 판정으로
  기록한다. 2026-08-12 세 번 연속 반려(issue #18) 실측.
  간명 상한(⑤b, 2026-08-14 편집장 반려): h1 공백 제외 30자·대시 2절 초과는 실패 —
  정본은 `headline-guard` brevity. 실물/개념어 구분은 R6 앵커의 사람 판정
- IG3. (기계) — `check_theme`: 연간 주제 정렬 전수 판정(화두=정렬 필수·무관 0)·검수 메타
  언어 0건. `eval/theme-날짜.md` 가 판정 원본. `python tools/check_theme.py <웹판.html>`
  종료코드 0. 마지막 전체검수 — 지면의 모든 단위(화두·01절 단신·section.sec·figure.exhibit)를
  `profiles/cheil.yaml` 의 annual_theme 기준으로 정렬/보조/무관 판정하고, '무관' 단위는
  지면에서 뺀 뒤 발행한다. 검수 설명(규명·표본·주장 강도·보류 슬롯 등)은 지면 오염이므로
  0건이어야 한다 — 판정 흔적은 eval 원본으로 옮긴다 (편집장 지시 2026-08-13 ②③)
- IG4. **(정성→기계 봉인)** — 심사 패널 3렌즈(reader/refuter/crosser) 독립 채점을
  `eval/review-날짜.md` 에 기록, `check_review` 종료코드 0. 임계 미달 시 재조판 1회→재심사.
  루브릭 정본은 `templates/insight-rubric.yaml`(v2 — 6차원 × 10점, v1 기록은 5차원 스코핑), 기록 양식은
  `templates/review-form.md`. 기계는 점수를 매기지 않는다 — 기록의 실존·임계·지적
  처리(반영/기각) 완결만 봉인한다 (루브릭 황금률). 재심사도 미달이면 발행하되
  보고에 「정성 미달 N점」 명기.
  **상태: 시범 운용 — 이원 배선** (2026-08-14 문안 통일, 이슈 #23 — 종전 문안
  「배선은 임계 확정과 함께 한다」는 5절 게이트 블록에 check_review 가 v2026.08.13.4
  부터 이미 배선돼 있던 사실과 모순이었다. 코드의 실제 동작대로 통일한다):
  ① **봉인 위반은 차단이다** — 기록 부재·3인×전차원 미완결·평균 산술 불일치·
  지적 처리 미완결·미달 후 재심사 부재는 check_review exit 1 이고, 5절 게이트
  블록의 「종료코드 0 필수」가 그대로 적용된다. ② **임계 미달 자체는 비차단이다** —
  재심사(또는 --retro 소급 확정)만 기록되면 exit 0, 발행하되 보고에 「정성 미달
  N점」 명기 (fail_path — 매일 나가는 브리핑이다). ③ 임계 7.0 은 시작값이고
  편집장 확정 전이다 — 임계 확정과 「미달도 차단」으로의 승격은 편집장 결정이다
  (2026-08-13 시범 심사 실측: 08.13호 패널 평균 6.40, 첫 데이터).
  심사 대상은 중도금(뼈대 MD)이 정본이고, 잔금(HTML)에서 정성 재심사는 없다 —
  잔금은 「조판이 뼈대를 배신했는가」만 본다 (`templates/pipeline-three-stage.md`)
- IG5. **작은축의 큰축 소속 선언** — 절(`section.sec`)마다 소속 큰축을 선언한다
  (`data-macro="MA#"` — 정본은 `profiles/cheil.yaml` macro_axes, 관측 원장은 `okf/axes.md`).
  무소속 절은 즉시 실패가 아니라 필요성 재평가 대상이다 — 그 재평가 근거(왜 큰축 없이도
  지면에 남는가, 또는 새 큰축 후보인가)를 `eval/YYYY-MM-DD.md` 에 남긴다
- IG6. **반증 렌즈와 재귀납** (2026-08-15 신설, 이슈 #35 — 항목 추가일 뿐 기존 번호는 그대로다).
  ① 오늘 수집에 refuter 채널이 돌았는가 — 지침 정본은 `profiles/<profile>.yaml`
  `macro_axes[].refute_probes`. ② 축마다 `okf/axes.md` 「반증 탐색 이력」에 오늘 한 줄이
  있는가 — **반증을 찾지 못한 날도 기록한다**(「안 찾은 날」과 「찾았는데 없는 날」의 구분).
  ③ 반증이 등재된 축은 「재귀납 기록」이 있는가 — routine-SKILL 2b 절(다렌즈 재독 →
  유지/분할/합병/신설 → 편집장 승인 → 기록). ①~③의 형식은 `check_ledger` ⑥ 이 봉인한다
  (⑥ⓐ 증류 슬롯·좌표 실존·대체 여부, ⑥ⓑ 재귀납 기록 부재 **또는 최신 반증보다 이른 기록**
  시 차단, ⑥ⓒ 최근 발행일 이후 탐색 기록 부재 시 경고).
  판정의 **내용**(반증인가 아닌가, 축을 접을 것인가)은 기계가 보지 않는다 — 사람·패널 몫이다.
  **봉인의 요구 수준을 정확히 적는다** (2026-08-15 정정 — 종전 ② 문안은 「오늘 한 줄」을
  요구했는데 기계는 「최근 발행일 이후 한 줄」만 본다. 규정이 봉인보다 넓게 말하면 사람이
  봉인을 규정으로 착각한다): ⑥ⓒ 는 **경고이지 차단이 아니고**, 「오늘 한 줄」은 사람 판정이다.
  또 결과 칸이 `refute_sweep.log_result_vocab.non_search` 어휘(미탐색·미실행·건너뜀)로
  시작하는 행은 탐색 기록으로 세지 않는다 — 「아무것도 탐색하지 않았다」는 자백이 통과의
  근거가 되면 안 된다(2026-08-15 수리: 채널 도입 당일 세 축의 첫 행이 정확히 그 자백이었고,
  ⑥ⓒ 는 경고 한 줄 없이 통과했다).
  실측 근거: okf/axes.md 계보 재계산 — MA1 지지 5·조건형성 7·중립 4, MA2 지지 14·조건형성
  9·중립 5, MA3 지지 9·조건형성 4·중립 0 (합계 57행, 무소속 원장 10행 별도). **세 축 전부
  반증 0** 이고, 반증 재료가 지면에 실려 있는데도 계상되지 않았다
  (종전 문안 「원장 67건 … 지지 9·조건형성 4·중립 2」 는 어떤 집계와도 맞지 않아 정정했다)

- IG7. **상시 소스 스윕의 전수 기록** (2026-08-15 신설, 이슈 #36 — 항목 추가일 뿐 기존
  번호는 그대로다). 오늘 지면을 냈다면 `profiles/<profile>.yaml` 의 `sources` 중
  **상시(cadence: 매일)** 소스 전수가 오늘 돌았는가. 어휘·로스터의 정본은 같은 파일의
  `sweep_gate` 블록(`daily_cadence`·`known_cadence`)이다 — 정본에 없는 cadence 값은
  조용히 빠지지 않고 **차단**되고, 상시 소스가 0건이면 그것도 차단된다(끄려면
  `sweep_gate.enabled: false` + `disabled_reason` 을 명시해야 한다).
  못 돈 소스는 조용히 두지 말고 「건너뜀 YYYY-MM-DD — 사유」 를 남긴다 — 그러면 차단이
  아니라 경고로 강등된다(F5 — 정직한 실패는 막지 않는다). **다만 로스터 전수를 건너뛰면
  차단이다** — 전수 건너뜀은 정직한 부분 실패가 아니라 「그날 스윕 0건」이다.
  면제는 **그날 생산 기록이 없을 때만**이다(quiet_day 또는 검증만으로 끝난 날).
  생산·발행한 뒤에 검증 세션을 이어 붙인 날은 면제되지 않는다 — 종전 판정은 그날 원장에
  `검증만` 항목이 하나라도 있으면 면제했고, routine-SKILL 0-a 상 **같은 날 두 번째 실행이
  정확히 그 기록을 남기므로** 아침에 좁게 돌아 발행한 날도 오후에 한 번 더 돌리기만 하면
  봉인이 사라졌다(2026-08-15 실측 수리).

  **기계 경로 두 개, 그리고 왜 `check_run` 이 required-gates 에 없는가** (2026-08-15 정정):
  ⓐ **발행 시점** — `check_publish`(E6, required-gates 안)가 오늘 날짜로 이 대조를 함께 한다.
  IG7 을 발행 시점에 강제하는 것은 이 경로다. ⓑ **배포 직후·다음날 점검** —
  `python tools/check_run.py --date <날짜>` 종료코드 0.
  `check_run` 자체는 로스터에 **넣지 않는다**: required-gates 는 run_log 종료 줄의 `gates`
  필드에 무엇이 있어야 하는지의 정본인데 `check_run` 은 바로 그 종료 줄을 판정한다 —
  발행 전에는 아직 없는 줄이라 순환이다. 그래서 봉인을 순환하지 않는 지점(ⓐ)으로 옮겼고,
  로스터 블록 자체는 손대지 않았다(기존 run_log 기록의 판정 불변).
  종전 문안은 「`check_run` 종료코드 0 이 판정이다」라고만 적어, 발행 시점에 IG7 을 강제하는
  기계 경로가 0개인 채로 항목만 떠 있었다 — 사람이 잊으면 아무것도 잡지 않았다.

  **한계 명시**: 스윕 기록(`last_checked`·run_log `sources`)은 스윕의 자기 보고다. 이 항이
  잡는 것은 「기록조차 안 한 채 좁게 돈 날」이지 「형식적으로 기록만 한 날」이 아니다 —
  소스를 실제로 열었는지, 열어서 무엇을 봤는지는 사람 판정으로 `eval/YYYY-MM-DD.md` 에 남긴다.
  또 **과거 날짜는 원장 증거가 있을 때만 판정한다**: 프로파일 `last_checked` 는 소스마다
  최신 한 값만 보관하는 현재값 스냅샷이라 어제의 스윕 범위를 증언하지 못한다. 그날 종료 줄에
  `"sources":[...]` 를 남긴 날만 소급 판정된다(2026-08-15 수리 — 종전에는 스냅샷을 임의 과거
  날짜와 등호 비교해 정상 발행된 옛 호가 전부 실패로 뒤집혔다)

## E. 배포
- E1. 게이트 전부 통과 후 Share "Anyone with the link" ON — **그날 발행한 아티팩트 전부에**.
  웹판만 켜고 덱을 두면 fmtbar 링크가 남에게 404다 (2026-08-11 실측: 발표판이
  private 인 채로 남았다). 산출물 하나마다 한 줄씩 공유 상태를 기록한다.
  순서 주의(2026-08-12 확정): 공유를 켜는 순간 그 버전이 핀으로 잡히고, 그 뒤의
  수정은 핀 이동 거절(재현됨·원인 미규명)에 노출된다 — 그래서 「발행 직후」가 아니라
  「게이트 전부 통과 후」에 켠다 (routine 4b 발행 절차 ①~⑤). 공유 후에는
  `check_formats --check-links` 로 익명 개통을 판정한다
- E2. 공유 고정 버전 = 방금 발행한 버전 (번호가 가장 큰 것, 시간 표기로 고르지 않는다)
- E3. 로그아웃 상태에서 링크 열어 이번 호 헤드라인 문자열 확인
- E4. 당일 URL 기록 + 배포 메시지에 링크·WAV 첨부
- E6. (기계) `python tools/check_publish.py <날짜>` 종료코드 0 — **직접 판정**(이슈 #21
  개정): ① 무작위 uuid 대조군 자기검증 ② `api/frame` 200+public 으로 공유 판정(401=
  비공개 또는 없는 uuid — 구판의 404=통과 휴리스틱은 오타 URL·삭제된 아티팩트도
  통과시켰다) ③ 발행본 본문(`_f/<ver>`)에 오늘 호 h1 대조 — 핀이 옛 버전·다른 호면
  실패. 발행·공유 완료 뒤에만 돌린다 (2026-08-13 지시: "항상 전체공유")
  **종료코드 3분법** (2026-08-15 신설, 이슈 #37): 0 통과 / 1 판정 결과 실패 /
  **2 판정 불가**(CF 봇 차단 챌린지·네트워크 오류 — 대조군이 챌린지면 대상 조회 전에
  2 로 끝낸다) / 64 사용법 오류. 2 는 통과가 아니다: `gates` 에 `check_publish:2` 로
  남고 `check_run` 이 「판정 불가로 미완결」로 차단한다. 2 를 받으면 **E5 처방을 따르지
  마라** — 봇 차단 응답은 공유 상태를 증언하지 않으므로 MP3 재이식·재발행은 멀쩡한
  호를 고치는 짓이다(2026-08-15 맥 회선 실측: CF 403 을 「공유 OFF」로 오진했고
  대조군도 같은 403 을 받아 ①이 조용히 통과했다). 다른 회선에서 재판정하거나,
  소유자가 브라우저로 공유·핀을 직접 확인하고 「수동 확인 마감」으로 기록한다
  (routine-SKILL 4b ⑤ — 자동 통과가 아니라 별도 표기다)
- E7. (기계) `--check-links` 의 발행본 대조 — 링크는 ①로컬 존재 ②개통 ③**발행본에
  실림** 세 판정이 전부다. ③이 없으면 「V4 를 발행하고 핀은 V3」가 게이트 초록으로
  통과한다 (이슈 #21, 2026-08-13 실측 — 독자에게 덱 링크가 없었다).
  **E6 와 같은 종료코드 3분법을 쓴다** (2026-08-15 신설 — 검수 문제 2·6): 익명 GET 이
  CF 봇 차단 챌린지를 받으면 「공유 OFF 의심」 1 이 아니라 **2(판정 불가)** 다.
  종전에는 이 게이트만 1 을 내서, 같은 CF 회선에서 E6 가 정직하게 2 로 끝나는 같은
  순간 `run_log` 에 `check_formats:1` 이 남았고 — 실행자는 E1/E7 문안대로 공유를 다시
  켜고 재발행하러 갔다. 2 를 받으면 공유를 다시 켜지도, 재발행하지도 마라: 다른 회선에서
  재판정하거나 「수동 확인 마감」으로 기록한다 (routine-SKILL 4b ⑤)
- E8. (2026-08-15 신설 — 검수 문제 4·7·9. **기존 번호는 손대지 않았다**)
  **판정 불가(2)를 받은 날을 닫는 법, 그리고 이 3분법이 못 하는 일.**
  - 닫는 길은 둘뿐이다: ⓐ 다른 회선에서 재판정해 `:0` 을 기록한다 ⓑ 소유자가
    브라우저에서 공유·핀을 **직접** 확인하고 run_log 종료 줄에 형식대로 적는다 —
    `"reason": "수동 확인 마감 <YYYY-MM-DD HH:MM> <판정 불가 게이트 이름 전부> — 확인 경위"`.
    `check_run` 이 이 **형식**을 읽어 그 날짜를 닫는다(넷 다 필요: 표식·시각·게이트
    이름 전수·경위 15자 이상). 종전에는 이 상태가 기계에 없어 `:2` 인 날짜가 영구히
    exit 1 이었고, 그 압력이 `:2` 대신 `:0` 을 적게 만들었다.
  - 그 통과는 **형식 봉인이지 공유 상태에 대한 기계의 판정이 아니다** (황금률 ③).
    회선이 회복되면 `:0` 으로 재판정해 기록을 갱신한다.
  - 한계 셋을 같이 읽는다: (a) 3분법을 쓰는 게이트는 **E6·E7·E9 셋뿐**이고 나머지는
    종전대로 0/1 만 낸다 (b) CF 판정은 응답 본문·`cf-mitigated` 헤더의 **문자열
    휴리스틱**이라 CF 가 문구를 바꾸면 다시 「공유 OFF」로 오진한다 (c) 이 수리는
    오진을 막을 뿐 **CF 회선에서 공유 상태를 판정해 주지 않는다** — 2 를 받은 호의
    공유 여부는 미지로 남는다.
  - 본문 표식은 **비-200 응답에서만** CF 로 본다 (2026-08-15 — 검수 문제 1·8):
    발행본 본문 대조에 넘어가는 것은 그 호의 지면 HTML 자체라, 지면이
    「just a moment」·challenges.cloudflare.com 을 인용하기만 해도 HTTP 200 정상
    응답이 판정 불가로 접혔다. `cf-mitigated` **헤더**는 지면이 못 붙이는 표식이라
    200 에서도 그대로 CF 로 본다.

- E9. (2026-08-16 신설 — 맥 워커 규명. **기존 번호는 손대지 않았다**)
  **상행 사전판정** — `python tools/check_uplink.py --target-mb <max_html_mb>`.
  이 기기가 표준 호를 **한 번에** 올릴 수 있는지를 호를 만들기 **전에** 판정한다.
  - 언제 반드시 돌리는가: ⓐ **설치 직후**(SETUP) — 아직 호를 만들기 전에 그 기기가
    발행 가능한 기기인지 확정한다 ⓑ 발행이 끊긴 직후 — D8 의 원인 판별을 이 게이트가
    대신한다. 둘 다 사람이 읽는 보고까지 낸다(버스트 허용량·지속 상한).
  - 종료코드 3분법: 0 목표 완주 / 1 못 올림 / 2 최소 단조차 못 보냄(기기가 아니라
    판정이 성립 안 함 — 망 단절·종단 장애). 사용법 오류는 64.
  - **1 을 받았을 때가 이 게이트의 존재 이유다.** 보고가 「셰이퍼」라고 하면 회선이
    좁은 게 아니다 — 그때 호 크기를 깎는 것이 D8 이 막으려는 오진이고, 킷 판형을
    한 기기의 셰이퍼에 맞춰 내리면 전 설치본이 판형을 잃는다. 셰이퍼를 없애는 것이
    정공법이고 소재 판별은 D8 ⑷ 다.
  - **왜 아래 로스터에 없는가** (의도된 부재다): 로스터는 「그 호」를 판정하는 게이트의
    정본인데 이것은 「그 기기」를 판정한다. 그리고 매 발행마다 외부 계측 종단에 수 MB 를
    올리는 의존을 전 설치본에 지우면, 그 종단이 바뀌는 날 모든 설치본이 발행 불가(2)가
    된다. 기기의 능력은 호마다 변하지 않으므로 설치 시점과 실패 시점에 돌린다.

## 기계 게이트 로스터 — check_run 이 읽는 정본 (2026-08-14 신설, 이슈 #24)

발행(산출물이 실존하는 날)의 run_log 종료 줄 `gates` 필드는 아래 로스터의 **전 게이트**를
`이름:0` 으로 담아야 한다 — `check_run` 이 이 블록을 읽어 대조한다. 종전에는 목록이
「비어 있지 않음」만 판정해, `gates:["check_tables:0"]` 한 줄로 나머지를 건너뛴 발행이
통과했다(우회 실측). 로스터에 없는 게이트를 추가로 기록하는 것은 허용된다.
게이트를 신설·폐지하면 이 블록과 routine-SKILL 5절 게이트 블록을 같은 커밋에서 고친다.

```required-gates
# 발행 직전에 돌려 run_log 종료 줄 `gates` 에 기록해야 하는 게이트 전수 (이슈 #24).
# `#` 로 시작하는 줄은 주석이다 (2026-08-15 신설).
# check_run 은 **의도적으로 여기 없다** — 순환이기 때문이다: 이 블록은 run_log 종료 줄의
# `gates` 필드가 무엇을 담아야 하는지의 정본인데, check_run 은 바로 그 종료 줄을 판정한다
# (발행 전에는 아직 없는 줄이다). IG7 의 발행 시점 봉인은 check_publish 가 대신한다 (IG7 ⓐ).
check_tables
check_exhibits
check_css_vars
check_ledger
check_size
check_insight
check_formats
check_theme
check_review
check_publish
```

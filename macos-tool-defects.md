# 도구층 결함 2건 — 배포판 재현 로그, 그리고 앞 코멘트 정정 (이슈 #33)

## 먼저 정정 — 앞 코멘트는 수정판으로 측정한 것이었다

첫 코멘트의 C-4 절에서 `check_run` 의 자정 넘김 결함을 「부수 발견」으로 보고했다. 그러나 **그 시점에 나는 이미 `tools/check_run.py` 를 로컬에서 고쳐 둔 상태였고, 그 사실을 밝히지 않았다.** 보고된 판정 일부는 배포판이 아니라 수정판을 돌린 결과였다.

순서가 뒤집혔다. 이 킷의 규율은 "규정은 실측 반려에서 나온다"이고 이 이슈 본문도 "실측 결과가 분기 커밋의 근거가 된다"고 정한다. 근거를 올리기 전에 수리부터 한 것은 그 순서를 뭉갠 것이고, CLAUDE.md 가 경고한 「맥락 없는 개선이 규칙의 근거를 지운다」에 정확히 해당한다.

그래서 수정을 **`git stash` 로 물리고 배포판(v2026.08.14.6, origin/main `5c25d4f`) 상태로 되돌린 뒤 다시 쟀다.** 아래는 전부 배포판 코드의 출력이다. 수리는 이 근거가 이슈에 박힌 뒤 별도 승인을 받아 진행한다.

---

## 결함 1 — `selftest.py` 가 실사용 시작과 동시에 죽는다 (신규, 중대)

이슈 본문은 selftest 를 「**도구층 검증은 이 한 방이다**」로 지정하고, 불일치가 나오면 출력 전체를 붙이라고 했다. 그 불일치가 나왔다.

### 배포판 출력

```
OK   [check_run] run 로스터 전수 기록 — exit 0 (기대 0)
OK   [check_run] run 한 줄 기록 — 로스터 미달 — exit 1 (기대 1)
OK   [check_run] run 실패코드 :1 기록 — exit 1 (기대 1)
OK   [check_theme] theme data-macro 정상(MA1) — exit 0 (기대 0)
OK   [check_theme] theme data-macro 오타(M2A) — exit 1 (기대 1)
OK   [check_ledger] ledger 원본 정합 — exit 0 (기대 0)
OK   [check_ledger] ledger 미태깅 신규 사실 — exit 1 (기대 1)
Traceback (most recent call last):
  File "tools/selftest.py", line 290, in <module>
    main()
  File "tools/selftest.py", line 263, in main
    setup()
  File "tools/selftest.py", line 252, in <lambda>
    lambda: edit(A, "**점수판** 지지 12", "**점수판** 지지 13"),
  File "tools/selftest.py", line 134, in edit
    raise RuntimeError(f"시험 픽스처 오류 — {os.path.basename(path)} 에 「{old[:30]}」 이 없다")
RuntimeError: 시험 픽스처 오류 — axes.md 에 「**점수판** 지지 12」 이 없다
```

15케이스까지 통과한 뒤 `RuntimeError` 로 **중단**된다. 남은 케이스는 실행되지 않는다.

### 원인

`selftest.py:252` 가 `okf/axes.md` 의 문자열 「`**점수판** 지지 12`」를 **시드 수치 그대로 하드코딩**해 픽스처로 쓴다. 그런데 점수판은 **루틴이 관측을 등재할 때마다 갱신되는 값**이다. 이번 실측에서 신규 사실 10건(F-056~F-065)을 등재하며 MA2 지지가 12 → 14 가 되자, 그 문자열이 사라져 픽스처가 깨졌다.

### 왜 중대한가

- **설치본이 루틴을 한 번이라도 실제로 돌리면, 그 순간부터 selftest 가 영구히 죽는다.** 원장은 되돌아가지 않으므로 스스로 낫지 않는다.
- 즉 이 스크립트는 **갓 clone 한 시드 상태에서만 동작한다.** 도구층 검증이 가장 필요한 시점(운영 중 회귀 검출)에 정확히 무력하다.
- macOS 와 무관하다. **모든 플랫폼의 모든 설치본에 걸린다.** 앞선 CLI 실측에서 24/24 통과가 나왔던 것은 그때가 루틴 실행 **전**이었기 때문이다 — 통과 자체가 "아직 안 써 봤다"의 부작용이었다.

### 재현 절차

1. 킷을 clone 하고 `python3 tools/selftest.py` → 통과
2. 루틴을 1회 실행해 관측을 등재 (axes 점수판이 갱신됨)
3. 다시 `python3 tools/selftest.py` → `RuntimeError` 로 중단

---

## 결함 2 — `check_run` 이 자정 넘긴 실행을 두 날짜로 가른다 (배포판 재현)

### 원장 (실행 1건 — 21:36 시작, 다음날 09:35 종료)

```
1행 start started_at=2026-08-14T21:36:06 ended_at=None
2행 end   started_at=2026-08-14T21:36:06 ended_at=2026-08-15T09:35:00
```

종료 줄에는 게이트 10종이 전부 기록돼 있다 (`check_publish:1` 외 9종 `:0`).

### 배포판 출력 — 시작일

```
판정 대상: 2026-08-14
  산출물  output/web/2026-08-14.html : 실존
  원장    해당 날짜 항목 1건
    1행 event=start mode=미정 result=None session=manual
실패: 게이트 기록 없는 발행 — 산출물은 있는데 해당 날짜 종료 줄의 gates 필드가
      없거나 비어 있다. 기계 게이트를 건너뛴 발행이거나 기록을 빠뜨린 것이다 (이슈 #18).
종료코드=1
```

### 배포판 출력 — 종료일

```
판정 대상: 2026-08-15
  산출물  output/web/2026-08-15.html : 없음
  원장    해당 날짜 항목 1건
    2행 event=end mode=생산 result=실패 session=None
실패: 실행이 스스로 실패/중단을 기록했다 (2행, 사유: ...)
종료코드=1
```

### 원인

`load_entries()` 가 날짜를 `stamp = ended_at or started_at` 으로 가른다. 시작 줄은 `ended_at` 이 없어 `started_at`(08-14)으로, 종료 줄은 `ended_at`(08-15)으로 잡혀 **한 실행의 짝이 두 날짜로 갈린다.**

### 왜 문제인가

- 08-14 판정이 내는 문면은 **「기계 게이트를 건너뛴 발행」(이슈 #18)** 이다. 그런데 이 실행은 게이트를 **전종 돌리고 종료 줄에 기록까지 했다.** 게이트를 성실히 돌린 실행을 게이트 미실행으로 지목하는 것은 #18 이 막으려던 것과 **정반대 방향의 오진**이다.
- routine-SKILL 0-원장 절은 "같은 `started_at` 으로 짝을 맞추고"를 규약으로 정하는데, 판정 스크립트는 그 짝을 다른 필드로 자른다. **규약과 판정이 어긋나 있다.**
- 07:00 생성이 정상 경로라 평시엔 드러나지 않는다. 지연·재시도·장시간 실행으로 자정을 넘긴 날에만 나오고, 하필 그런 날은 사람이 로그를 들여다보는 날이다.

---

## 잔여 항목 (다음 워커 인계)

| 항목 | 필요 조건 |
|---|---|
| B-3 allowlist 실적용 | **Windows 앱 워커** — 맥에서는 하네스가 `.claude/settings.json` 쓰기를 차단해 경로 자체가 없다. 같은 차단이 Windows 에서도 나는지가 관건 |
| C-3 `check_publish` E6 | **정상 회선** — 이 회선은 Cloudflare 챌린지로 익명 판정이 전건 403 이다 |
| C-4 루틴 완주 | 위와 동일 — 발행 종단이 열려야 호 마감까지 간다 |

---

## 정리

- 이번 회차의 값은 맥 검증이 아니라 **도구층 결함 2건의 규명**이다. 둘 다 macOS 와 무관하며 모든 설치본에 걸린다.
- selftest 결함은 특히 무겁다 — **이 킷의 도구층 검증은 "아직 안 써 본 설치본"에서만 동작한다.**
- 수리안은 이미 로컬에 있으나 `git stash` 로 물려 두었다. **근거가 이 이슈에 박힌 지금이 수리를 시작할 자리**이며, 그것은 별도 승인 사항이다.
- 앞 코멘트에서 수정판으로 측정한 부분을 이 코멘트가 대체한다.

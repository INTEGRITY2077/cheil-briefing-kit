# macOS 앱 환경 실측 — 이슈 #33 미실측 5항목

- **실측일**: 2026-08-14~15
- **킷 버전**: v2026.08.14.6 (`check_version.py` 종료코드 0, 원격 일치)
- **환경**: macOS Darwin 25.3.0 · arm64 (Mac mini) · Claude 데스크톱 앱 (Claude Code 탭)
- **직전 실측과의 차이**: 앞선 코멘트는 CLI 단독이라 GUI 의존 5항목이 미실측으로 남았다. 이번은 앱 세션이라 그 5항목을 대상으로 한다.
- **규정 미변경 원칙**: 실측 전 SETUP·게이트·템플릿을 고치지 않았다. 아래는 전부 관측이고, 문안 제안은 마지막 절에만 둔다.

---

## 요약

| 항목 | 판정 |
|---|---|
| B-1 사이드패널 브라우저 | **통과** |
| B-2 scheduled-tasks MCP / 세션 크론 | **통과 (둘 다 가용)** |
| B-3 allowlist 실적용 | **실측 불가 — 차단 사유가 발견 1건** |
| C-3 발행→전체공유→check_publish | **부분 — 발행 경로 통과, 실물 호는 회선에서 실패** |
| C-4 루틴 완주 run_log·check_run | **부분 — 게이트 전수 통과, 자정 넘김 결함 1건** |

**핵심**: 맥 고유 결함은 이번에도 0건이다. 킷의 런타임·게이트·앱 어포던스가 전부 동작했다. 완주를 막은 것은 킷도 macOS도 아니라 **이 설치 환경의 업링크**이며, 그 과정에서 킷 쪽 결함 2건(자정 넘김·크기 게이트의 침묵)이 부수적으로 드러났다.

---

## B-1. 사이드패널 샌드박스 브라우저 — 통과

`Claude_Browser` 계열이 앱에 존재하고 전부 동작한다.

| 호출 | 결과 |
|---|---|
| `preview_start {url:"https://claude.ai"}` | 성공 — `navOk: true`, 패널 1장 |
| `get_page_text` | 성공 — `Title: Sign in - Claude` (로그아웃 판정 ⓐ) |
| `tabs_create` / `tabs_close` | 성공 — 탭 왕복 |
| 로그인 후 `get_page_text` 재판정 | 성공 — `Title: New chat - Claude` (로그인 판정 ⓐ) |

- **0-3 판정 절차가 문안대로 성립한다.** 조용한 경로(`get_page_text`)로 ⓐ를 판정했고, 스크린샷 없이 로그아웃→로그인 전환을 잡아냈다.
- **#6·#8·#12 계보의 카드 타이밍 문제는 재발하지 않았다.** `preview_start` 한 번으로 카드가 한 장 떴고, 겹쳐 뜨거나 표시되지 않는 현상은 없었다. `tabs_create` 단독 호출 실패(#8)도 재현되지 않았다 — 이번에는 `preview_start` 이후 호출이라 조건이 달랐다.
- `mcp__claude-in-chrome__list_connected_browsers` 도 응답한다. 다만 이 계정에 연결된 크롬은 **Windows 기기 1대**(`osPlatform: "Windows"`, `isLocal: false`)뿐이라 맥 로컬 크롬 폴백 경로는 이번에 실측되지 않았다.

**결론**: 배포 조작(공유 토글·핀 지정·로그아웃 검증)의 전제인 내장 브라우저는 맥 앱에서 온전하다.

---

## B-2. scheduled-tasks MCP / 세션 크론 — 통과, 둘 다 가용

SETUP 0-2 「스케줄 도구 확인」에 **둘 다 가능**으로 적을 수 있다.

**scheduled-tasks MCP** — 생성·목록·삭제 왕복 전부 성공:
- `list_scheduled_tasks` → 초기 `No scheduled tasks found`
- `create_scheduled_task` (fireAt 일회성) → 성공, `/Users/<user>/.claude/scheduled-tasks/<id>/SKILL.md` 생성
- `list_scheduled_tasks` → 생성분이 `nextRunAt`·`enabled: true` 와 함께 노출
- `delete_scheduled_task` → 성공 (SKILL.md 는 복구용으로 디스크에 남는다 — 실측 후 직접 삭제함)

**세션 크론** — `CronCreate`(일회성)·`CronList`·`CronDelete` 전부 성공. 7일 자동 만료 고지는 도구 설명에 그대로 있다.

**기록해 둘 두 가지**:
1. 태스크 생성 응답이 **"Run now 로 도구를 미리 승인해 두라"** 고 안내한다 — 원격 커넥터·브라우저 제어를 쓰는 태스크는 이후 실행에서 권한 프롬프트에 멈출 수 있다는 뜻이다. SETUP 4-2 예비 태스크 등록이 정확히 이 조건에 해당한다(루틴이 브라우저로 공유를 켠다).
2. 스케줄드 태스크는 **앱이 켜져 있을 때만** 돈다는 문구가 도구 설명에 있다 — SETUP 4-2b ③ 문장과 일치한다.

---

## B-3. permissions allowlist 실적용 — 실측 불가 (차단 사유가 발견)

**결과부터**: allowlist 의 맥 대응 항목(`Bash(curl:*)` 필요 지점 등)을 **실측하지 못했다.** 앞 단계에서 막혔기 때문이다.

SETUP 5절대로 설치 위치에 `.claude/settings.json` 을 만들려 했으나, **Claude Code 의 auto mode classifier 가 그 쓰기를 거부**했다. 권한 프롬프트가 뜬 것이 아니라 **도구 호출 자체가 차단**됐다 — 즉 사용자가 승인할 카드조차 나오지 않는다.

이것이 뜻하는 바:
- 이슈 본문이 예상한 "권한 요청 팝업 = B-3 실측" 시나리오는 **성립하지 않았다.** 팝업 대신 거부다.
- SETUP 5절은 카드 ② 승인 직후 무인으로 파일을 만드는 절차인데, 이 환경에서는 그 절이 무인으로 완주하지 못한다.
- 이것은 **macOS 문제가 아니라 하네스 정책 문제**다. 같은 차단이 Windows 앱에서도 나는지는 이 워커가 판정할 수 없다 — 교차 실측이 필요하다.

**따라서 `PowerShell(Invoke-WebRequest:*)` 의 맥 대응 목록은 여전히 미실측이다.** 다만 정황 하나는 남는다: 이번 세션에서 `curl` 은 실측 목적으로 여러 번 호출됐고 **한 번도 권한 카드를 띄우지 않았다**. 이 세션의 권한 모드가 그렇게 설정된 것인지, allowlist 와 무관한 것인지는 위 차단 때문에 분리 판정이 불가능했다.

---

## C-3. 발행 → 전체공유 → check_publish — 부분

**발행 경로 자체는 맥 앱에서 동작한다.** 크기를 달리한 프로브 2건이 발행에 성공했다:

| 프로브 | 크기 | 결과 |
|---|---|---|
| 발행 경로 점검 | 0.3KB | 성공 (URL 발급) |
| 크기 프로브 | 1.0MB | 성공 (URL 발급) |

**실물 호(2.13MB)는 발행되지 않았다.** 반복 시도 전부 `ECONNRESET`.

원인은 킷도 플랫폼도 아니다. **이 설치 환경의 업링크**다 — 킷과 무관한 도구(curl)로, claude.ai 와 무관한 호스트(httpbin.org)에 같은 패턴이 독립 재현된다:

| 업로드 | 결과 |
|---|---|
| 512KB | 200 OK · 6.5초 (~80KB/s) |
| 1MB | 200 OK · 11.8초 (~89KB/s) |
| 2MB | 전송 완료 후 HTTP/2 스트림 리셋 (curl 92) |
| 3MB | 전송 완료 후 HTTP/2 스트림 리셋 (curl 92) |

- 상행 실측 **약 85KB/s (≈0.7Mbps)**.
- 크기 제한이 아니다 — 아티팩트 플랫폼 문서상 상한은 16MB이고, 실패는 깔끔한 거부 응답이 아니라 연결 리셋이다.
- 시간 제한도 아니다 — 소량 데이터로 92초를 끄는 연결은 정상 완료한다(대조 실험). **본문 크기에서 끊긴다.**
- 이 회선에는 터널 인터페이스가 8개 있고 MTU 가 제각각이다(1500·1380·2000·1000). 경로상 중간 장비가 큰 요청 본문을 끊는 정황이나, 여기까지가 이 워커가 잴 수 있는 범위다.

**따라서 전체공유 토글과 `check_publish` E6 개통 판정은 미실측으로 남는다** — 발행이 선행조건이기 때문이다. 이 두 항목은 정상 회선의 맥 앱에서 이어받아야 한다.

**부수 발견 — `check_size` 가 발행 불가 호를 초록으로 통과시킨다.** 2.13MB 호에 대해 「상한 대비 71% 통과」를 냈다. D8 상한(`config tts.embed.max_html_mb`, 기본 3.0MB)은 편집 규율이라는 주석이 이미 config 에 있으나, 게이트 출력만 보는 실행자는 발행 실패를 예측할 수 없다. 이 호가 그 공백의 실측 사례다.

---

## C-4. 루틴 완주 run_log·check_run — 부분

**게이트는 전수 통과했다.** 실전 산출물(전시물 3개·표 3개·오디오 이식 완료 웹판)에 대해 로스터 10종 중 발행 선행 9종이 종료코드 0:

```
check_tables:0   check_exhibits:0  check_css_vars:0  check_ledger:0
check_size:0     check_insight:0   check_formats:0   check_theme:0   check_review:0
```

- `check_ledger` 는 신규 사실 10건(F-056~F-065) 등재 후 **3중 장부 정합**(JSONL·facts 미러·index·axes 점수판)까지 통과했다.
- `check_review` 는 3렌즈 심사를 **본심사(평균 6.00) → 재조판 → 재심사(평균 6.50)** 로 돌린 기록을 봉인 판정했다. 임계 7.0 미달이라 규정(⑤ 정책 문면)대로 **「재심사 미달 발행」 명기 조건부 통과**다.
- `check_formats --no-deck` 은 단일 판형 호로 정상 통과 — 덱 없는 날 fmtbar 를 달지 않는 규칙(2026-08-13 반려)이 그대로 작동한다.
- `check_publish` 만 미실행 — C-3 사유와 동일하다.

**`check_run` 완주는 하지 못했다.** 발행이 막혀 호 마감(`output/artifact-url-<날짜>.txt` 기록)에 도달하지 못했고, 종료 줄의 `gates` 기록 시점도 그 뒤이기 때문이다.

**부수 발견 — 자정을 넘긴 실행의 원장 짝이 갈라진다.** 이번 실측은 21:36 에 시작 줄을 쓰고 다음 날 새벽까지 이어졌다. `check_run.py` 는 날짜를 `ended_at or started_at` 으로 판정하므로(`load_entries`), 자정을 넘긴 실행은 시작 줄이 전날에, 종료 줄이 당일에 잡혀 **같은 실행의 짝이 두 날짜로 갈라진다**. routine-SKILL 0-원장 절은 "같은 `started_at` 으로 짝을 맞춘다"고 규정하는데, 판정 스크립트는 그 짝을 하루 단위로 자른다. 07:00 생성이 정상 경로라 평시에는 드러나지 않으나, 지연·재시도로 자정을 넘긴 날에는 "안 돈 날"과 "돌았는데 빈손인 날"의 구분이 다시 무너진다 — 이슈 #9 가 막으려던 바로 그 지점이다.

---

## 실측이 뒷받침하는 문안 제안 (반영은 원작자 판단)

1. **SETUP 0-2 스케줄 도구 표** — macOS 앱은 scheduled-tasks MCP·세션 크론이 **둘 다** 잡힌다고 적을 수 있다 (B-2 실측).
2. **SETUP 4-2 예비 태스크 등록** — 태스크 도구가 안내하는 "Run now 선승인"을 절차에 넣을지 검토. 루틴이 브라우저로 공유를 켜므로 이후 실행이 권한 프롬프트에 멈출 조건에 정확히 해당한다 (B-2 관측).
3. **SETUP 5절** — `.claude/settings.json` 생성이 하네스에 차단될 수 있다는 분기가 필요하다. 현행 문안은 이 절이 무인으로 완주한다고 전제한다 (B-3 실측).
4. **`check_size` (D8)** — 상한 통과가 발행 성공을 뜻하지 않는다는 사실을 출력에 노출할지 검토. 이번 호가 실측 사례다 (C-3 부수 발견).
5. **`check_run.py`** — 자정을 넘긴 실행의 시작·종료 줄이 `started_at` 기준으로 한 날에 묶이도록 할지 검토 (C-4 부수 발견).

**완주가 남긴 것**: B-1·B-2 는 판정 완료, B-3 는 차단 사유 자체가 보고 대상, C-3·C-4 는 정상 회선의 맥 앱에서 이어받아야 하는 잔여 항목이다. 이번 실측에서 **macOS 고유 결함은 0건**이었다.

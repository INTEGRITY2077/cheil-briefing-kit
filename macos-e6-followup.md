# 추가 실측 — C-3 공유 토글 완주, 그리고 E6 게이트의 오판 (이슈 #33)

앞 코멘트에서 C-3 를 "발행이 막혀 미실측"으로 처리했으나, 그것은 과한 판정이었다. 이슈가 재려는 것은 *그 호가* 발행되는지가 아니라 **맥 앱에서 브라우저 대행 공유 토글이 되는지, `check_publish` 가 개통을 판정하는지**다. 이미 발행에 성공해 있던 1.0MB 프로브로 그 두 칸을 마저 쟀다.

---

## 1. 브라우저 대행 공유 토글 — **통과**

사이드패널 브라우저(`Claude_Browser`)만으로 전 과정이 됐다. 사용자 크롬을 점유하지 않았다.

| 단계 | 결과 |
|---|---|
| 아티팩트 URL 로 navigate | 열림 — "Artifacts are private by default" 안내 표시 |
| 공유 버튼 클릭 | Share 대화상자 — People with access / General access / Shared version |
| General access 드롭다운 | `Only you` → 선택지 2개(`Only people with access` · `Anyone with the link`) |
| `Anyone with the link` 선택 | 확인 대화상자 — "Share with anyone who has the link?" |
| `Share publicly` 클릭 | **성공** — General access 가 `Anyone with the link` 로 전환 |

**409 unscannable 거절은 발생하지 않았다** (이 프로브에는 오디오 임베드가 없다 — E5 계보와는 조건이 다르다).

**부수 확인 — 「공유를 켜는 순간 그 버전이 핀으로 잡힌다」가 재현됐다.** 토글 직전 `Shared version` 은 `Latest` 였는데, 공유를 켠 직후 `Version 1 · 크기 프로브 1.0MB · 1h ago` 로 **자동 고정**됐다. routine 4b 의 "공개 공유와 Latest 는 양립하지 않는다"가 맥 앱에서도 그대로다.

**로그인 판정 분기도 함께 확인됐다.** 앱 재시작으로 패널 세션이 만료된 구간이 있었는데, 그때 `get_page_text` 가 `Title: Sign in - Claude` 로 정확히 로그아웃을 잡아냈고, 비공개 아티팩트는 404 로 떨어졌다. routine 4b 첫 동작인 로그인 판정과 그 강등 분기가 맥에서 정상 작동한다.

---

## 2. `check_publish` (E6) — **실패, 그런데 사유가 게이트 쪽이다**

공유가 **켜져 있는데도** 게이트는 실패한다. 종료코드 1.

```
실패: 익명 메타 조회 HTTP 403 — 공유 OFF 이거나 존재하지 않는 아티팩트다.
```

**403 의 실체는 공유 상태가 아니라 Cloudflare 봇 차단이다.** 응답 헤더가 그렇게 말한다:

```
HTTP/2 403
cf-mitigated: challenge
server: cloudflare
cf-ray: ...-ICN
```

본문도 Cloudflare 인터스티셜(`<title>Just a moment...</title>`)이다. User-Agent 를 브라우저로 위장해도 동일하게 403 이다.

### 자기검증 ①이 이 상황을 못 잡는다

이슈 #21 이 심어 둔 대조군 자기검증은 "무작위 uuid 가 200 이면 판정 무효"다. 그런데 Cloudflare 아래서는:

```
무작위uuid http=403
실제uuid   http=403
```

**둘이 똑같이 403 이다.** 대조군이 200 이 아니므로 ①은 "판정 경로 신뢰 가능"으로 통과하고, 게이트는 그대로 ②로 내려가 챌린지를 **공유 OFF 로 오판**한다. 즉 ①은 "판정 경로가 헐거워졌는가"(200 오탐)는 잡지만 **"판정 경로가 막혔는가"(전건 차단)는 못 잡는다.** 오탐의 반대 방향에 구멍이 있다.

### 왜 문제인가

- 게이트가 **판정 불가**와 **공유 OFF** 를 구분하지 못한다. `check_publish` 의 설계 의도("판정 불가는 통과가 아니다")는 네트워크 오류(`code is None`)에만 걸려 있고, HTTP 403 챌린지는 그 그물에 안 걸린다.
- 결과적으로 이 환경에서는 **공유가 정상인 발행도 무조건 E6 실패**다. 무인 아침 루틴은 매일 여기서 멈추고, 실행자는 문면대로 "Share 를 Anyone with the link 로 전환하라"는 이미 한 조치를 반복하게 된다.
- 같은 `fetch_anon`·`FRAME_API_HEADERS` 정본을 쓰는 **`check_formats.py --check-links` 도 같은 맹점을 공유한다** (routine 4b ④). 링크 개통 판정 역시 챌린지를 "공유 OFF 의심"으로 읽는다.

### 이것은 macOS 결함이 아니다

Cloudflare 챌린지는 OS 가 아니라 **출발지 IP·네트워크 평판**에 걸린다. 이 설치 환경은 터널 인터페이스가 8개(MTU 1500·1380·2000·1000)이고 상행이 ~85KB/s 인 회선이다. 원작 Windows 환경에서 E6 가 통과해 온 것과 모순되지 않는다 — 같은 코드가 회선에 따라 갈린다는 뜻이고, 그 자체가 게이트의 취약점이다.

---

## 갱신된 판정

| 항목 | 앞 코멘트 | 이번 |
|---|---|---|
| C-3 발행 경로 | 통과 | 통과 (유지) |
| C-3 전체공유 토글 | 미실측 | **통과** — 브라우저 대행 전 과정 성공, 핀 자동 고정까지 재현 |
| C-3 `check_publish` E6 | 미실측 | **실패 — 게이트 결함 규명** (공유는 ON, 게이트가 Cloudflare 챌린지를 오판) |

---

## 문안 제안 (반영은 원작자 판단, 규정은 손대지 않았다)

1. **`check_publish.py` ② 판정 분기** — HTTP 403 응답에서 `cf-mitigated` 헤더 또는 본문의 Cloudflare 인터스티셜을 감지하면 "공유 OFF"가 아니라 **"판정 불가(봇 차단)"** 로 갈라 별도 종료코드·문면을 낸다. 지금은 실행자에게 이미 한 조치를 반복시킨다.
2. **자기검증 ① 보강** — 대조군이 **실제 uuid 와 동일한 비200 코드**를 돌려주면 "판정 경로가 막혔다"로 판정한다. 현행 ①은 200 오탐만 본다.
3. **`check_formats.py --check-links`** — 같은 정본을 쓰므로 1·2 를 함께 받는다.
4. 판정 불가일 때의 강등 경로 — routine 4b 는 로그인 실패에는 "수동 안내로 강등 + 재로그인 필요 기록"이라는 분기가 있으나, **게이트 판정 불가에는 대응 분기가 없다.** E6 가 판정 불가를 낸 날 무인 루틴이 무엇을 해야 하는지가 비어 있다.

---

## 정리

- **B-1·B-2·C-3 공유 토글**: 맥 앱에서 통과. 브라우저 대행 배포 경로는 온전하다.
- **B-3**: 하네스 차단으로 여전히 실측 불가 — Windows 교차 실측 대상.
- **C-3 E6·C-4 완주**: 이 회선에서는 게이트가 구조적으로 통과할 수 없다. 정상 회선에서 재실측하거나, 위 1·2 를 반영한 뒤 재판정해야 한다.
- **이번에도 macOS 고유 결함은 0건이다.** 드러난 것은 전부 게이트의 환경 가정(회선 평판·업링크)이며, 그 가정이 깨지는 환경이 실재한다는 것이 이번 실측의 값이다.

*프로브 아티팩트(1.0MB, 더미 내용)는 재실측을 위해 공개 상태로 두었다. 정리가 필요하면 Share → Only you 로 되돌리면 된다.*

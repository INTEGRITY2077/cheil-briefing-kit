# 통합 정리 — 앞선 실측 보고의 귀속 정정 + E6 게이트 전역 결함 발견

앞 코멘트(맥 앱 환경 실측)의 결론 일부를 **정정**한다. 추가 실측에서 원인 귀속이 틀렸음이 드러났고, 그 과정에서 macOS 와 무관한 **킷 본체 결함 1건**이 나왔다. #33 한 자리에서 보이도록 여기에 합친다.

---

## 1. 정정 — 이 실측은 OS 변수를 분리하지 못했다

앞 코멘트에서 미완주 항목들을 "회선 문제"로, 일부를 맥 맥락에서 서술했다. 그 귀속이 부정확했다.

Windows 검증은 원작자 기계에서 이뤄졌고, 이번 실측에서는 **OS 만 바뀐 것이 아니라 기계·회선·공인 IP·계정 세션·시점·하네스가 전부 동시에 바뀌었다.** 단일 변수 실험이 아니었으므로 "맥이라서"라는 귀속은 성립하지 않는다.

**실제로 macOS 고유였던 것은 2건뿐이고 둘 다 이미 수리됐다** — `python` 명령 부재(v2026.08.14.6), 3.10 하한(동). 그 외 이번에 관측된 모든 미완주는 OS 무관 변수였다.

| 관측된 증상 | 실제 변수 | macOS 고유? |
|---|---|---|
| `check_publish` 403 | Cloudflare 봇 차단 | **아니오 — 아래 2절** |
| 2.13MB 호 발행 실패 | 상행 약 85KB/s | 아니오 — 이 회선 |
| 세션·MCP 크래시 | 실측자가 돌린 대용량 진단 업로드 | 아니오 — 실측자 과실 |
| 패널 로그인 유실 | 시간 경과·앱 재시작 | 아니오 |
| allowlist 파일 쓰기 차단 | Claude Code 하네스 정책 | 아니오 |

**교훈**: 킷은 여태 한 대의 기계에서만 검증됐다. 그래서 「회선이 빠르다 · 공인 IP 평판이 좋다 · CF 가 봐준다」가 검증된 적 없이 전제로 깔려 있었다. 두 번째 기계에 올라가며 그 전제들이 한꺼번에 드러난 것이고, OS 탓처럼 보였을 뿐이다.

---

## 2. 신규 발견 — E6 게이트가 Cloudflare 에 막힌다 (킷 전역 결함 의심)

### 실측

`check_publish.py` 가 쓰는 익명 요청 경로가 Cloudflare 챌린지를 받는다.

```
GET https://claude.ai/api/frame/<uuid>?via=user_open
  → HTTP/2 403
     server: cloudflare
     cf-mitigated: challenge
     본문: <title>Just a moment...</title>
```

**아티팩트 고유 문제가 아니다. claude.ai 루트도 동일하다:**

```
GET https://claude.ai/            → HTTP/2 403 · cf-mitigated: challenge
GET raw.githubusercontent.com/... → HTTP 200 (대조군 정상)
```

공인 IP 는 **한국 가정용 회선의 일반 대역**이었다 — 데이터센터 IP 도 VPN 출구도 아니다. **IP 평판 문제로 설명되지 않는다.** (실제 주소값은 개인정보라 기록하지 않는다. 재확인이 필요하면 운영자 로컬에서 `curl -s ifconfig.me` 로 본다.)

즉 Cloudflare 는 공유 설정이나 아티팩트를 보고 막은 것이 아니라, **claude.ai 도메인 전반에서 브라우저가 아닌 클라이언트를 막고 있다.** 쿠키 없고 JS 안 도는 요청이 대상이고, `check_publish` 의 `urllib` 요청이 정확히 그 모양이다(`tools/check_formats.py` `fetch_anon` — "urllib 은 쿠키를 보내지 않으므로 그 자체로 로그아웃 독자 시뮬레이션이 된다").

### 더 심각한 것 — 자기검증이 이 실패 모드를 못 잡는다

`check_publish` 는 이슈 #21 에서 대조군 자기검증을 넣었다: 무작위 uuid 가 200 이면 판정 무효.

**CF 챌린지는 진짜 uuid 와 무작위 uuid 에 똑같이 403 을 준다.** 따라서

1. 대조군 검사 → **통과** (403 이므로 200 이 아님)
2. 실제 판정 → 403 → **「공유 OFF 이거나 존재하지 않는 아티팩트」로 판정**

**공유가 정상적으로 켜진 아티팩트도 실패로 찍힌다.** 게이트가 자기 고장을 감지하지 못하고 빨간불을 켠다 — 이슈 #21 이 세운 「판정 경로 자기검증」이 바로 이 실패 모드에 대해 무력하다.

역방향 위험이 더 크다: 이 게이트는 **발행의 완결 조건을 판정하는 최종 관문**(E6)이다. 관문이 항상 실패를 내면 루틴은 매일 아침 발행을 실패로 보고하거나, 실행자가 게이트를 신뢰하지 않고 우회하게 된다. 후자가 되면 이슈 #18·#21 이 막으려던 「게이트 없는 발행」으로 되돌아간다.

### 왜 여태 안 드러났나

CF 의 봇 정책은 언제든 조여진다. 조여진 날 킷은 **아무 경고 없이** 이 게이트를 잃는다. 「Windows 에서 정상작동했다」는 과거형이고, **지금도 되는지는 확인된 바 없다.**

---

## 3. 결정적 실험 — Windows 워커에게 한 줄

이 결함이 킷 전역인지 이 환경 고유인지는 명령 하나로 갈린다.

```bash
curl -s -o /dev/null -D - https://claude.ai/ | head -3
```

- **403 + `cf-mitigated: challenge`** → **킷 전역 결함.** E6 판정 방식을 바꿔야 한다.
- **200** → 이 회선·이 시점 고유로 좁혀진다. 그래도 CF 정책 변화에 노출된 설계라는 사실은 남는다.

같은 판정을 킷 안에서 하려면 `python tools/check_publish.py <아무 아티팩트 URL>` 의 출력이 「익명 메타 조회 HTTP 403」인지 보면 된다.

---

## 4. 전역 결함일 경우의 처방 방향 (문안만, 미반영)

**핵심 관측**: 앱 내장 브라우저(`Claude_Browser`)는 CF 를 통과한다 — 이번 실측에서 같은 아티팩트를 정상적으로 열었고 Share 대화상자까지 조작했다. 막히는 것은 `urllib` 경로뿐이다.

따라서 방향은 「익명 판정을 포기」가 아니라 **「판정 매체를 urllib 에서 브라우저로 옮기되 로그아웃 상태를 유지」** 다:

1. `check_publish` 가 403 + `cf-mitigated` 헤더를 만나면 **「공유 OFF」로 단정하지 않고 「판정 불가(CF 차단)」로 별도 종료코드**를 낸다 — 지금은 두 상황이 같은 실패로 뭉개진다. 이것만으로도 오판은 사라진다.
2. 익명 개통 판정의 정본을 내장 브라우저 경유(로그아웃 컨텍스트)로 옮기는 안을 검토한다. routine 4b 「검증」 절이 이미 내장 브라우저로 로그아웃 검증을 하도록 규정하고 있어 계보가 맞는다.
3. `check_formats --check-links` 도 같은 `fetch_anon` 을 쓰므로 동일 결함을 공유한다 — 함께 봐야 한다.

우선순위는 **#33 보다 높다.** 매일 아침 발행의 최종 관문이 조용히 무력화된 상태이기 때문이다. 별도 이슈로 뗄지, #33 안에서 처리할지는 원작자 판단에 맡긴다.

---

## 5. 현재 상태 요약 (#33 5항목 최종)

| 항목 | 판정 | 비고 |
|---|---|---|
| B-1 사이드패널 브라우저 | **통과** | preview_start·get_page_text·tabs 왕복, 0-3 판정 절차 성립, #12 카드 타이밍 재발 없음 |
| B-2 scheduled-tasks MCP / 세션 크론 | **통과, 둘 다 가용** | 생성·목록·삭제 왕복 확인 |
| B-3 allowlist 실적용 | **실측 불가** | 하네스가 `.claude/settings.json` 쓰기 차단 — 팝업이 아니라 거부. Windows 교차 실측 필요 |
| C-3 발행→공유→check_publish | **부분** | 발행·공유 토글은 앱에서 동작. E6 판정은 **CF 차단으로 판정 불가**(위 2절) |
| C-4 루틴 완주 run_log·check_run | **부분** | 게이트 9종 전수 통과. 호 마감 미도달(발행 페이로드) |

**macOS 고유 결함은 이번에도 0건이다.** 런타임·게이트·앱 어포던스가 전부 동작했다. 남은 미결은 OS 가 아니라 회선·하네스·CF 정책에 걸려 있다.

**이미 반영된 수리** (`d04a4df`, v2026.08.15): `check_size` 가 발행 불가 호를 초록 판정하던 것, `check_run` 이 자정을 넘긴 실행의 원장 짝을 갈라 판정하던 것 — 둘 다 이번 실측에서 실제로 밟은 결함이다.

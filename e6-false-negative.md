# 추가 실측 — E6 게이트(`check_publish`)의 거짓 음성 확증

앞 코멘트에서 C-3을 「발행 경로 통과, 공유 토글·E6는 미실측」으로 남겼으나, 그 뒤 프로브 아티팩트로 끝까지 밀어붙여 **두 가지를 확정**했다. 하나는 통과 판정이고, 하나는 게이트 결함이다.

## 1. 브라우저 대행 공유 토글 — 통과

맥 앱 사이드패널에서 routine 4b 「지정」 절차가 그대로 동작한다.

- 아티팩트 열기 → 공유 버튼 → `Share artifact` 대화상자 → `General access` 콤보박스 → `Anyone with the link` 선택까지 전 구간 브라우저 대행으로 수행됨
- 대화상자에 `Shared version` 콤보박스가 함께 있고 초기값이 `Latest` — 킷이 규정한 「공개 공유와 Latest 는 양립하지 않는다」의 그 지점이다
- 공유를 켠 뒤 상태가 **"shared with anyone with the link (viewers currently see this version, but will not see future publishes until the share pin is moved)"** 로 바뀌었다 — **공유를 켜는 순간 그 버전이 핀으로 잡힌다**는 routine 4b ③의 근거가 맥에서 그대로 재현됐다

즉 B-1(브라우저)·C-3(공유 토글)은 맥 앱에서 판정 완료다.

## 2. `check_publish.py` 가 공유 ON 을 「공유 OFF」로 판정한다 — 거짓 음성

**같은 아티팩트, 같은 시각의 두 판정이 정반대다.**

| 판정자 | 결과 |
|---|---|
| 아티팩트 실제 상태 (소유자 화면·독립 조회) | **shared with anyone with the link** |
| 익명 브라우저 렌더 | 로그아웃 상태에서 본문 정상 표시 (공개 확인) |
| `python tools/check_publish.py <URL>` | **실패: 익명 메타 조회 HTTP 403 — 공유 OFF 이거나 존재하지 않는 아티팩트다** |

게이트가 발행·공유가 완료된 호를 실패로 판정한다. 발행 절차를 정확히 수행해도 E6 에서 막힌다.

### 기전 — Cloudflare 가 판정 요청을 앱에 닿기 전에 자른다

```
$ curl -sI https://claude.ai/
HTTP/2 403
cf-mitigated: challenge
server: cloudflare
```

`check_publish` 는 `check_formats.fetch_anon`(파이썬 urllib)으로 익명 조회를 한다. Cloudflare 는 이 클라이언트를 챌린지로 막고 **403** 을 돌려준다. 앱이 공유 OFF 에 돌려주는 코드는 **401** 인데(`check_formats.py:102-104` 주석의 실측 기록), 게이트는 `code != 200` 을 전부 같은 실패로 뭉쳐 401 과 403 을 구분하지 않는다.

```python
# check_publish.py:88-93
if code != 200:
    print(f"실패: 익명 메타 조회 HTTP {code} — 공유 OFF 이거나 존재하지 않는 아티팩트다. ...")
    sys.exit(1)
```

### 왜 자기검증(①)이 이걸 못 잡나 — 맹점이 구조적이다

이슈 #21 이 넣은 대조군은 **「무작위 uuid 가 200 이면 판정 무효」** 한 방향만 본다.

```
실제 아티팩트  → 403 cf-mitigated: challenge
무작위 uuid    → 403 cf-mitigated: challenge      ← 완전히 동일
```

CF 가 **전부 403** 으로 막으면 대조군은 200 이 아니므로 ①을 통과한다. 게이트는 판정 경로가 건강하다고 믿은 채 확신에 차서 오판한다. 대조군이 잡는 것은 "모두 통과"뿐이고, "모두 차단"은 잡지 못한다.

### 파급 — macOS 문제가 아니다

- 원인은 **클라이언트 지문**(urllib)이지 OS 가 아니다. 같은 CF 정책이면 Windows·Linux 에서도 동일하게 막힌다
- 이 회선의 공인 IP 는 국내 가정용 대역이고, GitHub 등 CF 밖 호스트는 200 으로 정상이다 — IP 평판 문제로 보기 어렵다
- 이슈 #21 은 「간접 휴리스틱(404=통과)을 폐기하고 직접 판정으로 간다」였는데, **그 직접 판정이 미들박스 하나에 무력화**됐다. 게이트가 스스로를 신뢰할 수 있는 조건이 검증되지 않은 채 남아 있었다는 뜻이다
- 킷 전체에 걸린 문제이지, 맥 이식 문제가 아니다

### 제안 (반영은 원작자 판단)

1. **`fetch_anon` 이 응답 헤더의 `cf-mitigated` 를 읽어 「판정 불가」를 별도 상태로 만든다.** 지금은 판정 불가가 「실패(공유 OFF)」로 접혀 들어가 있다 — 킷의 「판정 불가는 통과가 아니다」 원칙의 짝은 「판정 불가는 실패도 아니다」여야 한다
2. **401 과 403 을 가른다.** 401 = 앱이 답한 공유 OFF(진짜 실패), 403 + `cf-mitigated` = 판정 경로 차단(판정 불가)
3. **대조군을 양방향으로.** 「무작위 uuid 가 200」뿐 아니라 **「무작위 uuid 와 실제 uuid 의 응답이 동일」**하면 판정 무효로 본다 — 이번 사례가 정확히 그 형태였다
4. 판정 불가일 때의 강등 경로를 정한다 — 발행을 막을 것인지, 「E6 판정 불가」를 보고에 명기하고 통과시킬 것인지는 정책 결정이라 여기서 정하지 않는다

## 남는 미실측

`check_run` 완주(호 마감)는 여전히 미실측이다 — 실물 호(2.13MB) 발행이 이 회선 업링크(~85KB/s, 1MB 초과 리셋)에 막혀 `output/artifact-url-<날짜>.txt` 기록에 도달하지 못했다. 이것은 킷·OS 와 무관한 환경 제약이다.

## 배경

이슈 #33 의 맥 앱 환경 실측을 수행하다 배포 종단(발행→공유→개통 판정)에서 막혔다.
막힌 지점을 파고들어 보니 원인이 환경 하나가 아니라 **게이트 3종이 공통으로 가진
구조 문제**였다. 실측을 완주하지 못한 사유이자, macOS 와 무관하게 모든 설치본에
걸리는 사안이라 별도로 올린다.

공통 구조는 한 문장이다 — **「판정 불가」와 「판정 결과 실패」와 「통과」가 서로
구분되지 않는다.** 이슈 #21 이 E6 에서 「간접 휴리스틱 폐기」를 세웠는데, 그 원칙의
반대편에 같은 크기의 구멍이 남아 있었다.

---

## 문제 1 — `check_publish` 가 Cloudflare 챌린지를 「공유 OFF」로 판정한다

`check_publish.py` 는 익명 메타 조회(`GET /api/frame/<uuid>?via=user_open`)의 응답
코드로 공유 상태를 가른다. 200+public 이면 통과, 그 외는 실패다.

그런데 이 경로가 **Cloudflare 챌린지를 받으면 403 이 돌아온다.** 게이트는 그것을
「공유 OFF 이거나 존재하지 않는 아티팩트」로 판정하고 E5 처방(오디오 MIME 확인,
MP3 재이식·재발행)을 안내한다. 실제로는 공유 상태를 **알 수 없는** 상태다.

**재현** (2026-08-15, 맥미니):

```
$ python3 tools/check_publish.py 2026-08-14
판정 대상: https://claude.ai/code/artifact/7f293c36-...
실패: 익명 메타 조회 HTTP 403 — 공유 OFF 이거나 존재하지 않는 아티팩트다. ...

$ curl -s "https://claude.ai/api/frame/7f293c36-...?via=user_open" | head -c 120
<!DOCTYPE html><html lang="en-US"><head><title>Just a moment...</title>...
```

응답 본문이 `Just a moment...` — API 의 403 이 아니라 **Cloudflare 봇 차단 페이지**다.
브라우저 User-Agent 를 붙여도 동일하게 챌린지를 받는다(실측). 이 회선의 IP 평판
때문으로 보이나, 원인이 무엇이든 **게이트가 그 사실을 알 수 없다는 것이 문제**다.

### 왜 위험한가

같은 403 이 서로 다른 세 상황에서 나온다:

| 실제 상태 | 게이트 출력 | 옳은가 |
|---|---|---|
| 공유 OFF | 실패 | 맞음 |
| 존재하지 않는 아티팩트 | 실패 | 맞음 |
| **공유 ON 인데 CF 챌린지** | **실패** | **거짓 음성** |
| **공유 OFF 인데 CF 챌린지** | 실패 | 우연히 맞음 |

이번 실측에서 네 번째 경우를 만났다. 공유를 켠 프로브(`3fb0ff20`)에서는 세 번째
경우(거짓 음성)를 만났다. **둘 다 같은 출력이라 구분할 방법이 없다.**

거짓 음성의 실질적 피해는 E5 오진이다 — 게이트가 「MP3 재이식·재발행하라」고
안내하는데, 공유는 이미 켜져 있고 오디오도 정상이다. 실행자가 그 지시를 따르면
멀쩡한 호를 다시 만들고 다시 발행한다.

### 대칭 위험 — 거짓 양성도 가능하다

이 게이트는 대조군 자기검증(무작위 uuid 가 200 이면 판정 무효)을 갖고 있다.
그런데 CF 챌린지 구간에서는 대조군도 403 을 받으므로 **자기검증이 통과한다** —
판정 경로가 죽었는데 살아 있다고 보고한다. #21 이 세운 안전장치가 이 구간에서
작동하지 않는다.

---

## 문제 2 — `check_size` 가 발행 불가 호를 초록으로 통과시킨다

2.13MB 호에 대해:

```
$ python3 tools/check_size.py output/web/2026-08-14.html
호 2.13MB = 오디오(base64) 2.09MB + 지면 0.04MB · 상한 3.00MB
통과: 상한 대비 71%
```

이 호는 **발행에 실패했다**(ECONNRESET 반복). 게이트는 초록이었다.

config 주석에는 이미 정직한 설명이 있다 — `max_html_mb 는 검증된 임계가 아니라
편집 규율이다 — 통과해도 공유가 보장되지 않는다`. 문제는 **그 사실이 게이트 출력에
없다**는 점이다. 실행자는 `통과` 한 줄만 보고 다음 단계로 간다.

D8 은 로스터 필수 게이트라 종료코드 0 이 발행 진행의 근거가 된다. 「통과」가
「발행된다」를 뜻하지 않는다면 그 사실이 출력에 있어야 한다.

---

## 문제 3 — 「호 마감」 정의가 개통을 보장하지 않는다

routine 4b ⑤ 는 호 마감을 이렇게 정의한다:

> 호 마감 = 당일 URL 기록(`output/artifact-url-YYYY-MM-DD.txt`) 완료

이번 실측 종료 시점의 실제 상태:

```
$ cat output/artifact-url-2026-08-14.txt
https://claude.ai/code/artifact/7f293c36-85ea-4f48-bff1-b7bcc1fbd5cb
```

파일이 있으므로 정의상 **호 마감이다.** 그런데 그 아티팩트는 **private** 이고
(소유자 조회로 확인), 독자가 열면 404 다. 게다가 실린 오디오는 구판이라 현재
로컬 산출물과 내용도 다르다.

`check_publish` 가 이것을 잡도록 설계돼 있으나, 문제 1 때문에 이 회선에서는
잡지 못한다. **URL 파일의 존재가 곧 개통을 뜻하지 않는데, 마감 정의는 파일 존재만
본다.**

---

## 부수 — `check_run` 의 자정 넘김

날짜 판정이 `ended_at or started_at` 이라(`load_entries`), 자정을 넘긴 실행은
시작 줄이 전날에, 종료 줄이 당일에 잡혀 **같은 실행의 짝이 두 날짜로 갈린다.**
routine 0-원장 절은 「같은 `started_at` 으로 짝을 맞춘다」고 규정하는데 판정
스크립트는 하루 단위로 자른다.

이번 실측이 21:36 시작 → 다음 날 새벽까지 이어지며 이 조건에 걸렸다. 07:00 생성이
정상 경로라 평시에는 드러나지 않으나, 지연·재시도로 자정을 넘긴 날에는 「안 돈 날」과
「돌았는데 빈손인 날」의 구분이 다시 무너진다 — 이슈 #9 가 막으려던 지점이다.

---

## 공통 구조

세 문제가 같은 형태다.

| 게이트 | 뭉개는 구분 |
|---|---|
| `check_publish` | 판정 불가(CF 차단) ↔ 판정 결과(공유 OFF) |
| `check_size` | 규율 통과 ↔ 발행 가능 |
| 호 마감 정의 | 기록 존재 ↔ 개통 확인 |

게이트가 「모르겠다」를 말할 출구가 없어서 전부 「안다」로 나온다. 판정 불가를
실패로 내보내면 멀쩡한 것을 고치게 되고, 통과로 내보내면 깨진 것을 내보내게 된다.
어느 쪽이든 게이트가 실행자를 잘못된 행동으로 이끈다.

---

## 판단이 필요한 지점 (원작자 몫)

규정 수정은 하지 않았다. 아래는 결정이 필요해 보이는 것들이다.

1. **`check_publish` 가 「판정 불가」를 별도 종료코드로 낼 것인가** — CF 챌린지·네트워크
   오류처럼 상태를 알 수 없는 경우. 현재는 실패(1)와 통과(0) 둘뿐이다.
   판정 불가를 통과로 취급하면 #21 이 폐기한 간접 휴리스틱으로 돌아가므로,
   실패로 두되 **사유를 구분해 출력**하는 쪽이 안전해 보인다.
2. **CF 챌린지 감지** — 응답 본문에 `Just a moment...` / `cf-mitigated` 가 있으면
   그것을 알아보고 다른 메시지를 낼 것인가.
3. **`check_size` 출력에 「통과가 발행을 보장하지 않는다」를 넣을 것인가** —
   config 주석에 이미 있는 문장이다.
4. **호 마감 정의에 개통 판정을 포함할 것인가** — URL 기록 + `check_publish` 통과.
   다만 1·2 가 정리되기 전에는 이 조건이 이 회선에서 영구히 미충족이다.
5. **`check_run` 의 날짜 판정을 `started_at` 기준으로 통일할 것인가.**

## 참고

- #33 (macOS 실측) — 이 문제들을 만난 경위. 그 이슈는 macOS 지원 확정으로 종결됐고,
  여기 있는 것은 OS 무관 사안이라 분리했다.
- #21 (E6 간접 휴리스틱 폐기) — 문제 1 은 그 원칙의 반대편 구멍이다.
- #9 (무증거 실행) — 부수 항목이 그 지점을 다시 연다.
- #18 (게이트 미실행 발행) — 문제 3 과 같은 계열이다.

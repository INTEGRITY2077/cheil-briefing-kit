# macOS **앱 환경** 실측 결과 — 이슈 #33 미실측 5항목

- **실측일**: 2026-08-14 ~ 08-15 (자정 넘김)
- **킷 버전**: v2026.08.14.6 (`check_version.py` 종료코드 0 — 원격 일치)
- **환경**: macOS Darwin 25.3.0 · arm64 (Mac mini) · Claude Code 데스크톱 앱 세션
- **선행 실측**: 2026-08-14 CLI 실측 코멘트(A·C 층 완료)의 후속. 이번은 **GUI 대기로 남겼던 5항목**.
- **규정 미변경 원칙**: 유지했다. `tools/`·`templates/`·SETUP·routine-SKILL 미수정.
  진단 목적으로 `config.yaml`의 `tts.embed.bitrate`를 일시 조정했다가 원값(32k)으로 되돌렸다.

---

## 요약

| 항목 | 판정 |
|---|---|
| B-1 사이드패널 브라우저 | **부분 통과** — 도구 전종 동작, 단 세션 지속성 실패 |
| B-2 scheduled-tasks MCP / 세션 크론 | **통과** |
| B-3 allowlist 실적용 | **실패(차단)** — 권한 팝업이 아니라 classifier 거부 |
| C-3 발행→전체공유→check_publish | **실패(발행 단계)** — 페이로드 임계로 차단, 공유·E6 미도달 |
| C-4 루틴 완주 run_log·check_run | **실패** — 자정 넘긴 실행에서 원장 짝이 두 날짜로 쪼개짐 |

**핵심 결론**: 맥 고유(arm64·OS API) 결함은 이번에도 0건이다. 대신 **앱 환경에서만 드러나는 결함 3건**이 나왔다 — ① 아티팩트 발행 페이로드 임계(약 1MB)가 킷의 크기 게이트(3MB)보다 훨씬 낮다, ② `.claude/settings.json` 생성이 auto mode classifier에 막혀 SETUP §5가 무인으로 완주되지 않는다, ③ `check_run`이 자정을 넘긴 실행을 판정하지 못한다. 셋 다 OS 문제가 아니라 **킷의 전제와 앱 동작의 어긋남**이다.

---

## B-2. scheduled-tasks MCP / 세션 크론 — 통과

Mac 앱 세션에 **둘 다 잡힌다.** 왕복 실측했다(생성 → 목록 확인 → 삭제, 잔여물 없음).

| 경로 | 도구 | 결과 |
|---|---|---|
| 예비(새 세션) | `mcp__scheduled-tasks__{create,list,update,delete}_scheduled_task` | 생성·조회·삭제 전부 성공 |
| 1차(현존 세션) | `CronCreate` / `CronList` / `CronDelete` | 생성·조회·삭제 전부 성공 |

- 스케줄드 태스크는 `~/.claude/scheduled-tasks/<taskId>/SKILL.md` 로 디스크에 저장되고, `fireAt`(1회성)·`cronExpression`(반복) 둘 다 로컬 시간으로 해석된다. 생성 응답이 "앱이 열려 있을 때 실행되고, 닫혀 있었으면 다음 실행 때 밀린 것이 돈다"를 명시한다 — SETUP 4-2b ③ 문안과 일치한다.
- 세션 크론은 **디스크에 쓰이지 않는다**(in-memory, 세션 종료 시 소멸). 7일 자동 만료도 응답에 명시된다 — SETUP 4-1 고지와 일치.
- **삭제 시 잔여물**: `delete_scheduled_task` 는 SKILL.md 파일을 남긴다("프롬프트 복구용"). 스모크 태스크를 지울 때 디렉토리를 별도로 지워야 했다. 설치 절차가 태스크를 재등록할 때 같은 taskId 를 쓰면 옛 프롬프트가 남아 있을 수 있다.

**SETUP 영향**: 없음. 4절(1차 세션 크론 / 예비 스케줄드 태스크) 이중 등록 구조가 맥 앱에서 그대로 성립한다.

---

## B-1. 사이드패널 브라우저 — 부분 통과 (도구 동작 O, 세션 지속 X)

### 동작한 것

`Claude_Browser`(앱 내장 사이드패널)가 존재하고 0-3 판정 경로가 전부 동작한다.

| 호출 | 결과 |
|---|---|
| `preview_start {url:"https://claude.ai"}` | 패널 열림 (`navOk: true`, tabId `seed`) |
| `get_page_text` | DOM 텍스트 반환 — ⓐ 로그인 판정 성립 |
| `tabs_create` / `tabs_close` | 성공 (SETUP 0-3 이 경고한 이슈 #8 재현 안 됨 — preview_start 뒤에는 정상) |
| `read_page` | 접근성 트리 반환, `ref_N` 부여 |
| `navigate` | 성공 |

**0-3 로그인 카드 타이밍(#6·#8·#12 계보)은 맥에서 재발하지 않았다.** `preview_start` 한 번으로 카드 한 장이 떴고, 중복 카드도 없었다. 판정 순서(조용한 경로 → 카드 문안 → preview_start)가 설계대로 성립한다.

로그인 판정도 양방향으로 확인됐다 — 로그아웃 상태에서 `Title: Sign in - Claude`, 사용자가 로그인한 뒤 `Title: New chat - Claude` 로 정확히 갈렸다.

### 실패한 것 — 세션이 앱 재시작을 넘기지 못한다

세션 도중 MCP 서버 전체가 끊겼다 재연결됐고(`claude-in-chrome`·`scheduled-tasks` 등 50개 도구 동시), **그 뒤 내장 브라우저는 로그아웃 상태로 돌아갔다.** 같은 세션 안에서 로그인 → (재연결) → 로그아웃이 관측됐다.

SETUP 0-3 은 이렇게 고지하도록 돼 있다:

> "로그인은 이번 한 번이면 됩니다 — 내장 브라우저에 **세션이 유지되므로**, 앞으로 매일의 공유 켜기·버전 고정·검증은 Claude가 이 안에서 자동으로 합니다."

이 전제가 이 환경에서는 성립하지 않았다. 루틴 4b 의 첫 동작이 로그인 판정이고 세션이 풀려 있으면 "수동 안내로 강등"이므로 **루틴은 안전하게 실패하지만**, "한 번이면 된다"는 고지는 사용자에게 지키지 못한 약속이 된다.

---

## B-3. allowlist 실적용 — 실패 (classifier 차단)

SETUP §5 의 허용 목록을 문서 그대로 `~/briefing-kit/.claude/settings.json` 에 쓰려 했다. 결과는 **권한 요청 팝업이 아니라 도구 호출 자체의 거부**였다:

```
Permission for this action was denied by the Claude Code auto mode classifier.
Reason: Blocked by classifier.
```

- 사용자에게 승인/거부를 묻는 카드가 뜨지 않았다 — 물어볼 기회 없이 차단됐다.
- 재시도·우회를 하지 않았다(차단 의도를 우회하지 말라는 지시가 응답에 함께 있다).
- 따라서 **allowlist 가 실제로 적용되는지(무인 아침 실행에서 프롬프트가 뜨지 않는지)는 이번에도 미실측이다.** 파일을 만들지 못했으므로 그 다음 판정으로 갈 수 없다.

**SETUP 영향이 크다.** §5 는 카드 ② 승인 이후 무인 구간에 있고, 0-0 은 "카드 이후 7절까지 무인"을 약속한다. 그런데 이 환경에서 §5 는 **에이전트가 수행할 수 없다** — 설치가 그 지점에서 멈추거나, 사용자가 직접 파일을 만들어야 한다. 후자라면 7절 '남은 일' 칸이 비지 않으므로 규정상 **"설치 미완결"** 보고가 된다.

부수 관찰: `PowerShell(Invoke-WebRequest:*)` 항목은 맥에서 무해하게 무시된다(§5 문면대로). 이번 실측에서 4b 검증 경로는 `Bash(curl:*)`·python urllib 로 대체 가능함을 확인했다 — DART RSS·목록 조회를 curl 로 수행했고 막히지 않았다. 다만 **`Bash(curl:*)` 는 현행 허용 목록에 없다** — 무인 실행에서 이 경로를 쓰려면 목록에 있어야 한다.

---

## C-3. 발행 → 전체공유 → check_publish — 실패 (발행 단계에서 차단)

### 측정값: 아티팩트 발행에 페이로드 임계가 있다

오늘 호(2.13MB — 오디오 base64 2.09MB + 지면 0.04MB)를 발행하려 하자 **3회 연속 `read ECONNRESET`** 으로 끊겼다. 크기 요인인지 격리하기 위해 합성 프로브로 사다리를 쟀다:

| 페이로드 | 결과 |
|---|---|
| 0.3KB | **성공** |
| 1.00MB | **성공** |
| 1.25MB | 실패 (ECONNRESET) |
| 1.50MB | 실패 (ECONNRESET) |
| 2.13MB (실제 호) | 실패 ×3 (ECONNRESET) |

**임계는 1.00MB 와 1.25MB 사이다.** 발행 경로 자체는 정상이다(소형 페이지 즉시 발행).

### 이것이 게이트와 충돌한다

`config.yaml` 의 `tts.embed.max_html_mb: 3.0` 과 `check_size.py`(D8)는 2.13MB 호를 **"통과: 상한 대비 71%"** 로 초록 판정했다. 즉 **게이트가 발행 불가능한 호를 통과시킨다.** D8 주석은 이미 "검증된 임계가 아니라 편집 규율"이라고 스스로 밝히고 있는데, 이번 실측이 그 경고를 수치로 확인한 셈이다.

비트레이트 인하로 맞추려 했으나 막혔다 — 대본이 6분대라 MP3 44.1kHz 하한인 32kbps 에서도 1.57MB(base64 2.09MB)이고, 16k 를 지정해도 libmp3lame 이 32k 로 올려 인코딩한다(MPEG-1 Layer III 포맷 제약, 도구 결함 아님). **현행 판형(6분 낭독 + WAV 원본 44.1kHz)으로는 1MB 아래로 못 내려간다.** 내리려면 대본을 줄이거나 합성 표본율을 낮춰야 하는데 둘 다 규정 변경이라 하지 않았다.

### 공유·E6 는 대조군까지만 측정했다

발행이 막혀 오늘 호로는 갈 수 없었으므로, 이미 발행된 프로브 아티팩트로 **음성 대조군**만 쟀다:

```
$ python3 tools/check_publish.py https://claude.ai/code/artifact/4a2eab67-...
실패: 익명 메타 조회 HTTP 403 — 공유 OFF 이거나 존재하지 않는 아티팩트다. ...
exit=1
```

- **① 판정 경로 자기검증 통과** — 무작위 uuid 대조군이 200 을 받지 않았다(이슈 #21 수리가 맥에서도 유효).
- **② 공유 OFF 를 403 으로 정확히 잡았다** — 비공개 아티팩트를 통과시키지 않는다.
- **③ 양성 판정(공유 ON → 200 + public)은 미실측** — 공유 토글에 claude.ai 로그인이 필요한데 재연결 후 세션이 풀렸고(B-1), 로그인은 대신 할 수 없다.

---

## C-4. 루틴 완주 run_log·check_run — 실패 (자정 넘김에서 짝이 깨진다)

루틴을 실제로 완주했다 — 시작 줄 append → 버전 판정 → 단일 생산자 확인 → 수집(Workflow 병렬) → 원장 등재(F-056~F-065 10건) → 산출물 3종 → 기계 게이트 → 종료 줄 append.

**기계 게이트 9종은 전부 종료코드 0 이다** (로스터 10종 중 check_publish 제외):

```
check_tables:0  check_exhibits:0  check_css_vars:0  check_ledger:0  check_size:0
check_insight:0 check_formats:0   check_theme:0     check_review:0
```

- `check_ledger` 가 "참조 67건 전부 실존, 사실 67건(대체 7) 상태 정합, 인덱스 신선, 큰축 등재 완결·**점수판 3중 장부 정합**" 까지 판정했다 — 신규 사실 10건 등재 + 미러 + index 재생성 + 축 점수판 갱신이 한 동작으로 성립했다.
- `check_review` 는 재심사 평균 6.50 으로 임계 7.0 미달이나, ⑤ 정책대로 **경고 후 통과(0)** 하고 "재심사 미달 발행을 보고에 명기하라"를 냈다 — 설계대로 동작한다.
- `check_publish` 는 발행 실패로 1.

### check_run 이 자정을 넘긴 실행을 판정하지 못한다

실행이 2026-08-14 21:36 에 시작해 08-15 09:35 에 끝났다(중간에 앱 재시작이 있었다). 원장 두 줄은 규약대로 같은 `started_at` 으로 짝지어 기록했다. 그런데 `check_run` 은 항목의 날짜를 `ended_at or started_at` 으로 가른다(`check_run.py:116`):

```python
stamp = str(obj.get("ended_at") or obj.get("started_at") or "")
if stamp.startswith(target):
```

결과로 **한 실행의 시작 줄과 종료 줄이 서로 다른 날짜로 갈라진다**:

| 판정 | 보이는 것 | 판정 결과 | 실제 |
|---|---|---|---|
| `--date 2026-08-14` | 시작 줄만 | "게이트 기록 없는 발행"(이슈 #18 문안) | 게이트 기록은 있다 — 08-15 로 갔을 뿐 |
| `--date 2026-08-15` | 종료 줄만 | "산출물 없음 + 기록된 실패" | 산출물은 있다 — 08-14 파일로 |

둘 다 실패로 뜨는데 **사유가 실제와 다르다.** 특히 08-14 판정은 게이트를 다 돌린 실행을 "게이트를 건너뛴 발행"으로 지목한다 — 이슈 #18 이 막으려던 것과 정반대 방향의 오진이다.

루틴 SKILL 0-원장 절은 "같은 `started_at` 으로 짝을 맞추고"라고 규정한다. 짝의 정본이 `started_at` 이면 날짜 판정도 같은 기준이어야 하는데, `check_run` 만 `ended_at` 을 우선한다. 생성 07:00 / 배포 07:45 의 정상 운용에서는 드러나지 않지만, 배포가 밀리거나 야간 실행이면 재현된다.

---

## 부수 발견

1. **DART RSS 창이 좁다.** `todayRSS.xml` 50건이 실측 시점에 **당일 09:26~11:33(UTC) 두 시간 분량**만 담고 있었다. profiles 의 `backup`(dsab007 목록)이 "실적 발표일처럼 공시가 몰리는 날"의 예비로 적혀 있으나, 실측일은 반기보고서 법정기한이라 상시 밀림에 가까웠다. 목록 검색으로 보완해 반기보고서 접수(F-056)를 잡았다 — RSS 단독이면 **오늘의 화두를 통째로 놓쳤다.**
2. **Workflow(다이나믹 워크플로우) 도구가 맥 앱 세션에 있다** — 채널별 수집 에이전트 병렬 실행이 성립했다. `config.execution.workflow_available: true` 로 기록했다.
3. **세션 안정성** — 긴 세션에서 MCP 서버 전체 끊김/재연결과 백그라운드 에이전트 소실이 발생했다. 킷 도구(파이썬)는 그동안 한 번도 실패하지 않았다. 킷 원인 아님으로 본다.

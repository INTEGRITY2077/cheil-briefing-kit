---
name: cheil-daily-briefing
description: 제일기획 일일 브리핑 — 07:00 생성 시작, 07:45 배포
---

제일기획 일일 브리핑을 생성한다.

# 실행 주체 (2026-08-12 확정 — 현존 세션 우선)
루틴의 **1차 실행자는 컨텍스트를 쥔 현존 세션의 세션 크론**(06:57)이다 — 새 세션은
권한·컨텍스트·중복 문제를 반복해서 일으켰다 (08.12 실측: 권한 정지, 이중 생산).
이 스케줄드 태스크(07:07, 새 세션)는 **예비**다: 현존 세션이 꺼져 있거나 크론이
만료(세션 크론은 7일 자동 만료 — 매주 재등록 필요)된 날만 실제로 생산하게 되며,
그 판별은 0-a 단일 생산자 규칙이 한다 (오늘자 산출물이 있으면 검증만 하고 종료).

# 시간표 — 생성과 배포를 나눈다

- **07:00 실행 시작** (스케줄러 지연으로 실제로는 07:0X에 뜬다) — 0~6절: 수집, 원장, EVAL, 산출물, 게이트, 음성 준비
- **07:45 배포** — 4b절(공유 버전 재지정 + 로그아웃 검증)은 **07:45 이전에 수행하지 않는다**

이유: 생성 도중의 중간 버전이 공유 링크에 걸리면 독자가 미완성본을 본다.
07:45 전에 모든 산출이 끝났으면 남은 시간을 재서 대기한 뒤 배포한다
(`python` 으로 07:45까지 남은 초를 계산해 sleep).
07:45가 지났는데도 게이트가 안 끝났으면 배포를 미루고 그 사실을 보고에 남긴다 —
미완성본을 정시에 내보내지 않는다. 늦는 것이 틀리는 것보다 낫다.

# 킷 위치
E:\제일기획\리서치\briefing-kit

# 파일 명명 규약 (2026-08-12 확정 — 한 호 = 한 파일)
산출물은 날짜가 곧 이름이다. 수식어(특별판·확정판·N장·gemini 등) 금지:
- 웹판: `output/web/YYYY-MM-DD.html`  · 대본: `output/script/YYYY-MM-DD.md`
- 오디오: `output/audio/YYYY-MM-DD.wav` · 슬라이드: `output/ppt/YYYY-MM-DD.pptx`
- URL 기록: `output/artifact-url-YYYY-MM-DD.txt` (웹판·슬라이드 두 줄)
버전은 파일 사본이 아니라 **그 파일을 lint(체크리스트)·eval 통과 후 제자리 갱신**으로
관리한다. 사본·별칭·아카이브 접두어 파일을 만들지 않는다 — 발행된 아티팩트가
버전 이력을 이미 보존한다. 어제 파일은 손대지 않는 것으로 아카이브가 된다.

`config.yaml`을 먼저 읽는다. 경로·검토자·TTS·보존 정책이 거기 있다.
아래 지시에 절대경로를 쓰지 않는다. config의 상대경로를 킷 루트에 붙여 쓴다.

# 0-a. 단일 생산자 규칙 — 시작하자마자 확인한다 (2026-08-12 이중 생산 사고에서)
`output/web/오늘날짜.html` 또는 `output/artifact-url-오늘날짜.txt`가 이미 존재하면
**오늘자는 이미 다른 세션(수동 작업)에서 발행된 것이다. 생성하지 않는다.**
이 경우 할 일은 검증뿐이다: 발행된 아티팩트가 열리는지, 공유 버전이 최신인지 확인하고
그 결과만 보고한 뒤 종료한다. 같은 날짜의 호를 두 번 만들지 않는다.

# 0. 준비 — 순서대로 읽는다
1. `config.yaml`
2. `profiles/<profile>.yaml`
3. `../ledger/broadcast_log.jsonl` — 여기 있는 개념은 다시 넣지 않는다
4. `okf/index.md`
5. `okf/agenda/` 전체
6. `okf/facts/` 중 `status: stable` 인 것

`okf/archive/` 는 읽지 않는다.

# 1. 소스 확인 — 키 없이, 브라우저 패널 없이 돈다

**DART는 RSS로 본다.** `https://dart.fss.or.kr/api/todayRSS.xml`
- XML 50건. `creator` 가 회사명이므로 대상 회사로 거른다
- `pubDate` 가 프로파일의 `last_checked` 이후인 것만 본다
- 공시가 몰리는 날은 50건에서 밀릴 수 있다. 그럴 정황이면 `dsab007/main.do` 목록으로 보완한다
- **보고서 원문 뷰어(`dsaf001/main.do`, `report/viewer.do`)는 열지 않는다.** robots.txt Disallow 대상이다.
  원문이 필요하면 `ir-results` 로 대체한다

나머지 소스 수집은 **다이나믹 워크플로우로 병렬 실행한다** (2026-08-12 실측 —
17개 에이전트 스윕이 순차 수집보다 넓고 깊었다):
- Sweep 단계: 채널별 에이전트 병렬 — 브랜드브리프 / 매드타임스 / DART / 경쟁사 IR 원문
  / 한경 컨센서스 / 글로벌 지주 4사 / 종합 검색(컨퍼런스·계열 물량·주가). 각 에이전트는
  structured output(제목·사실·날짜·출처·등급·층/축)으로 반환하고, 이미 원장에 있는
  사실은 제외한다
- Verify 단계: 채택 후보 각각을 팩트체커 에이전트가 출처 URL 직접 대조로 검증
  (날짜·숫자·주체 불일치 또는 신규성 미달이면 confirmed=false). 검증 통과분만 원장에 등재
- 워크플로우가 실패하면 WebFetch 순차 수집으로 강등하고 그 사실을 보고에 남긴다
브라우저가 꼭 필요한 소스는 새 탭으로 열고, 실패하면 실패로 기록한다.

확인 후 프로파일의 `last_checked` 와 `last_item` 을 갱신한다.

# 2. 원장 갱신
새 사실을 OKF 개념으로 만든다. 형식은 기존 `okf/facts/*.md` 를 따른다.
- `generated: { by: claude/opus-5, at: <ISO8601> }`
- `verified` 는 비운다
- `status: stable`, `stale_after` 는 티어별 기준일
- `grade` 는 1차자료 / 증권사 / 보도 / 관찰 중 하나

SUPERSEDE 는 프로파일의 `supersede.grade_order` 와 `known_chains` 를 따른다.
등급이 높은 자료가 오면 기존 개념을 `deprecated` 로 바꾸고 양방향 링크를 건다.
부분 기간과 누적 기간은 서로 대체하지 않는다.

새 일정은 `okf/agenda/` 에 개념으로 만든다.

# 3. EVAL
`templates/eval-rubric.md` 로 계산해 `eval/YYYY-MM-DD.md` 에 기록한다.
- 8점 이상 → 심층. 대본 4블록 + 아티팩트 갱신
- 3~7점 → 표준. 3블록
- 3점 미만 → 간략

뉴스 선별 우선순위 (2026-08-12 확정 — 주어는 제일기획이다):
1. 제일기획 자체 — 공시·IR·수주·인사·주가
2. 계열 물량 신호 — 삼성전자 제품 출시·판매·마케팅 집행
3. 업계·경쟁사 — 묶어서 1~2건, **헤드라인 불가**.
   경쟁사 실적이 아무리 커도 제일기획 뉴스의 참조물이지 주인공이 아니다
   (2026-08-12 실측: 이노션 IR을 헤드로 올렸다가 반려).

경쟁 지형은 3층으로 본다 (2026-08-12 확정 — 이노션 단일 비교 금지):
- **글로벌 지주**: WPP · 퍼블리시스 · 옴니콤(+IPG) · 덴츠 · 하바스 — 규모·수익모델·AI 전환의 기준선
- **침입자**: 액센추어 송·딜로이트 디지털 등 컨설팅 계열, 광고주 인하우스(버거킹 사례),
  플랫폼 자동화(오픈AI 광고·퍼브매틱 AgenticOS류) — 대행 수요 자체를 잠식하는 축
- **국내**: 이노션 · 대홍 · HSAD · TBWA코리아 · 독립 크리에이티브 — 이노션은 '같은 계열
  광고사 모델'이라 1:1 비교가 유효할 뿐, 유일한 경쟁자가 아니다. 한 회사만 반복 인용하면
  지형이 왜곡된다. 비교를 쓸 때는 어느 층의 비교인지 명시한다.

여기에 **내수/해외 2축을 교차**한다 (2026-08-12 확정): 제일기획은 이익 대부분이 해외에서
나오는 회사다 (1Q 기준 국내 삼성전자 거래 −12%, 북미 +32%, 해외 비계열 비중 32%).
- **내수 축**: 국내 광고 시장 경쟁(이노션·대홍·HSAD·독립·인하우스) + 국내 계열 물량
- **해외 축**: 글로벌 지주·현지 네트워크와의 경쟁 + 해외 계열/비계열 물량 (북미·유럽·중국·동남아)
뉴스와 비교는 어느 축의 이야기인지 명시한다 — 내수 경쟁사 실적으로 해외 사업을,
해외 지주 담론으로 내수 시장을 설명하지 않는다.

# 4. 산출물 생성
대본은 `templates/script-radio.md` 를 따라 `output/script/YYYY-MM-DD.md` 로 쓴다.
심층이면 아티팩트도 갱신한다.

두괄식을 지킨다.
- 문서 최상단 「핵심」 블록을 먼저 갱신한다. 여섯 줄만 읽으면 결론이 끝나야 한다
- 각 섹션은 h2 바로 아래 결론 한 줄(`class="lede"`)로 시작한다. 표부터 내밀지 않는다

반복 억제를 지킨다.
- 사실은 1회. supersede 로 되살아난 것은 정정으로 다룬다
- 일정은 신규 등재일 / D-7 / 당일, 최대 3회
- 심층에서 배경을 다시 설명하지 않는다
- 신규가 없으면 `quiet_day` 항목을 쓴다. 반복으로 분량을 채우지 않는다

SOT 참조는 개념 ID 로 건다.

# 4b. 배포 — 공유 버전 재지정과 검증 (07:45 이후에만)

**아티팩트를 갱신한 날에만 수행한다. 갱신하지 않았으면 건너뛴다.**
**07:45 이전이면 수행하지 않는다. 시간표 절 참조.**

배포 묶음에는 오디오 원본 파일(WAV/MP3)을 반드시 첨부한다 — 아티팩트 뷰어는
iframe sandbox에 allow-downloads가 없어 ⋮ 메뉴의 다운로드 항목 자체가 사라진다
(2026-08-11 실측). 링크만 보내면 청취자는 저장할 방법이 없다.

호 마감 규칙 (2026-08-11 실측 실패에서): 한 호를 닫으면 **산출물 인계 보고
(아티팩트 링크·PPTX·아카이브 경로)를 먼저 완료**하고, 다음 호 작업은 그 다음에
시작한다. 마감과 다음 호 생성을 한 흐름에 섞지 않는다 — 받는 쪽에는 조용한
실패와 구분되지 않는다.

배포 URL 규칙 (2026-08-12 확정 — 날짜별 독립 출판): **호마다 새 아티팩트를 발행한다.**
전날 호의 URL은 건드리지 않는다 — 그 자체가 영구 아카이브다. 어제 URL에 오늘 호를
덮어쓰는 것은 금지 (독자가 받은 링크의 내용이 바뀌면 판형 자체가 무너진다).
발행 절차: ① 새 파일로 Artifact 발행(새 URL) ② **발행 직후 아티팩트 페이지에서
Share → Anyone with the link 를 켠다** — 새 아티팩트는 비공개 기본값이라 이 단계를
빠뜨리면 독자에게 404가 뜬다 (2026-08-12 실측) ③ 공유 버전이 최신인지 확인
④ URL을 `output/artifact-url-YYYY-MM-DD.txt`에 기록하고 배포 메시지에 당일 링크를
공지한다. 호 마감 = `리서치/archive-YYYY-MM-DD-웹판-확정.html` 저장.
발행 이력: 08.11 = f2e8ea45-cd53-4ab2-a16f-a61fab5b13b0 · 08.12 = d89f9d6f-3065-4304-8a1c-638d658bd3c0

기준 시점 규칙 (2026-08-12 실측 실패에서): 리드와 「세 개의 숫자」는 **가장 최신
실적 자료 기준**으로 정박한다 — 공시(DART) 확정치와 IR 잠정치가 시점이 다르면
최신 쪽을 앞세우되 등급을 명시한다 ("2Q IR 잠정, 공시 확정은 반기보고서").
그래프에는 넣은 최신 분기를 리드에서 빼먹으면 문서 앞뒤가 어긋난다.

라디오 이식은 `python tools/embed_radio.py <웹판.html> <오디오> <대본.md>` 로
한다 (오디오 data URI 교체 + 접힘 대본 재구성, 검증 실패 시 원본 보존).

2구역 구조 (2026-08-12 확정): 웹판은 「오늘의 뉴스」와 「자료실」(.zonebar 경계)로
나뉜다. 매일 재작성하는 것은 뉴스 구역뿐이다 — 리드, 오늘 새로 확인된 것, 세 개의
숫자, 라디오. 자료실(원문 표·관계 구조·SOT)은 새 사실이 판정될 때만 수정한다.
어제 것을 재편찬해 오늘 것처럼 내보내지 않는다 — 뉴스 구역이 비면 quiet_day로
비었다고 쓰는 것이 재탕보다 낫다.

공개 공유와 `Latest` 는 양립하지 않는다. 시스템이 막는다
("Can't switch to Latest while shared publicly"). 공유 링크는 지정된 버전에 고정되므로,
갱신 후 새 버전으로 다시 지정하지 않으면 링크를 여는 사람은 옛 내용을 계속 본다.

브라우저 선택 (2026-08-12 확정): **앱 내장 브라우저(Claude_Browser)를 우선 사용한다** —
사용자의 크롬을 점유하지 않는다. 내장 브라우저에 claude.ai 로그인이 돼 있으면 공유 핀
지정·디자인 조작까지 전부 내장 브라우저에서 한다. 로그인이 없으면(검증만 가능) 그때만
claude-in-chrome 으로 폴백하고, 폴백 사실을 보고에 남긴다.

**지정**
1. `list_connected_browsers` 로 연결 확인
2. `output/artifact-url.txt` 의 URL 로 navigate
3. Share 버튼 클릭 → `Shared version` 콤보박스 클릭
4. **드롭다운을 screenshot 으로 펼쳐 목록 전체를 읽는다.**
   `find` 가 돌려준 항목 하나로 판단하지 않는다. 발행이 여러 번 있었으면 버전이 여럿 쌓인다
5. **번호가 가장 큰 버전**을 고른다. 시간 표기("6m ago")로 고르지 않는다.
   같은 시각에 여러 버전이 찍힐 수 있다
6. `General access` 는 건드리지 않는다

**검증 — 반드시 한다**
소유자 화면은 언제나 최신을 보여주므로 그것으로 확인했다고 하지 않는다.
로그아웃 상태에서 공유 링크가 실제로 주는 내용을 확인한다.

1. 이 앱 내장 브라우저(`mcp__Claude_Browser__navigate`)로 아티팩트 URL 을 연다. 이 브라우저는 로그아웃 상태다
2. `javascript_exec` 으로 iframe 의 `src` 를 읽는다 (`*.frame.claudeusercontent.com/_f/...`)
3. 그 URL 을 `Invoke-WebRequest` 로 직접 받아 이번에 넣은 내용이 들어 있는지 문자열로 확인한다
   (예: `class="keypoints"`, 새로 추가한 문장, 고친 수치)
4. 없으면 4b 를 다시 수행한다

크롬이 연결돼 있지 않거나 로그인이 없으면 **조작하지 말고 보고에 남긴다.**
"공유 버전이 옛 버전에 고정돼 있음 — 수동 조작 필요" 라고 적는다.
검증을 하지 못했으면 "검증 미실행" 이라고 적는다. 확인한 척하지 않는다.

# 5. 게이트 — 산출물 전부에 건다

**아티팩트와 대본 둘 다 대상이다. 하나라도 빠뜨리지 않는다.**

| 산출물 | style-guard genre | tone-guard |
|---|---|---|
| 아티팩트 | report | 적용 |
| 대본 | broadcast | 적용 |
| 원장(OKF) | 적용 안 함 | 적용 안 함 |

`templates/tone-guard.yaml` 과 `templates/style-guard.yaml` 을 읽고
`templates/eval-rubric.md` 의 게이트 G1~G8 을 각 산출물에 통과시킨다.

- 수치는 완화하거나 반올림하지 않는다
- 1차자료 근거가 없는 문장에 단정 어미를 쓰지 않는다
- 사내 부서·팀·개인을 지목하지 않는다
- 클로징은 "앞으로 확인할 지표"까지만

**HTML 을 고쳤으면 렌더를 확인한다.** CSS 를 새로 넣었으면 특히 그렇다.
`li` 나 문단에 `display:grid` 를 걸면 그 안의 `<b>` 같은 인라인 요소까지 그리드 아이템이 되어
글자가 세로로 쪼개진다. 실제로 겪은 사고다. 레이아웃을 바꿨으면 screenshot 으로 눈으로 본다.

**게이트를 돌린 결과를 `eval/YYYY-MM-DD.md` 에 산출물별로 기록한다.**
통과 / 수정 후 통과 / 미통과 중 하나로 적고, 수정한 항목을 나열한다.
게이트를 돌리지 않았으면 "미실행" 이라고 적는다. 돌린 척하지 않는다.

**아티팩트를 갱신했는데 게이트 기록이 없으면 그 실행은 실패로 본다.**

**발행 전 체크리스트가 최종 관문이다** (2026-08-12 확정):
`templates/publish-checklist.md`의 A~E 전 항목을 발행 직전에 돌리고 `eval/YYYY-MM-DD.md`에
항목 번호별 통과/실패를 기록한다. 하나라도 실패면 발행하지 않는다. 이 체크리스트는
2026-08-11~12 사용자 반려 전건을 항목화한 것이다 — 같은 지적이 다시 나오면
규칙 부재가 아니라 게이트 미실행이다.

# 4c. PPT — 클로드 디자인 (선택, 심층인 날만)

클로드 디자인(claude.ai 좌측 하단 「디자인」)의 Slides 템플릿을 쓴다. Max 구독에 포함, 별도 키 없음.
`output/ppt/YYYY-MM-DD-개요.md` 를 만들고(전 절 커버리지 — 압축본 금지, 2026-08-11 실측: 압축 개요를 넣으면 커버리지 부족),
프롬프트에 반드시 넣는 절대 규칙: 문안·숫자 한 글자도 변형 금지 / 파생값 계산 금지 / 시한부 서술의 단정화 금지 / 이미지 생성 금지 / 강의 덱 테마(Pretendard·크림 F5F1E8·잉크 1F2732·네이비 263A52·오렌지 D56643·표지 다크 17212B).
모델은 Fable 5. 생성 후 3종 오염 검사(사실 반전·파생값·단정 강화)를 돌린다 — 감마 실측에서 셋 다 발생했고 클로드 디자인은 통과했지만 매회 검사한다.
Share → PowerPoint(Universal fonts) 다운로드 → `output/ppt/` 보관. 다운로드가 .tmp 로 저장되면 zip 시그니처 확인 후 .pptx 로 복사.
덱도 날짜별 분리다 (2026-08-12 확정): 매일 클로드 디자인에서 **전일 파일을 복제해
새 파일(제일기획 아침 브리핑 YYYY-MM-DD)로 만든 뒤** 그 파일에서 갱신한다 —
새 파일에서 Publish as artifact 하면 새 아티팩트 URL이 나온다. 전일 덱 아티팩트는
그대로 보존하고 절대 덮어쓰지 않는다. 발행한 URL을 `output/artifact-url-slides-YYYY-MM-DD.txt`에
기록하고 **당일 웹판 fmtbar의 프레젠테이션 링크에 당일 덱 URL을** 넣는다.
아카이브 호의 프레젠테이션 탭은 링크 없는 라벨(PPTX 배포본)로 남긴다.
덱 구성은 **당일 웹판 구성을 미러링해 매일 재구성한다** (2026-08-12 유착 사고에서):
전일 슬라이드 승계 금지. 상시 자료는 '지난 호 안내' 1장으로만. 표지 헤드라인 = 당일 웹판 헤드라인.
발행 이력: 08.12 덱 = 5cac5793-cd6d-4a64-aa34-7e438b9477fd (08.11 덱은 PPTX 파일로만 보존).
브라우저(claude-in-chrome)가 없으면 이 절은 건너뛰고 보고에 남긴다.

인포그래픽 원칙 (2026-08-11 세 번의 실측 실패에서):
- 순서도 장르 금지 — 다이아몬드·예/아니오·박스-화살표 사슬은 뭘 입혀도 CS 알고리즘으로 읽힌다
- 손글씨·스케치 테마 금지 — 두 번 시도 모두 조악했다
- 원칙: 절차가 아니라 사례가 주인공. 규칙은 캡션으로 내린다. 흐름은 띠·지류·밴드 같은 지면 은유로
- 빈 칸도 정보다 — "반증 아직 0건, 가장 기다리는 칸" 같은 빈자리를 지우지 않는다

독자 언어 게이트 (2026-08-11 관계도·분류·보류 3장 연속 반려에서):
- 날짜는 날짜로 표기 — "5.27" 같은 소수점 표기는 점수로 읽힌다. "5월 27일"로
- 명사 조각 금지 — "agentic 채택 5.27", "원인 출처 미확인, IR 원문 필요"는 편집실 메모다. 슬라이드 본문은 완전한 문장으로
- 내부 운영 체계(보강/반증/전환/무관 분류, "1/3" 카운터, "축으로 올린다" 규칙)는 본문 금지 — 필요하면 작은 캡션으로만
- 검사법: 슬라이드를 처음 보는 사람이 앞뒤 설명 없이 문장만 읽고 이해되는가. 안 되면 반려
- 취재 과정 언어 금지 (2026-08-12 재발에서): "~로 확인됐다", "일정 정정", "8.12 새벽 기준",
  "보도로만 알던" 같은 문구는 편집실 일지다. 기사 언어로: 사실 자체를 서술하고 출처는 srcline으로.
  확인·정정·조회 시각 기록은 원장(facts.jsonl)의 note 필드에만 남긴다

# 5b. 저장소 동기화 — 규칙이 바뀐 날은 반드시 한다
이 문서(SKILL)나 `templates/`, `tools/`, `profiles/` 가 바뀌었으면, 루틴 마지막에
킷 저장소(https://github.com/INTEGRITY2077/cheil-briefing-kit)에 반영한다:
1. 이 문서를 킷의 `routine-SKILL.md` 로 복사한다 (경로는 원본 유지 — 설치자가 치환한다)
2. `git add -A && git commit -m "규칙 갱신: <한 줄 요약>" && git push`
3. push 실패(인증 등)면 조작하지 말고 보고에 "저장소 동기화 실패"라고 남긴다
산출물(output/)과 개인 설정은 .gitignore 가 막는다 — 커밋 전 `git status` 로
.env·config.yaml 이 스테이징에 없는지 확인한다. 있으면 커밋하지 않는다.

# 6. 음성
config의 `tts.engine` 을 본다. 기본값 `edge` 는 완전 자동이다:
`python tools/make_audio.py output/script/YYYY-MM-DD.md` — 대본을 한 글자 그대로 낭독한다.
생성 후 파일 크기가 0 이 아닌지, 대사 수만큼 세그먼트가 찍혔는지 로그로 확인한다.
`sapi` 면 ko-KR 음성을 탐지해 생성하고, 없으면 대본까지만 만들고 그 사실을 남긴다.
`notebooklm` 은 정기 브리핑에 쓰지 않는다 (아래 함정 참조). 특별판에서 사람이 직접 돌릴 때만.

노트북LM 을 쓸 때의 함정 (2026-08-11 실측):
- "제일기획"을 "제일기업"으로 발음한 전례. 프롬프트에 발음 지시를 [가장 중요]로 넣는다
- "어~ 안녕하세요 청취자 여러분" 애드리브 오프닝 전례. 첫 문장을 문자열로 고정한다
- 길이 지시(2~3분)를 무시하고 5분 이상 생성한 전례. 대화상자의 「길이: 짧게」를 선택한다
- 요약 화면에서 연도를 2024로 틀린 전례. 오디오 검수 때 연도를 확인한다
프롬프트 원본은 `output/notebooklm/_맞춤설정_프롬프트.txt`. 실패 기록이 주석으로 붙어 있다.
생성 후 검수 없이 audio/ 로 옮기지 않는다: 첫 10초 청취, 수치 대조, 연도 확인.

# 7. 기록
- `../ledger/broadcast_log.jsonl` 에 오늘 읽은 개념 ID 추가
- 방송한 개념에 `broadcast_at` 기록
- 심층이었으면 `okf/log.md` 에 한 줄
- `okf/index.md` 재생성

# 8. 정리
- `stale_after` 지난 개념을 재확인 대상으로 표시
- 오디오는 `retention.audio_days` 초과분 삭제

# 파일을 여럿 다룰 때
아티팩트 원본과 배포 사본이 따로 있으면 **편집한 파일과 발행한 파일이 같은지 확인한다.**
복사 후 발행하고, 발행 전에 두 파일의 크기나 해시를 대조한다. 실제로 어긋난 적이 있다.

# 보고
짧게 쓴다.
- EVAL 점수와 등급
- 신규 사실 건수, supersede 건수
- 게이트 결과 (산출물별)
- 아티팩트 갱신 여부, 공유 버전 재지정 결과, **로그아웃 검증 결과**, **배포 시각 (07:45 이후인지)**
- 대본 경로와 예상 분량
- 사람이 할 것
- 확인하지 못한 소스가 있으면 그 사실과 이유

확인하지 못한 것을 확인한 것처럼 쓰지 않는다. 부분 실패는 부분 실패로 보고한다.
# 제품 이름 (2026-08-12 확정)
독자·수강생에게 부르는 이름은 세 가지뿐이다: **오늘의 제일기획 뉴스(웹판) · 라디오 · 프레젠테이션(슬라이드)**.
'화두판', '발표판', '특별판', '확정판' 같은 내부 작업명은 파일명·대화·보고 어디에도 다시 쓰지 않는다 —
내부 작업명은 결국 지면과 배포 문구로 샌다 (2026-08-11~12 반복 실측).
